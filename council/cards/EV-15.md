---
id: EV-15
title: "Default the control-plane URL to `https://relay.jumpseat.sh`"
state: Deliberating
owner: null
epic: EPIC-7
goal: Resolving the control-plane server URL with neither `PI_REMOTE_SERVER_URL` nor `piRemote.serverUrl` set and no stored credential falls back to `https://relay.jumpseat.sh`; the `/rc:login` URL prompt is retained as the interactive override affordance and is pre-filled with the resolved value; a stored credential's own `serverUrl` still wins over that default.
---

## Intent

The extension should work out of the box against Jumpseat's relay; today a fresh host must be
handed a URL. The default is the last-resort tier — below any stored credential — so a host
already enrolled against a self-hosted relay is not silently re-pointed. The URL prompt is
kept (pre-filled with the resolved value) as the interactive override affordance, and the
resolved relay stays visible at enrollment and in the `<serverUrl>`-rendering surfaces. The
constant needs one home and one resolver rather than three `??` chains (`index.ts:263`,
`index.ts:545`, load-time `index.ts:856`, `rcLoginCommand`).

## Acceptance

1. On a fresh host (no env override, no `piRemote.serverUrl`, no stored credential),
   `/rc:login` presents the URL prompt pre-filled with `https://relay.jumpseat.sh`; accepting
   the prefill (empty submission) enrolls, and the persisted credential's `serverUrl` is
   exactly `https://relay.jumpseat.sh`.
2. Precedence is pinned by test: env → setting → stored credential →
   `https://relay.jumpseat.sh`. A stored credential carrying
   `serverUrl: "https://custom.example"` still dials `https://custom.example/tunnels`, never
   the default; the prompt prefill reflects the resolved tier in each case.
3. The three runtime resolve sites (`rearm` `index.ts:263`, `rcCommand` `index.ts:545`,
   `rcLoginCommand`) consult one shared resolution function so they cannot diverge; the
   load-time value at `index.ts:856` remains the explicit-override-only tier, not the default.
4. With the total resolver, `login.failure.noServerUrl` is unreachable: its emission site and
   copy rows are removed in this PR and the removal is named in the PR description.
5. The `<serverUrl>`-rendering surfaces (footer/error copy) render the literal
   `https://relay.jumpseat.sh` when the default is in force, pinned by test.
6. `AGENTS.md`'s configuration-contract table and `docs/PI-SPEC.md` §7.2 and §8's `/rc` row are
   synced in the same PR.
## Deliberation record

### Round 1 — independent first pass (owner job-6.1, principal job-6.4 re-dispatch after job-6.2 timeout, designer job-6.3)

**owner (job-6.1):**

Grounding done. I read the three runtime resolve sites, the load-time site, the login driver's `noServerUrl` path, both copy tables, the footer render path, and the existing test pins. Position follows.

## Approach

A new `src/server-url.ts` becomes the single home: `DEFAULT_SERVER_URL` plus a total `resolveServerUrl` and a `normalizeServerUrl`. The controller receives the env and setting tiers as **separate deps** (replacing the collapsed `serverUrl` dep) so the full env → setting → credential → default precedence lives inside one testable function, not in a `??` at the entry seam. The three runtime sites (`rearm` index.ts:263, `rcCommand` index.ts:545, `rcLoginCommand` index.ts:610-617) all consult it; `noServerUrl` and `rc.serverUrlRequired` become dead and are deleted.

## 1. Proposed design

**One home — `src/server-url.ts` (+ `test/server-url.test.ts`, per the one-suite-per-module convention):**

```ts
export const DEFAULT_SERVER_URL = "https://relay.jumpseat.sh";

/** Trim; undefined/null and empty/whitespace-only → undefined. */
export function normalizeServerUrl(raw: string | undefined | null): string | undefined;

/** Total resolver. Tier order: env → setting → stored credential → DEFAULT_SERVER_URL.
 *  Total: always returns a non-empty trimmed URL. */
export function resolveServerUrl(input: {
  envUrl?: string | undefined;
  settingUrl?: string | undefined;
  credentialUrl?: string | null | undefined;
}): string;
```

**Dep shape change.** `RemoteControllerDeps.serverUrl: string | undefined` (index.ts:89) becomes `envServerUrl` + `settingServerUrl` (both normalized inside the resolver). The entry passes `process.env.PI_REMOTE_SERVER_URL` and `settingString("serverUrl")` through unchanged — env stays read at the entry only (DI discipline preserved; no env reads inside `src/`).

**Runtime sites:**
- `rearm` (index.ts:263-266): `const serverUrl = resolveServerUrl({ envUrl: deps.envServerUrl, settingUrl: deps.settingServerUrl, credentialUrl: cred?.serverUrl })`; the gate collapses to `if (!cred)` — the `!serverUrl` half is dead. `enrollment_expired` (index.ts:265, 293) now always renders the resolved URL.
- `rcCommand` (index.ts:545-549): same resolver call; the `!serverUrl` branch printing `rc.serverUrlRequired` is deleted, and both copy rows go (`login.ts:268` English table, `copy.ts:73` Indonesian table, plus the key list in `copy.ts`'s header comment and whatever `test/copy.test.ts` pins for the 22-key table).
- `rcLoginCommand` (index.ts:610-617): read the existing credential **before** the prompt (move the `readCredential` at index.ts:618 up), compute `resolved = resolveServerUrl({ envUrl, settingUrl, credentialUrl: existing?.serverUrl })`, then — attended mode — always show the prompt pre-filled with `resolved` ("Control-plane server URL [https://relay.jumpseat.sh]:", exact copy settled in the design pass). Empty/whitespace submission **accepts the prefill** and enrolls with exactly `resolved` (Acceptance 1); `undefined` (Escape, per the `ctx.ui.input` contract noted at index.ts:~625) aborts with footer unchanged, no failure copy. **Headless mode skips the prompt entirely and uses `resolved` directly** — a blocking interactive prompt inside the RFC 8628 non-interactive driver is indefensible once the resolver is total, and it's what makes "works out of the box" true for `--headless` on a fresh host. This is a behavior change vs. today (index.ts:610 prompts in both modes) and must be named in the PR and synced into §8's `/rc:login` row.

**Load-time tier stays explicit-only.** There is no collapsed load-time `serverUrl` value anymore; the entry passes the two raw explicit tiers and the controller resolves. Nothing on the load path can pick up the credential or the default — Acceptance 3's constraint holds structurally, not by convention. (This is the literal reading of the binding ruling: the resolver itself performs env → setting, so that precedence is pinned in `test/server-url.test.ts`, not only at an untestable entry line.)

**`noServerUrl` removal (Acceptance 4).** Three artifacts: the `LoginReason` union member (login.ts:52), the `FAILURE_ROWS` row (login.ts:123-127), and both emission sites — the command-layer check (index.ts:615-617) and the driver-side defense-in-depth gate `if (!deps.serverUrl)` in `discoverForLogin` (login.ts:410-411). Type-level unreachability follows from `resolveServerUrl` being total; the closed-reason-set pin in `test/login.test.ts:29` shrinks by one. Removal named in the PR description per the standing ruling.

**`<serverUrl>`-rendering surfaces (Acceptance 5).** `applyFooter` (index.ts:213) renders `activeHttp?.serverUrl ?? deps.serverUrl`; with the collapsed dep gone it needs the resolved tier. Rather than adding a disk read (`readCredential`) to every footer transition, capture the resolved URL where it's already computed: extend the tunnel variant of `ErrorSource` with the resolved `serverUrl` (every `TunnelError` already carries it — see index.ts:265) and/or a closure-scoped `lastResolvedServerUrl` set alongside `lastRearmReason` for the re-render path at index.ts:~234. Non-error footer states ignore `serverUrl` today (renderFooter, index.ts:155-159) and stay that way — showing the default in the `off` footer would claim an active dial target that doesn't exist (OJ5/Copy Honesty).

**Doc syncs (Acceptance 6, same PR):** AGENTS.md configuration table's resolution line becomes "env → setting → stored credential → `https://relay.jumpseat.sh` default; prompt is the interactive override, pre-filled"; PI-SPEC §7.2 gains the default tier + constant; §8's `/rc` row keeps J2 (prompt never fires from bare `/rc`) and the `/rc:login` row gains the prefill/headless behavior.

## 2. Edge cases

| Case | Proposed behavior |
| --- | --- |
| `PI_REMOTE_SERVER_URL=""` (empty) | Normalized to undefined → falls to setting tier. **Behavior change:** today empty wins via `??` (index.ts:856) and dies later in discovery as `unreachable`; falling through is the useful semantics. |
| Whitespace-only env/setting | Trimmed to undefined → next tier. |
| Setting present but `""` | `settingString` returns it (index.ts:858-861), normalizer drops it → next tier. |
| Stored credential without `serverUrl` (hand-edited file only — every enrollment path persists it: login.ts:414, 606) | Falls to `DEFAULT_SERVER_URL`. Acceptable because the total resolver is what makes `noServerUrl` unreachable; the `!cred` gate still stops cred-less hosts. |
| Prompt empty submission vs Escape | `""`/whitespace = accept prefill (enroll with resolved); `undefined` = cancel, footer unchanged, no copy. |
| Prompt shown when env/setting set? | Yes (attended) — Acceptance 2's "prefill reflects the resolved tier in each case" only tests if the prompt appears in all four tiers; it's also what makes it an honest *override* affordance. **Behavior change** vs index.ts:610-612. Flagged for convergence. |

## 3. Test plan sketch (Acceptance 1–5)

1. `test/server-url.test.ts` — resolver unit: full precedence matrix including env-beats-setting with both set; normalization (empty/whitespace env, empty setting, missing credentialUrl); totality (every input yields non-empty).
2. **A1** (`test/index.test.ts`): fresh host → `/rc:login` → `inputPrompt` called with a prompt containing `https://relay.jumpseat.sh`; resolve it to `""` → `createLoginCommand` receives exactly `https://relay.jumpseat.sh`; persisted credential's `serverUrl` is exactly that.
3. **A2** (precedence, wired): env+setting+cred → dials env; setting+cred → setting; cred only → fetch double sees `POST https://custom.example/tunnels` and never the default; prefill string asserted per tier.
4. **A3** (no-divergence): `rearm` with cred only → dials cred's URL; `rearm` with no cred → `enrollment_expired` carrying the default; `rcCommand` cred-only → dials cred's URL. All three go through the one function; any site hardcoding a `??` chain fails these.
5. **A4** (`test/login.test.ts`): `"noServerUrl"` removed from the reason-set pin (line 29) and the row fixture (line 242); the union deletion makes any residual emission a type error.
6. **A5**: default in force → force a tunnel-source footer error (e.g., rearm with no cred) → `setStatus` received copy containing the literal `https://relay.jumpseat.sh`, no placeholder/empty substitution.

Gates afterward per repo record: `bunx tsc --noEmit`, `bun test`, and the doc surfaces (AGENTS.md, PI-SPEC §7.2/§8) diffed in the same PR.

## 4. Falsifiable claims verified in code

- `rearm` resolve: `deps.serverUrl ?? cred?.serverUrl`, gate `!cred || !serverUrl` → `enrollment_expired` — index.ts:263-266.
- `rcCommand` gate prints `rc.serverUrlRequired` — index.ts:545-549; English row login.ts:268, Indonesian row copy.ts:73.
- `rcLoginCommand`: prompt fires **only when** `!deps.serverUrl` and **in both modes**; falsy prompt result → `login.failure.noServerUrl` + footer off — index.ts:610-617.
- Load-time value is `process.env.PI_REMOTE_SERVER_URL ?? settingString("serverUrl")` — explicit-only today — index.ts:856; an empty-string env currently wins and would surface as discovery `unreachable`, not `noServerUrl`.
- Footer renders `activeHttp?.serverUrl ?? deps.serverUrl` and only the error branch substitutes `<serverUrl>` — index.ts:213, 155-159.
- Driver-side `noServerUrl` gate in `discoverForLogin` — login.ts:410-411; union member login.ts:52; copy row login.ts:123-127; test pins test/login.test.ts:29,242.
- Enrollment always persists `serverUrl` — login.ts:414, 606.

## Tradeoffs accepted

- **Two new deps instead of one collapsed `serverUrl`** — more churn in `RemoteControllerDeps` and its test fixtures, in exchange for the full tier order being pinned inside one unit-testable function rather than split between an entry line and a two-tier resolver. The binding ruling's wording ("resolving env → setting → stored credential → default") reads literally.
- **Prompt becomes unconditional in attended mode and disappears in headless** — two deliberate behavior changes beyond the minimum diff; both are what make the card's out-of-the-box goal and Acceptance 2's "in each case" actually true, and both are called out for §8 sync.
- **Credential-without-URL now dials the default** instead of erroring — slightly riskier for hand-corrupted credential files, but it's the price of the ruled total resolver; the alternative (keep a failure row) contradicts Acceptance 4.

## Testable claims

- **Empty-env fallthrough**: today `PI_REMOTE_SERVER_URL="" bun test`-style wiring reaches discovery and fails `unreachable`; after EV-15 it must reach the setting tier. Pin in the resolver matrix.
- **Headless fresh host**: today `--headless` with no config blocks on an interactive URL prompt (index.ts:610 runs before mode selection); after EV-15 it proceeds to the device flow against the default with zero input. Pin in `test/index.test.ts`.
- **Totality**: `resolveServerUrl({})` returns `https://relay.jumpseat.sh` — the property that makes `noServerUrl` unreachable; pin in `test/server-url.test.ts` first (TDD red before the module exists).

**principal (job-6.4):**

## Cross-seam reading

**What the contract actually is today (verified):**

- The runtime resolve sites are three independent `??` chains over a load-time value that is itself already collapsed:
  - `rearm`: `const serverUrl = deps.serverUrl ?? cred?.serverUrl;` then `if (!cred || !serverUrl)` (`index.ts:263–264`).
  - `rcCommand`: `const serverUrl = deps.serverUrl ?? cred.serverUrl;` then `if (!serverUrl)` → `rc.serverUrlRequired` (`index.ts:545–549`).
  - `rcLoginCommand`: `let serverUrl = deps.serverUrl; if (!serverUrl) inputPrompt(...)` (`index.ts:612–614`), and critically it reads `existing = readCredential(...)` **after** the prompt (`index.ts:617`).
- `deps.serverUrl` is the load-time collapse `process.env.PI_REMOTE_SERVER_URL ?? settingString("serverUrl")` (`index.ts:856`), and `RemoteControllerDeps.serverUrl` is documented as "env > stored credential. May be undefined" (`index.ts:88–89`). So today there is **no** default tier anywhere.
- `noServerUrl` is emitted at `src/login.ts:410–412`, is a `LoginReason` union member (`src/login.ts:52`), has a copy row (`src/login.ts:123–127`) and `rc.serverUrlRequired` has a footer row (`src/login.ts:268`) plus an id overlay row (`src/copy.ts:73`) inside the "22-key" table (`src/copy.ts:77–84` is pinned by `test/copy.test.ts:77–85`).
- Footer `<serverUrl>` substitution uses `activeHttp?.serverUrl ?? deps.serverUrl` (`index.ts:213`) — i.e. the explicit override only. With the default in force and no active dial, `serverUrl` is `undefined` and `renderCopy` **skips the substitution**, leaving `<serverUrl>` literal (`src/copy.ts:118–124`). Acceptance #5 requires the literal default here.

**Proposed design.**

`DEFAULT_SERVER_URL` and `resolveServerUrl` live in a new dependency-free `src/server-url.ts` (mirrors the `copy.ts` precedent; nothing imported, no cycle risk). `src/pi-host.ts` is the acceptable alternative — it already owns host config (`readHostSettings`). One home, one resolver.

```ts
export const DEFAULT_SERVER_URL = "https://relay.jumpseat.sh";

export interface ServerUrlSources {
  env?: string | undefined;        // PI_REMOTE_SERVER_URL
  setting?: string | undefined;    // piRemote.serverUrl
  credential?: string | undefined; // stored credential's serverUrl
}

const nonEmpty = (v: string | undefined): string | undefined =>
  v !== undefined && v.length > 0 ? v : undefined;

export function resolveServerUrl(s: ServerUrlSources): string {
  return nonEmpty(s.env) ?? nonEmpty(s.setting) ?? nonEmpty(s.credential) ?? DEFAULT_SERVER_URL;
}
```

Total return type `string` is what makes `noServerUrl` unreachable. Every tier treats `""` as absent (else `??` short-circuits on empty env and the default never engages).

Wiring. `RemoteControllerDeps.serverUrl` is replaced by the two raw tiers `envServerUrl` and `settingServerUrl` (injected; **not** re-read from `process.env` inside the controller). This is the only shape that lets acceptance #2's per-tier tests avoid env mutation. `index.ts:856` still reads the same two sources and does **not** fold in the default; if a collapsed value is still wanted for anything, it stays `env ?? setting`.

Each runtime site:
- `rearm` (`index.ts:263`): read `cred`, then `const serverUrl = resolveServerUrl({ env, setting, credential: cred?.serverUrl })`; the guard narrows to `if (!cred)`. When `!cred`, pass the resolved `serverUrl` into the `enrollment_expired` `TunnelError`/footer so the relay is named, not `""`.
- `rcCommand` (`index.ts:545`): resolve with `credential: cred.serverUrl`; delete the `if (!serverUrl)` / `rc.serverUrlRequired` block entirely.
- `rcLoginCommand`: move `existing = readCredential(...)` **above** the prompt; `const resolved = resolveServerUrl({ env, setting, credential: existing?.serverUrl })`; then prompt unconditionally with `resolved` embedded in the prompt text as the prefilled value; `const serverUrl = input && input.length > 0 ? input : resolved`. Empty submission = accept `resolved`.
- Footer: cache `let resolvedServerUrl = resolveServerUrl(...)` once in the factory closure (read credential once at construction), refresh it wherever a site resolves, and change `applyFooter` (`index.ts:213`) to `activeHttp?.serverUrl ?? resolvedServerUrl`. That is the acceptance-#5 fix.

`noServerUrl` removal: drop the union member (`src/login.ts:52`), the `FAILURE_ROWS` entry (`src/login.ts:123–127`), the emission guard (`src/login.ts:409–412`), `FOOTER_ROWS["rc.serverUrlRequired"]` (`src/login.ts:268`), the id row (`src/copy.ts:73`), and update the "22 keys" coverage text and the pinned test (`test/copy.test.ts:77–85`) to 21.

## Blind spots

- **The import/pipeline view cannot see the host surface.** `inputPrompt` is typed `(prompt: string) => Promise<string | undefined>` (`index.ts:101`) and maps to `ctx.ui.input(prompt)` (`index.ts:877`). The real SDK's `ExtensionUIContext.input` is `(title, placeholder?, opts?)` (`types.d.ts:75`). **There is no prefill capability on the public surface:** `ExtensionUIDialogOptions` exposes only `signal`/`timeout` (`types.d.ts:37–42`); the interactive wrapper `showExtensionInput` forwards only `{ tui, timeout }` (`interactive-mode.js:2095`); the underlying `ExtensionInputComponent` ignores `_placeholder` (`extension-input.js:26,43`) and its `initialValue` (`extension-input.js:45`) is unreachable through `ExtensionUIDialogOptions`. So "pre-filled with the resolved value" cannot be delivered as editable prefilled text today. The card's own parenthetical — "accepting the prefill (**empty submission**)" — is the tell: the intended semantics is *display the resolved value; empty input accepts it*. That must be rendered in the prompt title (the placeholder is inert), and empty-submit must resolve to `resolved`. A seat that writes "prompt prefill" as if the host supports it is claiming a capability the seam does not have.
- **The frontend/render view cannot see the credential tier.** `applyFooter` has `activeHttp` and `deps.serverUrl` only; it never reads the credential (`index.ts:213`). Acceptance #5 (literal default in `<serverUrl>` copy) is unreachable without a resolved-value cache in the closure — a footer-only fix cannot get there.
- **The data/credential view cannot see that its empty string becomes a dial target.** `readCredential` accepts `serverUrl: ""` as valid (it only checks `typeof === "string"`, `src/credential.ts:224–235`). With a total resolver, a credential carrying `""` now silently dials `https://relay.jumpseat.sh` with a token minted elsewhere. Today that path printed `rc.serverUrlRequired`. This is the one place where "resolver is total" and "credential integrity" collide.

## Reframe

The framing is sound; no cardinal reframe. One bounded tightening: **the resolver must not be the only gate on credential validity.** Treat `credential: ""` as absent in the resolver (consistent with every tier), but tighten `readCredential` to reject an empty `serverUrl` as corrupt (`src/credential.ts:233`) so "no usable URL" surfaces as *not enrolled* rather than "dial the public relay with a foreign token." This keeps the resolver pure and total while keeping the credential store honest about what it vouches for. I would name that as an explicit edge decision in the PR, not silently absorbed.

## Testable claims

1. **Today, no default exists.** `createRemoteController({ ...no serverUrl... })` on a fresh host renders the `unreachable` copy with literal `<serverUrl>`, not `https://relay.jumpseat.sh` — because `errorSentence`/`renderCopy` skip `undefined` subs (`index.ts:141–149`, `src/copy.ts:118–124`). Run the existing index fixture with no `serverUrl` and assert the raw `<serverUrl>` token survives.
2. **`resolveServerUrl` purity.** `resolveServerUrl({ credential: "https://custom.example" }) === "https://custom.example"`; `resolveServerUrl({ env: "https://e", setting: "https://s", credential: "https://c" }) === "https://e"`; `resolveServerUrl({ env: "", setting: "https://s" }) === "https://s"` (empty env is absent); `resolveServerUrl({}) === "https://relay.jumpseat.sh"`. All without touching `process.env`.
3. **Prefill semantics against the real surface.** Construct a real host `ui.input` with the resolver value as `placeholder`; submitting empty returns `""` (not the placeholder) — `extension-input.js:43–47` returns `input.getValue()` on confirm, and `_placeholder` is unused. This is why empty→resolved must be explicit application logic, not a host behavior.
4. **Credential tier wins over default, loses to override.** With `envServerUrl`/`settingServerUrl` undefined and a persisted `serverUrl: "https://custom.example"`, `rcCommand` dials `https://custom.example/tunnels` (assert the injected `fetch` URL), never the default.
5. **`noServerUrl` unreachable.** After removing the union member and guard, grep proves zero references to `noServerUrl`/`serverUrlRequired` in `src/`; `Object.keys(loginReasonCopy).sort()` equals the 12-row set; `Object.keys(indonesianCopy).length === 21` (`test/copy.test.ts:83` updated).
6. **Footer literal.** With no override and no credential, `applyFooter("error", {kind:"tunnel", reason:"unreachable"})` yields a string containing `https://relay.jumpseat.sh`, not `<serverUrl>` — passes only once `applyFooter` consults the cached resolved value (`index.ts:213`).

**designer (job-6.3):**

## Design Position (EV-15, first pass)

**Design position** — Prefilling the `/rc:login` URL prompt with the resolved value is the right affordance: the prompt is the consent moment for *which* server enrollment targets, and a fresh host must see the URL they are about to be enrolled against, not a blank field. Accepting the prefill (empty submission) means "use the resolved URL" — the existing parenthetical at `index.ts:566` ("…or leave blank for PI_REMOTE_SERVER_URL / piRemote.serverUrl") is internal plumbing the user does not need to read and becomes wrong once a default is in force, so it is replaced by a stated sentence naming the resolved URL and the two user actions (Enter to use, type to override). The same prompt always fires (per Acceptance #1); only the prefill and the embedded URL change with the resolved tier — that is the honest way to honor "the prompt prefill reflects the resolved tier in each case" without inventing a different prompt per tier.

**Gulf closed** — Gulf of Evaluation, for a fresh host at the moment they run `/rc:login` for the first time: today the field is blank or shows internal-plumbing hints; after, the field reads "Enroll this host against `<resolved>` — press Enter to use this server, or type a different URL:" — the user perceives which server they are about to enroll against without archaeology, and the "default is Jumpseat's hosted relay" claim is no longer silent. Also closes a smaller Execution gulf for the power user who wants to point at a self-hosted relay: the prompt is the override affordance, the prefill is the suggestion, and an override is one keystroke away in either direction.

**Principle and evidence** —

- **Signifier (what tells the person it is possible)**: the prompt's prefill *is* the signifier for "this is the server that will be used if you press Enter." The current prompt at `index.ts:566` is a parenthetical about env-var plumbing, which is not a signifier of consent — it is documentation of internal plumbing. Replace it with a sentence that names the resolved URL and the two actions, mirroring the existing `tunnel.error.unreachable` English row at `src/login.ts:131` ("Cannot reach `<serverUrl>` — check your network and try again.") which already uses the `<serverUrl>` substitution discipline (FLLWUP-4 OJ5). The prompt becomes a locale-aware copy key — e.g. `login.urlPrompt` — rendered via the same `loginEnglishFor` + `renderCopy` path the error lines use, which keeps it inside the single-vocabulary rule and adds one row to the partial-coverage boundary in `src/copy.ts` (the 22-key table becomes 23).
- **Conceptual model (the wrong model the design must prevent)**: a fresh user must not form the model "blank field = skip / fall through to a hidden default." The current text invites exactly that model. The new sentence forces the model "I am consenting to *this* URL; I may type to override" — the empty-submission semantic change (Acceptance #1) is the forcing function that makes that model correct.
- **Feedback (what tells the person it worked)**: the persisted credential's `serverUrl` is exactly the prefill value (Acceptance #1) — pin that with a test that re-reads the credential file after empty submission, so the user-visible "what server am I on" matches the file on disk.
- **Constraint (a forcing function where the wrong action is costly)**: the prompt is the only step between a fresh host and a credential written to disk against a hosted service; if it is glossed (e.g. "press Enter to continue"), the design has invited the slip. Making it a stated sentence with the resolved URL rendered in-line is the constraint — the user cannot proceed without reading the URL they are consenting to.
- **Knowledge in the world, not the head**: the resolution chain (env > setting > stored > default) is not surfaced to the user — only the *result* is. The user is not asked to know which tier supplied the URL; if they care, they read PI-SPEC §7.2. This honors "stated refusal, never a glyph" (Copy Honesty Doctrine): one sentence names what will happen, the resolution chain is internal plumbing.

**Falsifiable predictions** —

1. *Fresh host, no env, no setting, no stored credential.* `/rc:login` prints a prompt whose literal text begins "Enroll this host against `https://relay.jumpseat.sh`" and ends "type a different URL:"; an empty submission produces an enrolled credential whose `serverUrl` field equals exactly `"https://relay.jumpseat.sh"` (byte-equal, not "starts with", not "contains"). Falsifier: capture the inputPrompt argument in a fixture harness, assert exact string equality; assert `readCredential({configDir}).serverUrl === "https://relay.jumpseat.sh"` after `cmd.run("attended", null)` resolves.
2. *Stored credential carries `serverUrl: "https://custom.example"`; no env; no setting.* Prompt prefill literal equals exactly `"https://custom.example"` — never the default, never empty, never `undefined`. The pre-`rearm()` resolve site returns `https://custom.example`; `createTunnel` is called against `https://custom.example/tunnels`; no `https://relay.jumpseat.sh` string appears anywhere in the recorded request log. Falsifier: pin the inputPrompt argument byte-equal; spy on `fetch` and assert `new URL(req.url).origin === "https://custom.example"`.
3. *`PI_REMOTE_SERVER_URL=https://env.example`, no stored credential.* Prompt prefill literal equals `"https://env.example"`. Same harness as #2 with the env injected; the inputPrompt argument byte-equals the env value. Falsifier: same as #2 with `process.env.PI_REMOTE_SERVER_URL` set.
4. *`piRemote.serverUrl=https://setting.example`, no env, no stored credential.* Prompt prefill literal equals `"https://setting.example"`. Falsifier: same as #2 with `readHostSettings` returning `{ serverUrl: "https://setting.example" }` and `process.env.PI_REMOTE_SERVER_URL` undefined.
5. *All four tier combinations* (env+setting+stored+default) produce a *single* resolved value across the three runtime sites (`rearm` `index.ts:263`, `rcCommand` `index.ts:545`, `rcLoginCommand`). Pure-seam test: extract `resolveServerUrl({ env, settings, stored, default })` to a pure function, assert all 16 four-input combinations return the documented precedence (env > setting > stored > default), and assert the three call sites consult the same function — the test reads the resolved value at each site for an identical input set and asserts byte-equality. Falsifier: change the resolver precedence in one place, the seam test goes red.
6. *Load-time value at `index.ts:856` is explicit-override-only.* With `process.env.PI_REMOTE_SERVER_URL` undefined and `readHostSettings({configDir})` empty, the `serverUrl` passed into `createRemoteController` is `undefined` — *not* `"https://relay.jumpseat.sh"`. This pins that the load-time tier does not include the default, per Acceptance #3. Falsifier: assert `deps.serverUrl === undefined` in a harness where env and settings are both absent.
7. *`rc.serverUrlRequired` and `login.failure.noServerUrl` are unreachable.* Their `loginEnglishFor` / `englishFor` references return the raw key (the resolver's echo-fallback), so any code path that still emits them is detectable. The emission sites at `index.ts:545` (`rc.serverUrlRequired`), `index.ts:566` (the noServerUrl branch), and `src/login.ts:411` are removed. Falsifier: a `grep` for `loginEnglishFor("login.failure.noServerUrl")` returns zero hits in `index.ts` and `src/login.ts`; a `grep` for `loginEnglishFor("rc.serverUrlRequired")` returns zero hits anywhere; a fixture test that exercises every previously-reachable path (no env, no setting, no credential, empty prompt submission) shows `loginEnglishFor` is never called with these keys.
8. *`<serverUrl>`-rendering surfaces show the literal default.* When the default is in force and a `tunnel.error.unreachable` line is rendered (English or Indonesian), the resolved string contains `https://relay.jumpseat.sh` and contains no literal `<serverUrl>` placeholder. Falsifier: drive a `TunnelError("unreachable", "control_plane_unreachable", DEFAULT_SERVER_URL)` through `errorSentence` and assert the result string equals `Cannot reach \`https://relay.jumpseat.sh\` — check your network and try again.` for `en`, and the matching substituted Indonesian row for `id`. Also assert no `renderCopy` call returns a string containing `<serverUrl>` when given a defined value (a static-grep over `src/` for the bare `<serverUrl>` token in `index.ts` output paths is a coarser second line of defense).
9. *Prompt text is locale-aware and substituted.* Under `setLocale("id")`, the prompt's literal text is the Indonesian rendering with `<serverUrl>` substituted to the resolved value, byte-equal to `Daftarkan host ini ke \`<resolved>\` — tekan Enter untuk menggunakan server ini, atau ketik URL lain:` (or its accepted Indonesian row). Under `setLocale("en")`, the English row. Falsifier: set the locale, capture the inputPrompt argument, assert byte-equal.
10. *Empty submission to the prompt is interpreted as "use the prefill."* After EV-15, `deps.inputPrompt(...)` returning `undefined` (or empty string) must not emit `login.failure.noServerUrl` and must not leave the footer at `not enrolled`; it must proceed to `cmd.run(mode, existing)` with the resolved URL. Falsifier: a fixture test that resolves `inputPrompt` to `undefined` and asserts the login driver receives the resolved URL and the persisted credential's `serverUrl` equals the resolved value.
11. *The prompt text is a stated sentence, not a parenthetical.* The inputPrompt argument contains no parenthetical about env-var names. Falsifier: a string assertion that the argument does not include `PI_REMOTE_SERVER_URL` or `piRemote.serverUrl` literals — those are implementation details.
12. *The new copy key lands in the announced partial-coverage boundary.* `src/copy.ts` lists 22 keyed surfaces; after EV-15 the count is 23 (the new `login.urlPrompt` row). The header comment in `src/copy.ts` is updated to reflect this — the boundary is announced at the surface itself (Fixture-Green Honesty). Falsifier: count the keys in `indonesianCopy` and the keys listed in the header comment; they agree, and the count is 23.

**Comprehension risks worth flagging** —

- *Silent default enrollment on shared/CI hosts.* A user on a CI box or a shared dev host who runs `/rc:login`, sees `https://relay.jumpseat.sh`, and presses Enter — intending to skip — is now enrolled against Jumpseat's hosted relay. The mitigation is *not* "hide the default" (that breaks the out-of-the-box goal); it is the prompt's stated sentence making "press Enter = enroll against this URL" structurally readable. This is the forcing-function half of the design.
- *The prefill hides the resolution chain.* A user with both `PI_REMOTE_SERVER_URL=https://env.example` and a stored credential for `https://stored.example` will see `https://env.example` — env > stored is correct per the binding ruling, but the user does not see *why*. Honest copy does not require explaining the chain; it requires naming what will happen.
- *Empty submission semantic change is a behavior change.* Today empty = fail; after, empty = use the prefill. Anyone with muscle memory for "Enter to skip /rc:login" will be enrolled. The fix is the stated sentence — the prompt must read such that a fresh user understands Enter = consent, not Enter = cancel.
- *The prompt text adds a 23rd keyed surface to a partial-coverage boundary.* This is a one-row expansion of the announced partial-coverage boundary in `src/copy.ts`, not a violation of it — the header comment is updated in the same PR.

**Preferences, ranked last** —

- *Where `<serverUrl>` appears in the prompt sentence.* I prefer it embedded directly in the clause that names the resolved server ("Enroll this host against `<serverUrl>`…"), not as a trailing parenthetical. Grounded — that mirrors `tunnel.error.unreachable` and the success line. Not strongly held; the working-seat design pass owns it.
- *Whether the prompt explicitly names the resolution tier ("from your stored credential", "the Jumpseat default").* I prefer *not* — that exposes internal plumbing in user-visible copy and the resolved-URL-by-itself is honest about what will happen. Taste.
- *Voice: "Enroll this host against" vs "Sign in to" vs "Connect to".* "Enroll" matches `login.attended.success` ("enrollment credentials saved"). Taste.

---

Ground rule notes for the other seats: I have not opened the running TUI; predictions #1, #3, #4, #9, #11 are interaction claims that the CDP smoke harness (or a fixture recording the `inputPrompt` argument) would falsify. Predictions #5, #6, #7, #8, #10, #12 are pure-seam or unit-test claims. The prompt-text wording is the working seats' shared design pass per the Phase-1 surface-copy ruling — this position pins the *shape* (stated sentence, `<serverUrl>` substitution, locale-aware), not the exact words.

### Round 1 record-integrity note

The first write of this record block was corrupted by the facilitator (a garbled heredoc appended fabricated position text under the owner label); it was truncated and rewritten from the seats' verbatim dispatch outputs before any seat consumed it. No seat read the corrupted text. The principal's original job-6.2 dispatch timed out mid-grounding with no position produced; it was cancelled and re-dispatched once (job-6.4) per dispatch discipline, and job-6.4's output is the principal's round-1 position recorded above.

### Round 2 — bounded exchange (owner job-7.1, principal job-7.2, designer job-7.3)

**owner (job-7.1):**

Round-2 position, owner seat. I re-verified the two claims my round-2 turns on: `readCredential` validates only `typeof parsed.serverUrl === "string"` (src/credential.ts:224–238 — `""` passes), and `rcLoginCommand` prompts in both modes before reading the credential, with `undefined` distinguishable from `""` at the seam (index.ts:610–617).

**(a) Where I converge**

1. **Prefill mechanism — converged, no dispute remains.** I accept the principal's seam verification in full: the installed SDK's `input` has no editable-prefill capability (placeholder inert, empty submit returns `""`), so "prefill" is prompt-text-carries-the-URL plus explicit app logic (`input && input.trim() !== "" ? input.trim() : resolved`). I adopt the designer's copy shape: a locale-aware copy key (`login.urlPrompt`) with `<serverUrl>` substitution through the existing `renderCopy` path — that satisfies OJ5 and the single-vocabulary rule, and exact wording stays with the Phase-1 `surface copy` delegation. Mechanism is now undisputed; drop my round-1 bracket-prompt string as the spec. One correction to the designer's prediction #12: the count does not become 23. `rc.serverUrlRequired`'s id row (copy.ts:73) sits inside the 22-key pinned table and is deleted in this PR — net is 22 (−1, +1).
2. **`readCredential` tightening — accept, and it is in-scope.** The principal is right, and the scope argument is the card's own blast radius: Acceptance 4 deletes the `!serverUrl` guards that are the only thing today catching `serverUrl: ""`. Removing that guard without tightening `readCredential` is a regression this card introduces, not pre-existing debt. The tightening is one predicate: empty/whitespace-only `serverUrl` → corrupt → `null` → host surfaces as not-enrolled. I withdraw my round-1 "falls to DEFAULT_SERVER_URL" for this case.
3. **Footer resolved-URL source — converge on the principal's closure cache, with one amendment.** Amendment: **do not read the credential at construction.** Initialize with `resolveServerUrl({ envUrl, settingUrl })` (no disk I/O) and refresh at each of the three resolve sites, where the credential is already in hand. The cache only needs to be correct by the time an error footer renders, and every path that renders a tunnel error has just resolved.
4. **Resolver contract — confirmed.** Name `resolveServerUrl`, tier order env → setting → stored credential → `DEFAULT_SERVER_URL` (names fixed by the Phase-1 ruling). Empty/whitespace tiers are absent. One delta: adopt trim-then-emptiness over the principal's length-only check.

**(b) Points I hold**

- **Headless skips the prompt and uses `resolved`.** A1 is an attended-mode claim ("presents the URL prompt"); A2's "the prompt prefill reflects the resolved tier" has no referent in headless. Today `--headless` on a fresh host blocks a non-interactive RFC 8628 flow on an interactive prompt (index.ts:610 runs before any mode handling) — the card's goal is false for that path unless headless uses `resolved`. The designer's consent argument has no purchase in a mode with no human at the keyboard. It's a named behavior change for §8's `/rc:login` row. The lines being touched (index.ts:610–617) are the same lines this PR rewrites, so splitting it buys churn, not safety.
- **Cancel-vs-accept at the prompt.** Prediction #10 treats `undefined` and `""` alike. I hold they are distinct: `""`/whitespace = accept `resolved`; `undefined` (Escape) = cancel — driver never constructed, footer unchanged, no failure copy. A fixture resolving `inputPrompt` to `undefined` and asserting the login driver is never constructed is the test; the designer's #10 as written would fail it.

**(c) Final design deltas vs round 1**

1. Prompt spec changes from bracket-prefill string to `login.urlPrompt` copy key, `<serverUrl>` substitution, resolved URL in the sentence; empty/whitespace submit → `resolved` as explicit app logic (not host behavior).
2. Credential `serverUrl` `""`/whitespace → `readCredential` returns null (corrupt), not resolver fallthrough to default.
3. Footer: closure-scoped `resolvedServerUrl`, initialized without a credential read, refreshed at the three resolve sites; `applyFooter` consults it. Drop the `ErrorSource` extension.
4. `normalizeServerUrl`/tier emptiness is trim-based.
5. Unchanged from round 1: `src/server-url.ts` home, two raw dep tiers replacing collapsed `serverUrl`, load-time site stays explicit-only, full `noServerUrl`/`rc.serverUrlRequired` removal, headless-skip, doc syncs (AGENTS.md, §7.2, §8).
6. New test pins: headless-fresh-host red today (blocks on prompt) → green after; `undefined` prompt result aborts without constructing the driver; copy-table count lands at 22; `readCredential({serverUrl:""})` → null.

**principal (job-7.2):**

**Cross-seam reading (new finding this round):** `src/copy.ts`'s header explicitly classifies the keyless `inputPrompt` literal as **"Remaining English under every locale, by design"**, alongside the 28 login-flow rows. The 22-key id table is pinned by `test/copy.test.ts:77-85` as `Object.keys(indonesianCopy).sort()).toEqual(expected)` where `expected = tunnelReasonCopy.userLineKeys ∪ Object.keys(FOOTER_ROWS)`. A new `login.urlPrompt` row would live in `NON_FAILURE_ROWS` (it feeds `englishDefaults`, src/login.ts:277-289) — **neither** of the two sets the drift alarm derives from. So the designer's prediction #12 ("count is 23") is true but incomplete: adding the id row makes the `toEqual(expected)` assertion fail unless the expected set is hand-edited away from its data-derived construction.

**Where I converge**

1. **Mechanism (all three, converged).** Prompt text carries the resolved URL; empty/whitespace submission is accepted *by explicit application logic* resolving to `resolved`; `undefined` (Escape) cancels with footer unchanged and no failure copy. I converge with the designer on shape and with the owner on the call-site order (read credential before the prompt; compute `resolved` once; embed it).
2. **Headless (converge with owner).** Skipping the prompt in the non-interactive driver follows from the resolver being total. **One condition I attach:** removing the prompt removes the only place the user names the target. Headless must print one line naming `resolved` — otherwise the consent argument that justifies the prompt in attended mode is silently dropped in headless.
3. **Resolver contract (converge on owner's shape).** `resolveServerUrl({ envUrl, settingUrl, credentialUrl })` returning `string` total; whitespace-trim, empty/whitespace → absent. I drop my `{env, setting, credential}` field names. I would **not** export `normalizeServerUrl`: no consumer outside the resolver; test normalization through `resolveServerUrl`.
4. **Footer source (converge on owner's ErrorSource, drop my cache).** My construction-time `resolvedServerUrl` cache is the weaker option and the owner's staleness point is correct: a login written after construction makes any construction-time credential read stale. Converge on **per-emission threading**: extend the tunnel variant of `ErrorSource` with `serverUrl?: string`, set it from `err.serverUrl` in `handleEnrollmentTerminal` (and record a `lastRearmUrl` for the `onTransportEvent`-synthesized source at index.ts:234-243), then `errorSentence` uses `activeHttp?.serverUrl ?? source.serverUrl`. No cache exists, so nothing can go stale; `renderFooter` already ignores `serverUrl` for non-error states, so Acceptance 5 needs only this path.
5. **Credential-empty and the two-deps change (converge).** Two raw deps `envServerUrl`/`settingServerUrl` is right. Empty credential `serverUrl` → absent in the resolver, as both of us said.

**Where I hold**

- **A. The prompt stays keyless; `login.urlPrompt` as a 23rd key is not the default shape.** `copy.ts`'s header **states as a design decision** that the `inputPrompt` literal stays English under every locale, and the 22-key table's drift alarm is data-derived, so a `login.urlPrompt` id row is a *silent reversal of an announced boundary* plus a *hand-maintenance of a set that is currently constructive*. Acceptance 1/2 mandate the URL substring and locale-independent wording, not locale-awareness. If the shared design pass wants the consent sentence localized, it must be named as a boundary expansion (add the en row to a home that feeds `englishDefaults`, add the id row, and change `test/copy.test.ts` to include the key in an explicitly-tagged covered set) — not folded in as "one more row." Falsifier: with `setLocale("id")`, a keyless prompt renders the English sentence; that is the current documented behavior. If any acceptance test requires Indonesian prompt text, the keyless shape is wrong and the boundary expansion is required.
- **B. `readCredential` tightening is a follow-up, not this card.** The corrupt-credential path is **not reachable through shipped enrollment code**: `rcLoginCommand` rejects empty `serverUrl` today, enrollment persists the resolved non-empty value, and post-EV-15 `resolveServerUrl` is total. The only writer of `serverUrl: ""` is a hand-edited file, and the consequence is a 401/`unreachable`, not a security boundary. Tightening `readCredential` changes an unrelated module's schema contract to cover an unreachable state — scope creep. Falsifier: if any test or enrollment path can persist `serverUrl: ""`, the "unreachable" claim is false and the tightening becomes in-scope.
- **C. Prompt fires in all four attended tiers (confirm the owner's flag).** Acceptance 2's "the prompt prefill reflects the resolved tier in each case" is only testable if the prompt fires in each tier. Honest consequence: a user with `env` set now sees a consent prompt they did not see before; the copy must make Enter=accept obvious.

**Blind spots (named separately)**

- **Owner's view:** cannot see that the *same* drift-alarm test is what the designer's *addition* collides with; the owner's removal plan ("update the pinned test to 21") and the designer's addition plan ("count is 23") are two edits to one data-derived assertion that neither position has joined. The owner's footer fix is also underspecified as "ErrorSource and/or a closure cache" — pick per-emission threading.
- **Designer's view:** models the copy key as a row addition and counts keys against the header comment, but cannot see that `indonesianCopy` is pinned by set-equality against an *intersection of two English tables*, and that a login-flow key has no home in either — so prediction #12 passes while `test/copy.test.ts` goes red.
- **My own round-1 blind spot:** I proposed a construction-time credential read into a footer cache; the owner's staleness observation is correct and I withdraw it.

**Reframe:** keep the resolver change and the copy-boundary change separate decisions. EV-15's *removal* (22→21) is mechanical; any *addition* (the prompt key) is a boundary expansion that needs its own justification and its own test-delta.

**Testable claims:** (1) copy-key collision: adding `"login.urlPrompt"` to `indonesianCopy` while `test/copy.test.ts` still derives `expected` from `tunnelReasonCopy ∪ FOOTER_ROWS` fails `toEqual` — run it; passing requires editing the derivation, not the count. (2) Headless names the target: with no env/setting/credential, `--headless` produces zero `inputPrompt` calls and printed output containing `https://relay.jumpseat.sh`; if not, I oppose the headless change. (3) Footer freshness: write a credential with `serverUrl:"https://custom.example"` after construction, then trigger `rearm` failure; the error footer must render `https://custom.example`, not a construction-time default. (4) Empty-env fallthrough pins. (5) Credential-empty unreachable: grep + test proves no shipped path writes `serverUrl: ""`; if none, the tightening is a follow-up.

**designer (job-7.3):**

**(a) Where I converge**

1. **Prompt shape: stated sentence → bracket form.** The principal's `extension-input.js:26,43,45` evidence is decisive: no editable prefill surface; `getValue()` returns `""` on confirm. My stated-sentence shape and the owner's bracket shape are mechanically the same thing — both render the resolved URL inline in the prompt title, both depend on the application layer to interpret empty submission. The bracket form is the correct adaptation: shorter, familiar convention ("default shown in brackets; empty input accepts the default"). I converge on the bracket form, dropping round-1 predictions #1–#4, #9, #11 in favor of a bracket shape that names the resolved URL as the default.
2. **Localization seam: the prompt copy key is required and lands in this PR.** Round-1 #12 (`login.urlPrompt` as a 23rd keyed surface) holds. The id overlay's announced partial-coverage boundary explicitly excludes the keyless prompt literal; the prompt's current keyless state is a *documented gap*, not a feature. After EV-15 the prompt is the first user-perceived text on a fresh host. The Acceptance substring that forces the new key is the goal's parenthetical: "the URL prompt is retained as the interactive override affordance and is pre-filled with the resolved value." The header comment in `src/copy.ts` and the pinned `test/copy.test.ts:77-85` array both update in the same PR, and the 22 → 23 boundary change is named in the PR description.
3. **Empty-submit semantics: the owner's mechanism mitigates the comprehension risks I flagged.** Escape is explicit, Enter is consent. It mitigates the resolution-chain risk cleanly. It mitigates the shared-host risk only partially — I accept the partial mitigation; the bracket form *is* the forcing function under the no-prefill constraint.
4. **Footer: the principal's cache composes with the tunnel-source enrichment; the principal's framing is the cleaner one because it works for all states.**

**(b) Points I hold with falsifiable predictions**

- **R2-1.** With no env, no setting, no stored credential, no active dial, the `off`-state footer literal equals exactly `"Off"` (en) / `"Mati"` (id) — byte-equal, no `<serverUrl>` token, no default URL substring. Falsifier: drive `applyFooter("off", null)` with the resolved-cache set to the default and assert against the literal row.
- **R2-2.** The `/rc:login` prompt fires unconditionally in attended mode across all four resolve tiers, with the literal title `Control-plane server URL [<resolved>]:`. Empty submission (`""` or whitespace) is applied as `serverUrl = resolved`; `undefined` (Escape) cancels — footer unchanged, no failure copy, no persisted credential. The headless path never invokes the prompt; the device-flow driver runs directly against the resolved URL. Falsifier: captured-args harness across the four-tier matrix.
- **R2-3.** Fresh host, empty submission: persisted credential's `serverUrl` equals exactly `"https://relay.jumpseat.sh"` (byte-equal).
- **R2-4.** `id` locale: captured `inputPrompt` argument contains no `PI_REMOTE_SERVER_URL`/`piRemote.serverUrl` literal; the Indonesian row of `login.urlPrompt` is the resolved copy.
- **R2-5.** `Object.keys(indonesianCopy).length === 23` and the `src/copy.ts` header comment lists `login.urlPrompt` alongside the existing 22 keys.

**(c) Final design deltas vs round 1:** #1–#4 drop (bracket form); #5–#8 hold; #9 holds as copy-key; #10 tightened (empty vs undefined distinct); #11 holds; #12 holds (23 keys); add "prompt fires unconditionally in attended mode" and "headless skips the prompt", both §8-synced. Preferences: square brackets over angle brackets (substitution tokens are `<name>`; bracket-default is a different convention); "Control-plane server URL" as the noun phrase (matches existing vocabulary). Both not strongly held.

**Open disputes entering round 3:** (1) copy-key boundary — owner+designer want `login.urlPrompt` keyed (owner: net 22; designer: 23) vs principal's "keyless is the announced design; a key is a named boundary expansion with its own test-delta"; (2) `readCredential` empty-`serverUrl` tightening — owner: in-scope regression prevention vs principal: follow-up (state unreachable through shipped code); (3) footer mechanism — owner's refreshed closure cache vs principal's per-emission ErrorSource threading (both now claim the other's staleness/freshness test passes).
