---
id: FLLWUP-8
title: "Wire the ui_prompt_start raise path end-to-end (runtime-observable acceptance for FLLWUP-5 contract b)"
state: Deliberating
owner: null
epic: EPIC-1
goal: Wire deps.on ui_prompt_start with manual PiEvent construction and registerPrompt on the canonical SDK payload so FLLWUP-5's pi.human_input.resolved becomes runtime-observable end to end
---

## Step 1 — path classification

- Full council (not mechanical): cross-seam (index.ts live-path wiring + translate.ts
  pure mapper) and spec-ambiguous/design-judgment — the orchestrator's Phase-1 record
  explicitly delegates the payload re-mapping fork to this deliberation: the SDK's
  `ui_prompt_start` carries `{type, reason:"ui_prompt", kind:UIPromptKind, title?}`
  while the synthetic seam expects `{event:"ui.confirm", promptKind, prompt}`; whether
  to add a backward-compatible PiEvent variant for the live shape or re-map into the
  existing synthetic shape is a design fork, as is whether the raised frame's data
  carries the SDK's `kind` verbatim.
- Surface-touching: yes — the remote client's approval UI begins receiving live
  prompts (the raise path becomes runtime-observable; same user-visible surface
  FLLWUP-5 seated `designer` for). `designer` seated as third generator in steps 2–3.
- Binding context carried in (not open for relitigation): manual PiEvent construction
  per FLLWUP-5 S-O2, never `ev as PiEvent`; dispatch names ruled in FLLWUP-3/EV-2 are
  stable keys (pi.human_input, pi.human_input.closed, pi.human_input.resolved,
  pi.human_input.stale, pi.human_input.fallback_to_steer — existing names may not be
  relitigated); steward R3 Side B (EV-6): wiring `deps.on("ui_prompt_start")` is
  OBSERVATION — the host already receives the prompt — not host-UI sponsorship, and
  the steering fallback is the permanent live resolution path; FLLWUP-9's typed
  `on()` union is live (`src/pi-sdk-on.ts`, `PiSDKOnEvent` includes `ui_prompt_start`
  per SDK types.d.ts:927) and any new subscription MUST typecheck against it; the
  `ui.confirm` synthetic seam is fixture-only (guard bridge returns early) and
  load-bearing for 5 tests (FLLWUP-9 deletion probe); the raise stamps
  (promptId, occurrence) onto the wire via `injector.registerPrompt({promptId, kind,
  prompt}) → {occurrence}` (EV-8 pattern, index.ts forward() ui.confirm special case);
  FLLWUP-5 contract (b) is fixture-green and this card's acceptance re-opens it to
  runtime-observable; J-REPLAY (FLLWUP-5 PO ruling): replay need not be
  self-sufficient — resolved surfaces only in the live stream after resync; gates =
  `bunx tsc --noEmit` exit 0 + `bun test` full suite green (172 baseline; add fixtures
  for the live raise path); no Mongo, no boot gate; FLLWUP-11/12 are explicitly NOT
  this card's scope; the FLLWUP-3 §4 runtime-unreachability caveat is scoped to the
  four FLLWUP-3 families only — wiring ui_prompt_start does NOT make those live, so
  the spec amendment in this PR covers only what this PR actually falsifies (the
  FLLWUP-5 §5.4 "fixture-green today, runtime-observable once the raise path lands
  (FLLWUP-8)" sentence becomes false on this PR and must be amended in the same PR).
- Facilitator-verified SDK evidence (grounding, not opinion): installed SDK
  `dist/core/extensions/types.d.ts:563-570` — `UIPromptKind = "select" | "confirm" |
  "input" | "editor" | "custom"`; `UIPromptStartEvent { type:"ui_prompt_start";
  reason:"ui_prompt"; kind:UIPromptKind; title?:string }` (UIPromptEndEvent identical
  shape, :572-577); emitted from `runner.js:270-300` `withUIPrompt` — depth-guarded,
  only the outermost prompt emits, `title` omitted when absent (`...(title ? {title}
  : {})`); typed `on("ui_prompt_start", ExtensionHandler<UIPromptStartEvent>)` at
  types.d.ts:927. `registerPrompt` is NOT an SDK symbol — it is the injector's own
  method (`src/inject.ts:66-69,179`): `registerPrompt(input: {promptId: string; kind:
  string; prompt: string}): {occurrence: number}`. Current translate.ts mapping:
  `ui.confirm` → CUSTOM `pi.human_input` data `{promptKind, prompt, schemaVersion:1,
  promptId: fnv1a(promptKind + "\u0000" + prompt)}` (translate.ts:525-539);
  `ui_prompt_end` → CUSTOM `pi.human_input.closed` (translate.ts:541-549).

## Intent

Filed from the binding FLLWUP-5 product-owner ruling (2026-08-31, item 5,
`vault/raw/2026-08-31-po-fllwup5-ruling.md`) and pre-confirmed by the
orchestrator under this canonical id. FLLWUP-5 shipped contracts (a) and (b)
fixture-scoped: the lifecycle layer emits `pi.human_input.resolved` on a
tracked resolution, but the raise path is dead in production — the installed
pi SDK has no `ui.confirm` event, `ExtensionAPI.on` is an exhaustively typed
interface, and no `deps.on("ui_prompt_start")` exists (FLLWUP-5 Skeptic probe
8), so `registerPrompt` is never called live and every live answer is
unknown → untracked → no resolved frame. This card wires the raise so the
remote approval flow is end-to-end runtime-observable.

Per the ruling's scope, the raise wiring is: (a) a `deps.on("ui_prompt_start", …)`
subscription; (b) manual construction of a `{event:"ui.confirm"}` PiEvent from
the SDK payload — note the SDK `ui_prompt_start` payload carries `{kind, title?}`
(`UIPromptKind: "select" | "confirm" | "input" | "editor" | "custom"`), NOT
`{promptKind, prompt}`, so the mapper needs a re-mapping or a
backward-compatible PiEvent variant; (c) wiring `registerPrompt` into the
host's response to `ui_prompt_start`; (d) updating `forward`'s `ui.confirm`
handler to handle both the synthetic and the (now live) path.

## Acceptance

Remote approval flow is end-to-end runtime-observable: a host that receives a
`ui_prompt_start` raises a `pi.human_input` CUSTOM frame with
`(promptId, occurrence)`, the host that resolves it emits
`pi.human_input.resolved` live, and a reconnecting client sees the resolved
frame in the live stream after replay. When this lands, FLLWUP-5's acceptance
(b) re-opens from fixture-green to runtime-observable. Gates: `bunx tsc
--noEmit` exit 0; `bun test` full suite green (155 baseline grows).
## Binding ruling rider (FLLWUP-3 general rule, product-owner — binding)

The dispatch names ruled in FLLWUP-3 and its consolidation are stable keys
(EV-2 Item 2) — this card may not relitigate them. Every new subscription uses
manual PiEvent construction per FLLWUP-5 S-O2, never `ev as PiEvent`. This
card's acceptance re-opens from fixture-green to runtime-observable as already
stated, and the FLLWUP-3 spec amendment's runtime-unreachability caveat
(scoped to the four new families only, per O-1) must be amended again in the
same PR that makes it false.

## Step 2 — independent first pass (round 1; owner job-7.1, principal job-7.2, designer job-7.3 — dispatched in parallel, each with only the card + binding contract)

**owner (job-7.1, verbatim).** The fork: new PiEvent variant, not re-mapping into the synthetic ui.confirm shape. Add `{ event: "ui_prompt_start"; kind: UIPromptKind; title?: string }` to the PiEvent union in src/translate.ts and a mapper case for it. Do NOT squeeze live SDK payloads into `{event:"ui.confirm", promptKind, prompt}`. Reasons: (1) src/pi-sdk-on.ts:23-26 contractually declares the synthetic seam "fixture-only … FLLWUP-8 folds it into the ui_prompt_start raise path," and it's load-bearing for 5 tests via the deletion probe — if live traffic re-shapes into ui.confirm PiEvents, the fixture-only seam now carries production traffic and the FLLWUP-9 probe's meaning dissolves; (2) S-O2 manual-construction discipline: the live payload is {kind, title?} — the SDK never sends a `prompt` string; re-mapping fabricates one; (3) does the raised frame's data carry the SDK's kind verbatim? Yes, verbatim, after validation — mirrors the existing ui_prompt_end handling, so raise and close frames are symmetric. Wiring: subscription alongside the existing ui_prompt_end handler, same narrowing pattern (isUIPromptKind guard, title string|undefined), forward({event:"ui_prompt_start", kind, title}). Mapper case emits CUSTOM name:"pi.human_input" (same stable frame name) with a backward-compatible data superset: {kind: input.kind, title: input.title, promptKind: input.kind, prompt: input.title ?? "", schemaVersion: 1, promptId: fnv1a(`${input.kind}\u0000${input.title ?? ""}`)} — value.pi is "ui_prompt_start" (name is the dispatch key everywhere, so safe, and gives honest provenance). forward()/registerPrompt: generalize the existing ui.confirm special case — stamp (promptId, occurrence) on ANY translated frame with name==="pi.human_input" carrying a string data.promptId, reading kind/prompt with fallbacks to promptKind/prompt; collapses the two-branch forward into one path; item (d) of the card face. Resolved-frame observability: nothing new needed — onInbound → injector.handle → resolved | steered_fallback(tracked) → emitResolved already exists; today dead because registerPrompt is never called live; once the raise registers, answers match pending entries → tracked → resolved fires live. Resolved frames sent directly on transport, not folded into replay — J-REPLAY satisfied by construction. Same-PR spec amendment: PI-SPEC.md:227 sentence becomes false on this PR, amended in it; ui.confirm fixture seam, guard bridge, 5 tests untouched. Tradeoffs accepted: redundant data fields (kind/title plus promptKind/prompt aliases) — ugly on the wire but the remote client is out of this repo and FLLWUP-11/12 are out of scope; prompt: title ?? "" alias is mildly fabricated when title absent — documented compat alias, empty so absence stays distinguishable; 32-bit fnv1a collision risk negligible; offline raises are silent (forward early-returns with no transport). Testable claims: (1) live raise e2e — h.emit("ui_prompt_start", {type,reason,kind:"confirm",title:"Allow rm -rf?"}) while live → relay receives exactly one CUSTOM pi.human_input with data.kind==="confirm" verbatim, data.title, data.promptId===fnv1a("confirm\u0000Allow rm -rf?"), occurrence 1, schemaVersion 1, value.pi==="ui_prompt_start"; (2) registerPrompt live wiring (the actual FLLWUP-5 re-open) — after the raise, broadcast pi.human_input.response envelope {promptId, occurrence:1, response:"yes"} with production resolvePendingPrompt:()=>false → steered_fallback tracked:true, sendUserMessages receives the response, pi.human_input.resolved with matching (promptId, occurrence) reaches relay; before this card the identical test yields tracked:false and NO resolved frame — the red→green boundary of the whole card; (3) occurrence counter on identical raises → 1 then 2; (4) title absent → frame emitted with data.title undefined, promptId===fnv1a("input\u0000"); (5) payload distrust — kind:"bogus" → no frame, no registration; (6) synthetic seam untouched — all 5 existing ui.confirm tests pass unmodified; (7) gates tsc 0 / bun test green on 172 baseline + new fixtures; (8) spec amendment grep-able — the line-227 sentence gone. Residuals: local-answer race (local user answers at host → pending never settles → later remote answer steers and emits tracked resolved for an already-answered prompt — follow-up card, not this scope); resolved semantics drift (always means "steered fallback surfaced" — spec amendment should state); alias-necessity question for the designer (does the client need promptKind/prompt at all — FLLWUP-11/12 adjacent, out of scope); value.pi provenance confirmation (client must not dispatch on it).

**principal (job-7.2, verbatim).** The raise side of the seam is asymmetric in a way the card's fork understates. The synthetic ui.confirm path carries {promptKind, prompt} where prompt is the message body (ui.confirm(title, message, opts)), and promptId = fnv1a(promptKind + "\u0000" + prompt) (translate.ts:525-539). The live SDK event carries {kind, title?} — and NO prompt at all. Confirmed in the installed SDK: UIPromptStartEvent {type, reason, kind, title?} (types.d.ts:565-570), and withUIPrompt(kind, title, run) captures only activeUIPrompt={kind, title} before emitting — the message/placeholder/prefill/options/factory args are passed to the UI call but never reach the event (runner.js:283-307); for custom, title itself is undefined (runner.js:280). So "re-map into the existing synthetic shape" is not a rename — it is impossible to do faithfully: the live event has no message body to put in a field named prompt. Any re-map stamps title into prompt, and the client renders the headline as if it were the question. Two further facts that decide the fork: (1) the synthetic ui.confirm shape is fixture-only — the entry point's on: closure early-returns for "ui.confirm" and never forwards it to the SDK, so no live client will ever see {promptKind, prompt}; the live client sees only whatever the new path emits; (2) the pi.human_input family's live shape is already {kind, title, schemaVersion:1} on the close side (pi.human_input.closed, translate.ts:541-549, FLLWUP-5 contract (a)) — the raise side should mirror it, not introduce a third vocabulary. Also: registerPrompt accepts {promptId, kind, prompt} but its body uses only promptId (inject.ts:179-190) — kind/prompt are vestigial, which is why the live path does not need a real prompt text to correlate. Reframe: the fork as posed is a false choice — the re-map is unfaithful by construction. The live raise is a new PiEvent variant, and its wire data carries kind verbatim (SDK UIPromptKind) plus title?, with promptId as a pure bucket hash over (kind, title) and occurrence as the true discriminator. Concretely: (1) PiEvent union (translate.ts:141) add {event:"ui_prompt_start"; kind: UIPromptKind; title?: string}; leave the ui.confirm variant byte-for-byte untouched (5 load-bearing fixtures stay green). (2) Mapper: new case "ui_prompt_start" emitting CUSTOM {name:"pi.human_input", value:{pi:"ui_prompt_start", data:{kind, title, schemaVersion:1, promptId: fnv1a(`${kind}\u0000${title ?? ""}`)}}} — the exact kind/title/schemaVersion shape of ui_prompt_end, plus promptId. No prompt, no promptKind key. (3) forward(): collapse the ui.confirm branch into a general post-translate stamp: for every emitted frame with name==="pi.human_input" and a string data.promptId, call injector.registerPrompt({promptId, kind: …, prompt: …}) and stamp occurrence. registerPrompt's kind/prompt are dead, so pass data.kind ?? data.promptKind ?? "" and data.title ?? data.prompt ?? "" (or, as a residual, simplify the signature to {promptId}). Removes the event-type special case entirely; future-proof for any other raise. (4) Subscription mirroring the existing ui_prompt_end subscription exactly; DepsOnEvent already includes "ui_prompt_start" so no pi-sdk-on.ts change; manual field validation, never ev as PiEvent. (5) Resolved observability: zero change to inject.ts or the onInbound → emitResolved path. Blind spots named: import/data view cannot see that the SDK runner discards the prompt body (would invent a value for prompt) nor that the synthetic seam is never live (would over-weight backward compatibility with a shape no production client consumes); frontend/render view cannot see that registerPrompt ignores kind/prompt (would assume the wire prompt text is load-bearing) nor that title is the only text the SDK ever surfaces (would design the approval card around a full question body that will be empty). Testable claims: (1) mapper purity + shape — runSequence([{event:"ui_prompt_start", kind:"confirm", title:"Allow rm -rf?"}]) → exactly one pi.human_input frame, value.pi==="ui_prompt_start", data has kind, title, schemaVersion:1, hex promptId, and NO prompt/promptKind key; two runs byte-identical promptId. (2) SDK-fidelity loss pinned — {event:"ui_prompt_start", kind:"custom"} → data.title===undefined and promptId===fnv1a("custom\u0000") (a constant); fails if someone "recovers" a prompt body that doesn't exist. (3) Live wiring — h.emit("ui_prompt_start", raw SDK shape, mirroring the existing ui_prompt_end probe at index.test.ts:555-556) → relay receives pi.human_input with occurrence:1; fails if the stamp only fires on ui.confirm. (4) End-to-end resolved (FLLWUP-5 (b) reopened) — after the live raise, broadcast pi.human_input.response {promptId, occurrence:1, response:"yes"} with default resolvePendingPrompt:()=>false → relay receives pi.human_input.resolved with exactly {promptId, occurrence:1, deviceId, ts}; fails if the live raise didn't registerPrompt (answer lands untracked → no resolved). (5) No synthetic regression — the 5 ui.confirm fixtures pass unchanged with the guard-bridge untouched. (6) Union gate — tsc 0 with the new variant and subscription; the FLLWUP-9 @ts-expect-error probe stays red-on-widen. Residuals: (1) SDK discards the prompt body — ui.confirm(title, message) emits only title; the remote approval UI can show kind + title, not the question; fidelity loss only the SDK can fix; route as a future SDK-boundary card; name it in the spec amendment's caveat; (2) promptId degenerates to a (kind,title) bucket; occurrence is the real discriminator, and the counter restarts per session; worth a one-line spec note that promptId alone is never a global identity for the live path; (3) value.pi provenance "ui_prompt_start" (honest, symmetric) — flag so the client doesn't key on pi; (4) registerPrompt's {kind, prompt} are dead params — follow-up cleanup to {promptId}, out of scope here (touches the EV-8 pattern and 5 fixtures).

**designer (job-7.3, verbatim).** Take Option B — re-map inside forward()'s new deps.on("ui_prompt_start", …) subscription, and leave translate.ts's PiEvent union unchanged on the raise side. Construct {event:"ui.confirm", promptKind:<sdk.kind>, prompt:<sdk.title ?? "">} by hand in index.ts and feed it to the existing forward() special case. Preserves the established wire shape, the forward() raise path's single read-site for registerPrompt, and the FLLWUP-9 deletion probe's five tests that still key on {event:"ui.confirm", promptKind, prompt}. The SDK's kind is carried into promptKind verbatim (with the isUIPromptKind guard already at index.ts:395). The SDK's title is the only field that plausibly fills prompt. This is a prompt-lossy re-mapping — kind round-trips, title does not — and that is the right trade because kind is the discriminator a client needs to render the right control (select/confirm/input/editor/custom), while title is free text the host UI already has and the SDK has not standardised as a wire field elsewhere. The forward special case at index.ts:400-415 already does exactly the work this needs: calls translate() with the synthetic {event:"ui.confirm"}, walks the resulting frame for promptId + promptKind + prompt, stamps registerPrompt({promptId, kind, prompt}). Re-using it means the live ui_prompt_start path lands through the same seam that today is only reachable from fixtures, and no new path duplicates its work. The only seam change in translate.ts is none. deps.on("ui_prompt_start", …) added in index.ts next to the existing synthetic deps.on("ui.confirm", …) handler (which becomes the harness-only seam — index.ts:668-671 already short-circuits ui.confirm at the pi.on bridge; remains load-bearing for the FLLWUP-9 deletion probe's five tests). The handler validates the SDK payload manually, never ev as PiEvent, constructs the synthetic PiEvent by hand; type-safety rides the FLLWUP-9 typed on() union (DepsOnEvent includes ui_prompt_start per pi-sdk-on.ts:12). Gulf closed: the evaluation gulf, for the remote user, at the moment a host actually needs their answer — today the remote client has no signal that a prompt exists, cannot perceive the raise, cannot see its kind, cannot answer it, cannot perceive the resolution; this card gives the client the same pi.human_input CUSTOM frame it has been parsing in fixtures, now landing live with (promptId, occurrence). Principle and evidence: mapping (control layout matches effect layout — adding a new variant would force a second mapper case and a second forward special-case, doubling the seam with zero user-facing benefit; smallest change that closes the named gulf); signifier (kind carried verbatim into promptKind — the UIPromptKind union is the client-perceived discriminator; FLLWUP-3's rename convention drops SDK-internal verbs, not SDK discriminators; kind is a discriminator, keep it); conceptual model (single prompt lifecycle, four surface names — Option B introduces no sixth name; Option A would, and that breaks dispatcher stability without adding value); knowledge in the world (a first-time client reader who sees promptKind:"confirm" knows what control to render; a private rename would force clients to learn pi-remote's vocabulary); purity (translate.ts cannot mint kind from title; cannot know the SDK's UIPromptKind union; cannot enrich the raise frame — Option A would either pull the SDK UIPromptKind type into translate.ts (breaking the pure fold's dependency boundary) or carry {kind, title} through and re-map inside forward(), at which point Option A reduces to Option B plus a new variant that exists only to be unwrapped again). Falsifiable predictions: P1 single read-site — grep case "ui_prompt_start" in src/ shows zero matches in translate.ts (source-assertion); P2 single special-case — the existing single ui.confirm branch in forward() unchanged plus exactly one new deps.on("ui_prompt_start") subscription that builds the synthetic PiEvent; P3 SDK kind round-trip — fixture h.emit("ui_prompt_start", {type, reason, kind:"select", title:"Pick a port"}) → resulting pi.human_input CUSTOM's value.data.promptKind==="select" and value.data.prompt==="Pick a port"; P4 title absence produces empty prompt — emit with no title → value.data.prompt==="" and registerPrompt({promptId, kind, prompt:""}) still called; P5 FLLWUP-9 deletion probe stays green — the five tests keying on {event:"ui.confirm", promptKind, prompt} pass unchanged (route to skeptic: assert none of those five tests were touched); P6 typecheck — tsc 0 with the new deps.on("ui_prompt_start") call site constructing a synthetic PiEvent; P7 FLLWUP-5 designer round-2 identity preserved — raise wire frame still carries (promptId, occurrence), a client parsing promptId+occurrence keeps working with no client change. Tradeoffs accepted: title is the only field with plausible client-side use and it is squeezed into prompt (honest for confirm/input kinds; lossy for select — the SDK's full prompt text may include options not surfaced; remote UI is best-effort for those kinds, kind-driven and re-prompt); kind is the SDK's literal union, not a renamed client concept; no new PiEvent variant — translate.ts union stays at its FLLWUP-5 footprint (if a future SDK event carries a richer payload it will need a new variant and a remap conversation); forward()'s ui.confirm special case is now reachable from both paths (one raise path, one source of (promptId, occurrence) stamping, one registerPrompt call site). Residuals: prompt for select/editor/custom may be empty or wrong — product call whether a remote UI is best-effort for those kinds or a richer SDK payload is needed (route to product-owner as J-prompt-text item, not blocking); fallback_to_steer loud-once notice still fires only on the response path, never on the raise (raise-time explanation of the fallback would be a separate card — route to product-owner, not FLLWUP-8 scope); spec caveat wording moves (FLLWUP-8's wiring makes ui_prompt_start reachable — the caveat/amendment is a single sentence riding the PR); FLLWUP-9 deletion probe → skeptic assertion that none of the five tests were touched; test-harness h.emit("ui.confirm", …) still emits the synthetic event, the new h.emit("ui_prompt_start", …) fixture feeds the SDK payload shape (mirrors the existing ui_prompt_end shape fixture at index.test.ts:556).

### Round outcome (facilitator record)

The fork is live with a 2-vs-1 shape: owner + principal want a NEW PiEvent variant `{event:"ui_prompt_start", kind, title?}` (mapper-mapped, CUSTOM pi.human_input, value.pi "ui_prompt_start"); designer wants re-mapping at the subscription site into the synthetic {event:"ui.confirm", promptKind, prompt} shape with translate.ts untouched. Within the variant camp there is a second divergence: owner adds backward-compat aliases (promptKind/prompt duplicated alongside kind/title); principal carries NO aliases (data exactly {kind, title, schemaVersion:1, promptId}; "no prompt, no promptKind key"). Principal surfaced two factual claims that, if verified, are decisive against the designer's position: (F1) the SDK's ui_prompt_start carries NO prompt body — re-map is unfaithful by construction (runner.js:283-307; custom kind has no title at all); (F2) registerPrompt's body uses only promptId — kind/prompt are vestigial (inject.ts:179-190), undercutting designer's "single read-site needs promptKind/prompt" argument. Designer's P1/P2 predictions and its "Option A would introduce a sixth name" claim partially mischaracterize the variant camp (both variant positions keep the pi.human_input dispatch name). Round 2 dispatches with each seat seeing the other two round-1 positions. The designer's residual questions (J-prompt-text best-effort UI for select/editor/custom; raise-time fallback explanation) are noted for the consolidator.
