---
id: EV-11
title: "Tunnel lifecycle spec — POST /tunnels, signed one-time URL, DELETE, error taxonomy"
state: Deliberating
owner: null
epic: EPIC-2
goal: docs/SERVER-SIDE-SPEC.md specifies the tunnel lifecycle REST surface — the POST /tunnels request and response shapes, the signed one-time wss URL with TTL and single-use consumption, the DELETE semantics, and an error taxonomy covering every credential and tunnel failure case the client distinguishes — as a self-contained section.
---

## Intent

The tunnel lifecycle, written on the page in full: the POST request (session
id, session name, cwd, host metadata; Bearer access token), the response
(`{tunnelId, url, tokenTtl}`), the signed self-describing token whose claims
are embedded (no server lookup state), single-use consumption at WS upgrade,
TTL default and bounds, DELETE semantics (idempotent, discards token state),
and the error taxonomy. The taxonomy must distinguish — in the document's own
words — the two credential failure classes the client treats differently:
invalid/expired/revoked credentials (retry with re-enrollment) versus
valid-but-insufficient-scope credentials (re-consent plus an administrator
scope grant), because the client renders different remedies for them. Also
specified: the no-double-create expectation while a tunnel is live, and the
consumed-token replay rejection at upgrade.

**Self-containment requirement:** the section cites no client type names and
no PI-SPEC sections; the error taxonomy is stated as the server's contract with
its own reason vocabulary, which the council-side check confirms is exhaustive
over the client's handled cases.

## Acceptance

- POST and DELETE are specified with complete request/response shapes and
  idempotency semantics.
- The signed URL's claims, TTL, single-use consumption, and replay rejection
  are specified to implementation precision.
- The error taxonomy covers, in the document's own vocabulary, every failure
  case the client distinguishes — council-side check against the client's
  TunnelError handling confirms zero gaps and zero contradictions.

## Deliberation record

### Round 1 — independent first pass (owner job-20.1, principal job-20.2, 2026-09-02)

**Step-1 record:** full council (spec-ambiguous: TTL bounds, signed-URL encoding, taxonomy vocabulary, no-double-create semantics); NOT surface-touching (server-facing spec prose, no product copy/state) — no designer seat.

**owner (job-20.1), verbatim:**

All grounding reads are done: the spec's §1–§2 in full, the card, `src/tunnel.ts` with its reason/status mapping, `test/tunnel.test.ts` fixtures, PI-SPEC §7.2/§8, `src/transport.ts`'s upgrade-failure path, and the wiki (index, `tunnel.ts`, `Reason Taxonomy`). Here is my position.

**Approach**

The section becomes §3 with six subsections mirroring the lifecycle: **3.1 Tunnel objects and lifecycle** (what a tunnel is, its states, the one-connection rule, the no-double-create expectation), **3.2 Tunnel creation — `POST /tunnels`**, **3.3 The signed connection URL** (shape, token encoding, claims, TTL), **3.4 Token consumption at the WebSocket upgrade** (single-use mechanics, replay rejection, handshake failure statuses), **3.5 Tunnel deletion — `DELETE /tunnels/{tunnelId}`**, **3.6 Error taxonomy**. Each subsection fixes the wire contract normatively and carries at most one Guidance block (R2/R3). Everything below is restated on the page in the document's own vocabulary — no client type names, no PI-SPEC refs, no module paths (R5).

**Detailed position**

**§3.1 Tunnel objects and lifecycle.** A tunnel is a (tenant, host session instance) pair with a server-assigned `tunnelId` — an opaque, URL-safe string, globally unique across all tenants. Lifecycle normatively fixed as: *created* (POST success) → *live* (its single connection token consumed at a successful upgrade) → *spent* (connection ended) or *deleted* (DELETE). Normative rules: **exactly one connection token per tunnel and exactly one successful upgrade per tunnel** — a spent tunnel is never re-connectable; a new connection requires a new POST. This matches the reference behavior where a reconnect re-creates the tunnel, and it makes single-use concrete. **No-double-create: both sides, asymmetrically.** The host-side expectation is normative: a host MUST NOT issue a second create while a tunnel it created is live (the correct behavior is a local no-op with a notification, per the already-live acknowledgment). Server enforcement is MAY: a server MAY reject a create that would produce a second *live* tunnel for the same (tenant, sessionId) with `409`; if it does not enforce, it MUST issue a fresh tunnel. Spent tunnels MUST NOT block a new create.

**§3.2 POST /tunnels.** `Authorization: Bearer <access token>` REQUIRED (§2.6/§2.7 semantics). Body `application/json`, all four fields REQUIRED: `sessionId` (non-empty string, the host session's stable identifier, treated as opaque), `sessionName` (non-empty string, display name), `cwd` (non-empty string, informational — the server MUST NOT interpret it), `hostMetadata` (JSON object with string keys and string values, MAY be empty, informational). Validation the server MUST do: reject a missing/unparseable body, a missing or wrong-typed field, or a non-object `hostMetadata` with `400 invalid_request`. The server MUST accept all values within these maxima and MAY reject beyond them with 400: sessionId ≤ 128 chars, sessionName ≤ 256, cwd ≤ 1024, hostMetadata ≤ 32 entries with keys and values ≤ 256. Unknown request fields MUST be ignored (§2.1's rule inherited). The request carries **no TTL field**; the TTL is server-chosen (§3.3). Tenant is derived from the token alone (§2.6). Response **`200`** (not 201 — one shape) with `application/json`: `tunnelId` REQUIRED (URL-safe: unreserved URL characters only, so it embeds in paths without escaping), `url` REQUIRED (absolute `wss://` URL per §3.3), `tokenTtl` REQUIRED (positive integer seconds, equal to the issued token's actual validity window — the dialer converts it to an absolute expiry). `sessionId`/`sessionName`/`cwd`/`hostMetadata` are retained as tunnel metadata and relayed to granted client devices; nothing here interprets them (INV-1 spirit extended to metadata).

**§3.3 The signed URL.** Shape: `wss://<data-plane host>/<tunnelId>?token=<token>`. Normative: scheme MUST be `wss` (`ws` MUST NOT be issued); the token rides in exactly one query parameter named `token`; the path's final segment MUST equal the `tunnelId`; the authority MAY differ from the control-plane origin (a dedicated data-plane host is permitted); no userinfo, no fragment. The token is a **compact JWS** — three base64url segments — signed with a server-held key; hosts never hold signing keys. Guidance (one recommendation): asymmetric EdDSA with a rotating key pair, so token verification can be delegated to data-plane replicas. Claims, all binding: `tenantId` REQUIRED (MUST equal the creating enrollment token's `tenant_id` — INV-3's second contract point), `tunnelId` REQUIRED (MUST equal the path segment), `sessionId` REQUIRED (MUST equal the create request's `sessionId`), `exp` REQUIRED (epoch seconds), `jti` REQUIRED (unique per issued token — the consumption identity). `iat` SHOULD. **TTL:** default 60 seconds (SHOULD), normative bounds: an issued TTL MUST be ≥ 30 s and ≤ 300 s; `tokenTtl` in the response MUST equal `exp` minus mint time. Below the floor a dialer with a slow link cannot reliably reach the upgrade; above the ceiling a leaked URL lives too long (INV-5). Expiry is judged by the **server's clock** at upgrade only — the server MUST NOT rely on any host clock; a host's local pre-expiry check is advisory.

**§3.4 Consumption at the upgrade.** The upgrade is the WebSocket opening handshake — an HTTP GET against the URL. Authentication happens strictly before `101`: a server MUST NOT accept the upgrade and reject afterwards. Before completing the upgrade the server MUST, in order: verify the signature (`401` on failure); check `exp` against its clock (`401` on expiry); verify the path/claim bindings — path tunnelId = claim `tunnelId`, and the tunnel exists and is not deleted (`401` on any mismatch or absent tunnel); then **atomically consume** the token by recording its `jti` as consumed *before* sending `101`. A second handshake presenting a consumed `jti` MUST be rejected with **`409 token_consumed`** — the replay rejection — regardless of whether the original connection is still live. Guidance: the consumed-set entry need only live until `exp` plus a small skew allowance, after which expiry rejection alone covers the token, so the store garbage-collects itself. Handshake failure statuses (closed set): `400` malformed URL/token, `401` (bad signature, expired, binding mismatch, deleted/unknown tunnel), `409` replay, `5xx` data-plane unavailability; success is `101`. These are diagnostic for operators: the dialing side treats any non-101 as a failed handshake with one uniform remedy — create a fresh tunnel and dial again. It does not parse handshake statuses.

**§3.5 DELETE /tunnels/{tunnelId}.** Bearer-authenticated; tenant derived from the token. Idempotent. Success: **`204 No Content`**, no body. Unknown, already-deleted, expired, or **another tenant's** tunnel id: **`404`** — never 403, so existence is not leaked across tenants, and a repeated delete is indistinguishable from a first success. "Discards token state" precisely means the server MUST do all of: (a) invalidate any un-consumed connection token for that tunnel — subsequent upgrade attempts with it are rejected `401` per §3.4; (b) close the live data-plane connection if one exists (a normal WebSocket close); (c) release tunnel-scoped relay state (cached frame window, fan-out registrations); (d) record the id as deleted so further DELETEs return `404`. Deletion is permanent; the same session returns only via a fresh POST. Statuses: `204`, `401`/`403` per §2.7, `404`, `5xx`. A failed delete notification must be safe to ignore: the server SHOULD expire spent and abandoned tunnels itself, so a host that proceeds with local teardown despite a failed notification leaves no orphan.

**§3.6 Error taxonomy.** The §2.7 body (`{"error", "error_description"}`) and the never-merge-401/403 rule extend to every non-2xx in this section. Vocabulary: inherited `invalid_token`, `insufficient_scope`; new codes defined here — `invalid_request` (400), `tunnel_not_found` (404), `tunnel_already_live` (409, server-enforced double-create), `token_consumed` (409, replay), `internal_error` (5xx). The normative closed status partitions: POST `{200, 400, 401, 403, 409, 5xx}`; DELETE `{204, 401, 403, 404, 5xx}`; handshake `{101, 400, 401, 409, 5xx}`; a server emitting any other status on these surfaces is non-conformant. Coverage of the connecting side's classes, restated in the document's own words: (i) **invalid/expired/revoked credential** → control-plane `401`, remedy re-enrollment; (ii) **valid-but-insufficient scope** → `403`, remedy re-consent plus administrator grant (§5 surface); (iii) **connection-layer failure** (DNS/TCP/TLS, request never completes) → no HTTP status exists; no server obligation can be stated; the remedy is check network and retry; (iv) **response-shape defense** → a 2xx body deviating from §3.2/§3.5 is emitted only by a non-conformant server; the connecting side's validation failure maps to no status; (v) **failed delete notification** → any DELETE non-2xx other than 401/403/404 — with this taxonomy only 5xx can produce it; the host correctly proceeds with local teardown regardless (§3.5's expiry rule makes that safe); (vi) **handshake failures** → §3.4's closed set, uniform remedy re-create. **The one refinement of §2.7 this section must state explicitly:** `401` at a *control-plane* tunnel endpoint means the enrollment credential is dead (re-enroll); `401` at the *data-plane handshake* means the connection token is unusable (re-create the tunnel). Same status, different credential, different remedy — disambiguated by surface, and stated as such so §2.7's "status alone selects the remedy" rule stays true per-surface.

**Tradeoffs accepted**

- **409-on-POST renders as a generic server error for the host.** I keep server-side double-create enforcement MAY rather than MUST precisely because a host that (wrongly) triggers it would show a misleading "try again" line — a conformant host never sends the second create, so the gap is unreachable in conformant operation. If a future ruling makes enforcement MUST, the client-side reason set needs a new member, which per the closed-vocabulary discipline requires its own ruling — I flag this now.
- **`404`-only for DELETE idempotency, no `410` alternative.** One shape (R3), even though the reference client also treats 410 as success. Client leniency toward a status the page forbids is not a contradiction.
- **CamelCase tunnel-token claims (`tenantId`) vs §2.6's snake_case enrollment claims (`tenant_id`).** Deliberate: the connection token's declared claim vocabulary is already fixed in the host-side contract this section mirrors; renaming it here would break the one place the two documents must describe the identical token. The inconsistency is cosmetic and I'd rather keep the claim names identical across both descriptions of the same wire object.
- **`jti` REQUIRED** rather than SHOULD: single-use consumption needs a stable identity; hashing the raw token string is the fallback I'm deliberately *not* offering (R3).

**Testable claims**

1. **Upgrade-failure uniformity:** the transport treats any non-101 handshake outcome as a failed dial (`onerror` → the unreachable class) and re-creates the tunnel with backoff — it never parses handshake statuses. Falsifiable check: `src/transport.ts` lines ~380–400 resolve `opened=false` without reading any HTTP status. Therefore the page's 400/401/409/5xx handshake set cannot contradict the client, and the Skeptic should confirm the council check does not demand status-specific client behavior on the data plane.
2. **wss-only vs client leniency:** the client's scheme gate accepts both `ws` and `wss` (`WSS_SCHEME` in `src/tunnel.ts`), so a page mandating `wss`-only still passes every shipped fixture (`happyCreateResponder` issues `wss://`). Falsifiable: `bun test test/tunnel.test.ts` passes unchanged with a wss-only rule; it would fail only if the council check required the page to permit what the client merely tolerates.
3. **DELETE partition closure:** with DELETE statuses fixed to `{204, 401, 403, 404, 5xx}`, every observable client outcome is covered: 2xx and 404/410 → idempotent success, 401 → re-enrollment, 403 → re-consent, 5xx → the teardown-failure class with local teardown proceeding. A page that adds 400 or 409 to DELETE would open an uncovered gap the client folds into teardown-failure — the Skeptic's exhaustiveness check should fail any draft that widens that partition.
4. **TTL floor risk:** the 30 s floor must cover the host's worst path — one silent refresh (two HTTPS round-trips) plus POST plus dial — before the dialer's local advisory pre-check (`now() > expiresAt` → the url-expired class) rejects a valid URL. Falsifiable prediction: on a deployment with sub-second TLS round-trips this never fires; sustained >15 s round-trip latencies could, and the page's "pre-check is advisory" sentence is what keeps that a deployment concern rather than a conformance contradiction.
5. **Clock authority:** the host computes absolute expiry from its own clock, so host-server clock skew beyond the TTL causes the local pre-check to reject a token the server would accept. The page must place expiry authority solely in the server clock (§3.3) or the Skeptic can construct a skew scenario the two documents resolve differently.

**principal (job-20.2), verbatim:**

## Cross-seam reading

The contract that already exists on the two sides of the seam, before this card writes anything:

**Host side (the shipped client — reference behavior only):**

- `src/tunnel.ts` `createTunnel` POSTs `${serverUrl}/tunnels` with `Authorization: Bearer <token>`, `content-type: application/json`, body `{sessionId, sessionName, cwd, hostMetadata}` — **camelCase keys**, and `hostMetadata` is `Record<string,string>`. It parses the response as `{tunnelId: string, url: string, tokenTtl: finite>0}` and validates `url` against `/^wss?:\/\//` (scheme only — **it never inspects the path or the query parameter name**), `tunnelId` against `typeof === "string"`, `tokenTtl` against finite > 0. It computes `expiresAt = now() + tokenTtl*1000` locally.
- `throwForStatus` is the client's real dispatch table: create/refresh → `401`⇒`enrollment_expired`/`unauthenticated`, `403`⇒`enrollment_rejected`/`forbidden`, `>=500`⇒`server_error`, **any other non-2xx** (so 400, 404, 409, 429…) ⇒ `server_error`; DELETE → `401`/`403` checked first, `404`/`410` handled *before* as idempotent success, network failure swallowed, and **everything else (including 5xx) ⇒ `teardown_failed`**. The body is never parsed for dispatch; the client acts on status alone (§2.7's stated design).
- `src/transport.ts` `rearm` = a fresh `POST /tunnels` **with no DELETE in between**, called on every reconnect. Any failed upgrade (any HTTP status from the handshake) collapses to `relay_unreachable`; `url_expired` is computed **client-side** from the local `expiresAt`, never from a server response.

**Server side (what §3 inherits):**

- §2.6 already fixed the JWT-claim convention as **snake_case** (`sub`, `tenant_id`, `scope`, `exp`, `iss`, `iat`).
- §2.7 already fixed the 401/403 rule (never merged; body `{"error","error_description"}`; 401=`invalid_token`; 403=`insufficient_scope`) and the body shape §3 must reuse.
- §2.5 fixed that `pi-remote:host` is the sole tunnel-operations scope.
- §1.4 INV-3 and INV-5 bound §3: the tunnel token is the second tenancy contract point, signed, short-lived, single-use, self-describing "where feasible".

**The four seams that will bite an implementer drafting this section:**

1. **camelCase vs snake_case is split across surfaces, not uniform.** The host-visible JSON body is camelCase (`sessionId`, `tunnelId`, `url`, `tokenTtl`) — these names are fixed by the host's parser and cannot be "restated". The JWT claims (§2.6) and the error body (§2.7) are snake_case. §7.2's prose says the tunnel token's claims are camelCase (`tenantId`, `tunnelId`, `sessionId`) — but **the host never decodes the tunnel token** (opaque, dialed as-is; only scheme-checked), so the token's claim names are a server-internal contract and §3 is *free to align them with §2.6's snake_case* (`tenant_id`, `tunnel_id`, `session_id`). Importing §7.2's camelCase claim names would contradict §2.6. This is the single most likely contradiction the council check would catch.
2. **§7.2's URL path is `/tunnelId` and the token param is `?token=`, but the test fixture uses `wss://tunnel.example/live?sig=abc`.** The fixture is not normative — the host's regex accepts any `wss?://` — so §3 must *fix* the normative shape (`wss://<data-plane-origin>/<tunnelId>?token=<jwt>`) as a server-emitted-and-server-validated contract, and the "zero contradictions" check must not treat the fixture's `sig`/`/live` as a wire requirement.
3. **"No server lookup state" in the card's Intent is over-broad and, taken literally, false.** Self-describing means claim *verification* needs no lookup. But single-use consumption (replay rejection) is inherently stateful, and "DELETE discards token state" means a post-DELETE token whose signature and `exp` still verify must still be rejected — which requires a live-tunnel existence check. §3 must name **two bounded pieces of required server state** — the consumed-token (`jti`) record (retained ≥ TTL) and the live-tunnel existence record — or a conformant implementer will claim statelessness and get replay rejection wrong.
4. **The client's create-path error mapping is coarser than the server's vocabulary.** On create, 400 (`invalid_request`), 409 (`tunnel_already_live`), and 5xx all land in one host class (`server_error`), while DELETE's 5xx lands in a *different* class (`teardown_failed`). A taxonomy that says "5xx always ⇒ server_error" contradicts `tunnel.test.ts` (`deleteTunnel 500 → teardown_failed`). The section must state the server's fine vocabulary **and** the host's coarse grouping of it, without pretending the host distinguishes 400 from 500.

**Blind spots**

**The import/data-pipeline view (`tunnel.ts`'s author) cannot see:** that the reconnect path POSTs a fresh tunnel with no prior DELETE (`transport.ts` `rearm`). From inside the REST client, "no-double-create" naturally means "reject a second POST for a session that has a row" — but that reading deadlocks reconnection. Only the transport side can see that "live" must key on *host socket connected*, not *tunnel row exists*.

**The render-path/frontend view (`transport.ts`'s author) cannot see:** that the server's WS-upgrade rejection status codes (401 expired, 403 replay, 404 unknown) are all invisible to it — every failed handshake is `relay_unreachable`, and `url_expired` is a purely local pre-check. So a frontend-vantage position will under-specify the upgrade failure vocabulary as "doesn't matter, it's all unreachable", when those codes *are* normative server behavior the council check reads against.

**The §2-author view (enrollment side) cannot see:** that the tunnel response body is camelCase and that the token is opaque to the host — so it will want to harmonize everything to snake_case and miss that §3 must deliberately hold two conventions side by side (camelCase host JSON body, snake_case JWT claims + error body).

**The card-intent view cannot see:** that "no server lookup state" collapses two different properties (claim-verification statelessness vs. replay/post-DELETE statefulness). This is the one phrase in the card that earns a reframe.

**Reframe**

The framing is sound — the card names the right surfaces, the right single-use/replay/DELETE questions, and the right 401-vs-403 inheritance. One reframe, scoped to the card's own Intent:

> **Reframe "no server lookup state" to "no lookup required to *interpret the token's claims*."** Single-use consumption and DELETE-discard are stateful by definition; the section must name the two bounded server-state pieces (consumed-`jti` set retained ≥ TTL, and live-tunnel existence) as *required*, not as an embarrassing exception to a claimed statelessness.

Second, a definition the card leaves open and must not leave open: **"live" = the tunnel's host data-plane WebSocket is currently connected.** The 409 no-double-create key is socket state, never row existence, precisely because the reconnect path POSTs without DELETE.

Beyond those two sharpenings, the design writes itself. Concrete §3:

**§3.1 Endpoint location and authentication.** Tunnel endpoints are fixed paths on the configured control-plane origin (the origin of §2.1 discovery) — `POST /tunnels`, `DELETE /tunnels/{tunnelId}` — not discovered. Every request MUST carry `Authorization: Bearer <access_token>`; §2.7's 401/403 contract governs, and is not restated. Tenancy is derived from the token's `tenant_id` alone (§2.6); the body carries no tenant field, and any tenant-like body field MUST be ignored.

**§3.2 `POST /tunnels`.** Request body (all REQUIRED, camelCase — the host wire names): `sessionId` non-empty string (opaque; the logical connection id), `sessionName` non-empty string, `cwd` non-empty string, `hostMetadata` object of string→string (server MUST store verbatim, MUST NOT interpret — INV-1). Validation: malformed JSON / missing or mistyped / empty-string field ⇒ `400` `invalid_request`; unknown fields MUST be ignored. Success ⇒ `200 OK`, body `{tunnelId, url, tokenTtl}` — `tunnelId` REQUIRED string, server-assigned, globally unique, URL-path-segment-safe; `url` REQUIRED absolute `wss:` string; `tokenTtl` REQUIRED positive integer seconds. (camelCase here is a wire contract the host parses; do not "snake_case" it.) No-double-create: if a live tunnel already exists for the authenticated `(tenant_id, sessionId)` pair, the server MUST answer `409` `tunnel_already_live`; otherwise POST MUST succeed. "Live" is defined in §3.1-adjacent prose as *host data-plane socket connected* — a prior never-connected tunnel (POST succeeded, host never dialed) or a closed socket does NOT block; a new POST MAY (SHOULD) supersede any prior unconsumed token for that session, which is harmless regardless (single-use + `exp`).

**§3.3 The signed one-time URL.** Shape: `wss://<data-plane-origin>/<tunnelId>?token=<jwt>` — `wss` (TLS) MUST, path MUST be exactly one segment equal to `tunnelId`, token MUST ride in a query parameter named `token`. Token encoding: a JWT (three dot-separated base64url segments, RFC 7519), signed by a server-held key (algorithm stack-neutral; MUST verify signature before trusting claims; mint-and-verify are same-side, so the alg is server-internal). Claims — **snake_case, aligned with §2.6, not §7.2's prose**: `tenant_id` REQUIRED (INV-3 tenancy), `tunnel_id` REQUIRED (MUST equal the path segment — a mismatch ⇒ `401 invalid_token`), `session_id` REQUIRED (echo of the POST), `exp` REQUIRED (= issuance + `tokenTtl`), `jti` REQUIRED (unique, the replay marker), `iat` SHOULD, `iss` SHOULD, `aud` SHOULD (`wss:<data-plane-origin>`). State requirement made explicit: claim verification is lookup-free; replay rejection and post-DELETE rejection are not — the server MUST retain (a) consumed `jti` values for ≥ `tokenTtl` after issuance, and (b) live-tunnel existence.

**§3.4 Token lifetime.** Default `tokenTtl` 60 s; bounds `[5, 300]` s, normative (server MUST emit within them; configured values clamp to the nearest bound). Binding rule: `exp − iat == tokenTtl`, so the host's locally-computed `expiresAt` and the server's enforcement window are the same number. Upgrade MUST reject any token past `exp` ⇒ `401` `token_expired`. Guidance (non-normative): allow a few seconds' clock leeway when comparing `exp` to the server clock, and retain consumed `jti` for `tokenTtl + leeway`; the host's local pre-emption and the server's `exp` check are independent clocks, and leeway absorbs a slightly-ahead host clock.

**§3.5 Single-use consumption and replay rejection.** At upgrade: verify signature + claims + path-binding + `exp` + live-tunnel existence; then **atomically** mark `jti` consumed and complete `101`. Two simultaneous upgrades with one token: exactly one `101`, the other gets the replay rejection. Rejections are HTTP responses before `101`, with the standard error body: `401` `token_expired` (past `exp`), `401` `invalid_token` (unverifiable / malformed / claim-path mismatch), `403` `token_already_used` (replay — authenticates but spent), `404` `unknown_tunnel` (deleted or never existed). These are data-plane rejections, explicitly scoped as NOT the §2.7 control-plane 401/403 contract (that contract governs enrollment-token requests only); the 401/403 semantics here mirror it (401 = does not authenticate, 403 = authenticates but no longer authorized). Post-upgrade socket close codes are §4's business — §3 defers there.

**§3.6 `DELETE /tunnels/{tunnelId}`.** `tunnelId` is one URL-encoded path segment; MUST be tenant-scoped — a tunnel outside the token's tenant is treated as non-existent (`404`, no existence leak). Success ⇒ `204 No Content`, empty body. `404` (never existed) and `410` (already deleted) are **idempotent success**, empty body, not errors — repeat DELETE MUST NOT be a 5xx or 409. "Discards token state" precisely: on successful DELETE the server MUST (a) terminate the host socket if connected (close code per §4), (b) delete the tunnel's existence record so a subsequent upgrade of even an unexpired, signature-valid token gets `404`, and (c) purge the consumed-`jti` record. `401`/`403` per §2.7 still apply; any other 4xx/5xx is a server fault that the host reports but does not retry (best-effort teardown).

**§3.7 Error taxonomy (the closed vocabulary).** Body shape inherited from §2.7: `{"error":"<code>","error_description":"<sentence>"}`; the host acts on status alone, never the body. One table, columns *status / surface / `error` code / meaning / host-side class (restated) / remedy*:

| Status | Surface | `error` | Meaning | Host class | Remedy |
|---|---|---|---|---|---|
| 401 | POST, DELETE | `invalid_token` | credential invalid/expired/revoked | re-enrollment | re-enroll (§2.7) |
| 403 | POST, DELETE | `insufficient_scope` | valid, missing `pi-remote:host` | re-consent | re-consent + admin grant (§2.7) |
| 400 | POST | `invalid_request` | malformed/mistyped body | generic retryable | correct the request |
| 409 | POST | `tunnel_already_live` | live tunnel exists | generic retryable | defense-in-depth; not normally seen (host refuses while live) |
| 5xx | POST | `server_error` | internal fault | generic retryable | retry |
| 5xx/other 4xx | DELETE | (any) | teardown fault | teardown failure, non-blocking | local teardown proceeds; no retry |
| — (no response) | any | — | network/DNS/TCP/TLS/discovery | unreachable | check network/URL |
| 2xx malformed | POST | — | success body fails response table | validation | server conformance violation; discard response |
| 401 | WS upgrade | `token_expired` / `invalid_token` | expired / unverifiable | dial-failed | re-dial ⇒ rearm (fresh POST) |
| 403 | WS upgrade | `token_already_used` | replay | dial-failed | re-dial ⇒ rearm |
| 404 | WS upgrade | `unknown_tunnel` | deleted/absent | dial-failed | re-dial ⇒ rearm |

Conformance sentence to close §3.7: a 2xx MUST never carry an error body, and a 4xx/5xx MUST never carry a usable tunnel (no partial success on POST).

**Testable claims**

These are disagreements-as-tests, each runnable against the repo as it stands:

1. **The tunnel token is opaque to the host.** `grep -E "tenant_id|tenantId|tunnel_id|session_id|decode|atob|jwt|jti" src/tunnel.ts src/transport.ts` must return nothing that parses the `url`'s query string — only the `WSS_SCHEME`/`WS_URL_RE` scheme checks. If any host code reads a token claim, §3's claim naming is *not* server-internal and must match the host, contradicting the "align with §2.6 snake_case" choice.
2. **`rearm` POSTs without DELETE.** Trace `src/transport.ts` `dialLoop`'s `deps.rearm()` path: it must reach `createTunnel` without any `deleteTunnel` call. This is what forces "live = socket connected, not row exists"; if a §3 draft keys 409 on row existence, reconnection deadlocks and this trace refutes it.
3. **The client's create/delete error mapping is asymmetric.** `test/tunnel.test.ts` already pins `deleteTunnel 500 → teardown_failed` while `createTunnel 500 → server_error`. A §3 taxonomy claiming uniform "5xx ⇒ server_error" is contradicted by the existing test; the taxonomy must reproduce this asymmetry.
4. **The client lumps 400/409/5xx on create.** `throwForStatus` has no branch between `403` and `>=500` for the create/refresh kinds, so 400 and 409 fall through to `server_error`. Any §3 statement implying the host renders a distinct remedy for `invalid_request` or `tunnel_already_live` is a contradiction; the taxonomy must mark them one host class.
5. **§2.6 claim convention is snake_case.** `grep "tenant_id" docs/SERVER-SIDE-SPEC.md` at §2.6 vs any camelCase claim names §3 introduces — a §3 that emits `tenantId` (camelCase) contradicts §2.6's own JWT table and fails the "must not contradict §2" check even though §7.2's prose uses camelCase.
6. **Clock-skew boundedness.** With `tokenTtl >= 5` and default 60, a host/server clock delta up to ~`tokenTtl/2` is absorbed by the host's pre-emption; beyond it the host dials a server-expired URL, gets `401` at upgrade, and self-heals via rearm. Falsifiable claim: no code path depends on the two clocks agreeing within less than the TTL; verify by inspection that `expiresAt` is the *only* expiry the host consults before dialing.

### Round 2 — bounded exchange (owner job-20.3, principal job-20.4, 2026-09-02)

**owner (job-20.3), verbatim:**

All grounding checks are done — I ran the principal's greps, traced `throwForStatus`, `deleteTunnel`, and the `rearm` path, and read §1.4–§2.8 of `docs/SERVER-SIDE-SPEC.md` plus the EV-2/EV-8 design records. Here is my round-2 position.

**Dispute 1 — claim naming: CONCEDE to snake_case (`tenant_id`, `tunnel_id`, `session_id`).** The decisive evidence is principal-claim #1, which I ran: `grep -nE "tenant_id|tenantId|tunnel_id|session_id|decode|atob|jwt|jti|URLSearchParams|query" src/tunnel.ts src/transport.ts` returns nothing (exit 1). The host scheme-checks the URL and dials it verbatim; it never reads a claim. My round-1 reason for camelCase ("the host-side contract describes the same token") is not page-writable under R5 and, worse, leaves §3's JWT table camelCase while §2.6's own table (read this round: `sub`, `tenant_id`, `scope`, `exp`, `iss`, `iat`) is snake_case — a within-document contradiction I can't justify in-document. The page-writable grounding for snake_case: the server issues the URL for verbatim presentation at the handshake; the token's claim names are a server-internal contract, and §2.6's claim convention governs every JWT this document specifies. `jti` stays REQUIRED.

**Dispute 2 — replay status: CONCEDE to `403 token_already_used`.** The document's own semantic axis (§2.6–§2.7) is: 401 = does not authenticate; 403 = authenticates but insufficient. A replayed token authenticates — signature valid, unexpired, binding intact — and is merely spent. My 409 was REST-resource intuition imported from `tunnel_already_live`; conceding also keeps 409 meaning exactly one thing on this surface (double-create state conflict). Since the dialer treats every non-101 uniformly, there is no client consequence either way; the choice is normative hygiene, and the principal's is more consistent with §2 as the reader sees it. One condition: the page must state explicitly that handshake 401/403 *mirror* §2.7's semantics axis but are the data plane's own contract with its own codes — otherwise a reader applies §2.7's `insufficient_scope` meaning to the handshake 403.

**Dispute 3 — DELETE idempotency: HOLD `404`-only.** Three arguments, none of which I made round 1. (a) §1.5 makes the status partitions normative closed sets: permitting 410 without requiring it yields *two conformant shapes for one semantic*, which is exactly the R3 one-shape violation — and "already deleted" vs "never existed" is the very distinction 404-only exists to erase. (b) 404-only is the stronger privacy posture and is consistent with the page's own cross-tenant rule: a repeat DELETE is indistinguishable from a first, and deletion leaves no observable history. 410 re-introduces the "this id existed" signal we elsewhere refuse to leak. (c) The reference client's 410 tolerance is leniency toward a status it never needs (`src/tunnel.ts:427` — `404 || 410` → return, same path); §1.7 says the document governs over the repository. Verified this round: `test/tunnel.test.ts:300-319` pins 404 and 410 both resolving, so a 404-only page contradicts no shipped fixture. If the principal wants 410 acknowledged at all, the only R3-clean place is outside the partition — and I'd rather not spend a conformance exception on it.

**Dispute 4 — no-double-create: CONCEDE to server MUST `409 tunnel_already_live`, keyed on socket state — and I withdraw my "needs a new client reason" objection as factually wrong.** Verified: `throwForStatus` (`src/tunnel.ts:274-293`) for create/refresh has no branch between `403` and the fall-through, so 400, 409, and 5xx all land in the existing generic retryable class. No closed-vocabulary extension is needed; my round-1 tradeoff rested on a false premise. The facilitator's two questions: yes, socket-state keying removes my deadlock objection, and no, MUST-enforcement doesn't conflict with the client vocabulary. But the page must add one normative clause the principal's position lacks: **a socket that is closed or fails the server's liveness detection MUST be treated as not live** — otherwise the half-open-socket case (server still believes the old socket connected after a network partition) 409s a legitimately reconnecting host for the duration of stale-socket detection. That is acceptable *because* the host's 409 remedy is the generic retryable class with backoff, so the delay self-heals; but the clause must be stated or the MUST is unimplementably vague. Remaining spec content: never-connected tunnels and spent tunnels MUST NOT block; a new POST SHOULD supersede prior unconsumed tokens (harmless — single-use + `exp`); the host-side MUST-NOT-create-while-live expectation stays normative.

**Dispute 5 — TTL floor: CONCEDE to `[5, 300]` with clamp, default 60.** Arguing it on my own grounds: the floor's real protective work is done by two sentences, not the number — (i) expiry is judged solely by the server's clock at upgrade, and (ii) configured values clamp to the nearest bound rather than failing. With (ii), a misconfigured 2 s becomes a workable 5 s instead of a boot failure, which is strictly better than my "MUST be within [30, 300]". The residual skew hazard (host clock ahead by more than the TTL makes every dial fail the local pre-check) is a deployment concern bounded by rearm self-heal, and the floor can't fix it — a 30 s floor fails at 31 s skew just as a 5 s floor fails at 6 s. Take the clamp semantics; I won't defend 30 on taste.

**Dispute 6 — unknown/deleted tunnel at upgrade: CONCEDE to `404 unknown_tunnel`.** The token authenticates; the resource is gone. Folding that into 401 conflates "credential unusable" with "credential fine, tunnel absent" — and once replay moves to 403 (dispute 2), the data plane inherits §2.7's clean axis and 404 is the honest third slot. No existence leak: an attacker without a valid signature is rejected 401 before any tunnel lookup, so 404 is visible only to token-holders. Binding mismatch (path ≠ `tunnel_id` claim) stays `401 invalid_token` — the principal and I already agree there. One synthesis on codes: keep the handshake 401 vocabulary to a **single** `invalid_token` (signature, expiry, malformed, binding mismatch) rather than splitting `token_expired` out — matching §2.7's minimalism; codes are diagnostic-only since the dialer acts on status alone. Handshake closed set becomes `{101, 400, 401, 403, 404, 5xx}`.

**The principal's two reframes: both ACCEPTED.** (a) "No lookup required to interpret the token's claims," with the two named state pieces — consumed-`jti` set (retained ≥ TTL + leeway, self-GC'ing after `exp`) and live-tunnel existence record — stated as REQUIRED server state. This was always what my §3.4 did; the reframe is the honest way to write it without contradicting the card's "self-describing." (b) "Live" = host data-plane socket currently connected, accepted as the normative definition, with my liveness clause above.

**The principal's testable claims, run:**
- #1 (token opaque) — **VERIFIED** (grep exit 1, zero claim/query parsing in host code).
- #2 (rearm POSTs without DELETE) — **REFUTED as stated, conclusion survives.** `transport.ts` only invokes `deps.rearm()` (`src/transport.ts:353`); the closure is EV-8-authored, and EV-8's own design record (`docs/superpowers/specs/2026-08-31-EV-8-design.md` §3: "It performs **DELETE-old → optional one-silent-refresh → createTunnel**") contradicts the principal's trace. The blind spot is inverted: the reconnect path *does* DELETE first. But the conclusion (socket-state keying) still stands, for the reasons the principal's crash-loss scenario exposes: a host that loses its `tunnelId` (crash) can never DELETE the old row, so row-existence keying deadlocks permanently; and the host's own guard is already keyed this way (EV-2 design §3.3: "`activeTunnel` … keyed on 'live' not 'ever created' so EV-3 reconnect re-arm is not blocked").
- #3 (create/delete 5xx asymmetry) — **VERIFIED**: the `kind === "delete"` branch precedes the `>=500` check (`src/tunnel.ts:285-286`); DELETE 5xx → `teardown_failed`, create 5xx → `server_error`. The taxonomy must carry the host's coarse grouping and this asymmetry, as we both already specified.
- #4 (400/409/5xx lumped on create) — **VERIFIED** (`src/tunnel.ts:274-293`); this is the check that defeats my round-1 MAY position.
- #5 (§2.6 snake_case) — **VERIFIED** by reading the §2.6 table.
- #6 (clock-skew boundedness) — **ACCEPTED** by inspection: the host consults only its locally computed `expiresAt` (`tokenTtl × 1000` added to host `now()`); `url_expired` is host-computed, never server-reported.

**Convergences, stated plainly** so round 3 can confirm rather than re-argue: `200` (not 201) on POST; `POST /tunnels` and `DELETE /tunnels/{tunnelId}` as fixed paths on the control-plane origin, not discovered; `?token=` query parameter; `wss` MUST (`ws` MUST NOT be issued); camelCase wire bodies for POST request/response and DELETE (the host parses these names); atomic single-use `jti` consumption strictly before `101`; the host acts on status alone and never parses bodies; the 401/403 never-merge rule, mirrored semantically on the data plane; host class grouping of 400/409/5xx-on-create into one generic retryable class and DELETE 5xx into the non-blocking teardown-failure class; tenant-derived-solely-from-token with cross-tenant DELETE → 404; `{"error", "error_description"}` body; unknown request fields and tenant-like body fields ignored; `cwd`/`hostMetadata` informational (INV-1 spirit); consumed-`jti` GC guidance; signing-key guidance as one recommended shape (asymmetric, server-held; algorithm stack-neutral).

**Tradeoffs accepted:** snake_case claims abandon literal agreement with the prose description of the same token in the host-side contract document (bounded cost — token opaque to host, verified); 403-for-replay gives up the REST-conventional 409 reading (compensated by 409 keeping one meaning); MUST-enforced 409 means a conformant host can observe a transient 409 in the half-open window (bounded by the liveness clause); 5 s TTL floor accepts a misconfigured-but-conformant deployment can approach the skew-failure regime (clamp + server-clock authority do the protective work); 404-only DELETE leaves the reference client's 410 leniency permanently unused (leniency, not a requirement — zero cost).

**Testable claims:** (1) rearm deletes first (refutes principal claim #2's premise — DELETE lives in the rearm closure; socket-state keying stands on the crash/lost-tunnelId case instead); (2) 409-on-create needs no new client reason (`src/tunnel.ts:274-293` — no branch between 403 and fall-through); (3) 404-only DELETE contradicts no fixture (client 404/410 tolerance is client-side, §1.7 makes the document govern); (4) §2.6-consistency: any claim name not in §2.6's table style is non-conformant page text, `jti` the one new REQUIRED member; (5) handshake partition `{101, 400, 401, 403, 404, 5xx}` closes every host-observable outcome under the uniform re-dial remedy.

**Residual deltas for round 3 (only these):** (a) my hold on 404-only DELETE vs the principal's 404+410; (b) my single-`invalid_token` code for handshake 401 vs the principal's `invalid_token`/`token_expired` split. Everything else has converged; if the principal accepts both, round 3 can be skipped.

**principal (job-20.4), verbatim:**

**Cross-seam reading (re-verified, with two self-corrections):**

- **Handshake uniformity is real on both sides.** `src/transport.ts:381–383` resolves `opened` from `onopen`/`onerror` alone; `:395` `if (!opened)` → `relay_unreachable`; no HTTP status is ever read. My round-1 testable claim #1 and the owner's identical claim are both confirmed, down to the line. The handshake status set is server-side diagnostic, full stop.
- **The tunnel token is opaque to the host, confirmed.** `src/tunnel.ts` and `src/transport.ts` contain zero `atob`/`decode`/`jwt`/`jti` reads. The only JWT decode in the whole of `src/` is `src/login.ts:287–310`, and it decodes the **enrollment access token** (`tenantIdFromAccessToken` reads `sub` at `:310`) to cache a display tenant id — not the tunnel token. So the tunnel-token claim *spellings* are below the host-visible contract line.
- **My round-1 testable claim #2 was wrong, and the corrected trace makes the point sharper.** `src/index.ts:233` `rearm` is `deleteTunnel(currentTunnelId)` best-effort (`:247–249`, catch-swallowed) → optional silent refresh → `createTunnel` (`:292`). There **is** a DELETE in the rearm path; I claimed there wasn't. What survives: the DELETE is *best-effort*, and on the first `/rc` after a crash there is **no DELETE at all** — `rcCommand` (`:517`) only calls `teardown` when a prior `transportRef.handle` exists, so a crashed-and-restarted host POSTs a fresh tunnel while the server may still hold a zombie. "Live = socket state, never row existence" is therefore still the only non-deadlocking key, but the supporting trace is "DELETE is best-effort and absent on the crash path," not "rearm has no DELETE."
- **My round-1 TTL claim #4's premise was wrong.** The silent refresh at `src/index.ts:497–511` (and `rearm:264–292`) runs **before** `createTunnel`, so it consumes zero tunnel-TTL. The tunnel token is minted at the POST response (`src/tunnel.ts:351`) and dialed immediately; the only pre-dial expiry check, `src/transport.ts:369` `now() > expiresAt`, runs in the rearm branch ~0 ms after the POST returns and is **not run at all on the first dial** (`firstDial` skips the whole block). The floor covers exactly one network RTT, not "refresh + POST + dial."
- **Client error mapping, confirmed asymmetric.** `src/tunnel.ts:279–291`: 401→`enrollment_expired`, 403→`enrollment_rejected`, delete→`teardown_failed`, `>=500`→`server_error`, *else* (400/404/409/429…)→`server_error`. `:427` pins `404 || 410` → idempotent success on DELETE. My claims #3 and #4 stand.

**Blind spots:**

**The owner's §2-author/server-vantage blind spot:** it reads "no-double-create" from the REST surface and cannot see `index.ts`'s wiring — that a conformant host *does* reach the second-create path via crash recovery (no DELETE) and via half-open sockets (best-effort DELETE lost, socket still live server-side). "A conformant host never triggers it" is false for exactly the cases where a server-side backstop matters; the owner's MAY-with-fresh-tunnel answer silently forks the ordered stream in those cases, which is a §1.4 INV-1 violation (two live producers of one session's monotonic `seq`), not merely a cosmetic gap.

**My own round-1 tunnel-vantage blind spot (owned):** I traced `dialLoop` without reading the `rearm` closure in `index.ts` and asserted a DELETE that isn't there ("without any deleteTunnel") — and I reasoned the TTL floor from the enrollment-refresh latency, which precedes mint. Both corrections are on the record above; neither changes the conclusion, and each makes it stronger once corrected.

**Reframe — six disputes, resolved:**

1. **Claim naming: hold snake_case.** Three facts the owner's position cannot see from the §2 seat: (a) the card Intent does **not** name token claims at all — it names only the response body `{tunnelId, url, tokenTtl}` (camelCase, host-parsed, agreed). The camelCase claim spellings (`tenantId`, `tunnelId`, `sessionId`) exist only in PI-SPEC §7.2 prose and §7.5's table, not in the card. (b) R5 bars the page from citing PI-SPEC, so §3 cannot *argue* "camelCase per §7.2" on the page; the only in-document claim convention it can inherit is §2.6's snake_case table (`tenant_id`, `sub`, `scope`, `exp`, `iss`, `iat`), and introducing a parallel camelCase `tenantId` in the same document contradicts §2.6 on the same page. (c) §1.7 settles precedence: "where this document and that repository appear to disagree, **this document governs**." The two documents must agree on the host-observable properties of the token (signed, compact, opaque, single-use, `?token=`, short-TTL) — they do. Claim spellings are mint-and-verify-internal. If the council wants prose parity across docs, a one-line PI-SPEC §7.2/§7.5 correction rides the implementing PR per Spec Correction Governance; it is not a §3 decision. **§3 emits snake_case.**

2. **Replay rejection: concede to `409 token_consumed`.** New reason, independent of taste: §2.7 pins `403` to a specific meaning — "valid but insufficient … `scope` is the only deficiency." A spent token is not a scope deficiency; reusing 403 overloads a meaning §2.7 already fixed. 409 is the semantically exact code (request conflicts with current resource state), and it makes 409 mean "state conflict" uniformly across the POST surface (`tunnel_already_live`) and the handshake (`token_consumed`). My 403 was anchored to a §2.7 pattern that does not actually extend.

3. **DELETE idempotency: concede to `404` only.** The deciding fact is state, not ceremony: emitting `410` requires a deletion tombstone (the server must remember that an id once existed to distinguish "never" from "gone"), *solely* to produce a distinction the client ignores (`src/tunnel.ts:427` maps both to the same idempotent success). `404`-only lets "discards token state" mean **literal discard** — the absence of a live-tunnel record is the 404, no tombstone. The owner is also right that client leniency toward 410 is not a contradiction (a tolerant client + a stricter server is the healthy asymmetry). One consequence worth putting in §3.5 explicitly: **no retained record of a deleted tunnel is required**, which also dissolves the `(d) record the id as deleted` step in *both* of our round-1 drafts as unnecessary.

4. **No-double-create: hold server-side MUST `409`, keyed on server-side socket state.** With the corrected trace: a conformant host reaches the collision on (a) crash recovery (first `/rc`, no DELETE) and (b) half-open with lost DELETE. The owner's remedy cost evaporates on inspection — the client renders 409 as "try again," and for *both* reachable cases "try again" **is** the correct remedy (retry after the zombie is reaped). The alternative the owner leaves open (MAY → "issue a fresh tunnel") is the one option that is *wrong*: it lets a crashed host fork the monotonic `seq` stream across two live sockets, violating INV-1's single-producer ordering. Synthesis to absorb the owner's real worry: keep the MUST 409, and add a **SHOULD reap unresponsive data-plane sockets promptly** (Guidance: ping timeout well under the token TTL) so the collision window — and thus the spurious-error window — is small. The state is already required (live-tunnel existence, the second state piece both of us name), so the MUST costs no additional state; it only fixes observable behavior on existing state.

5. **TTL floor: hold `≥ 5 s`, with a corrected latency model.** The floor covers mint→dial, which is one network RTT (the POST response and the WS upgrade are the two halves of one round-trip) plus TLS — sub-second to low seconds on any plausible relay link. `≥ 5 s` is 5–50× headroom. `≥ 30 s` adds no host-observable safety (the default 60 s already covers pathological links; the floor only binds administrators who configure *down*, and it forbids a safe 10 s high-security choice) while lengthening every leaked URL's window against INV-5's "short-lived." The owner's "slow link cannot reliably reach the upgrade" is a claim about RTT, answered by a floor set to a multiple of worst-case RTT — which 5 s already is for any link this product targets; if multi-second RTT must be supported, the default (not the floor) is the place that absorbs it.

6. **WS-upgrade unknown/deleted tunnel: hold `404 unknown_tunnel`.** After conceding #2, the only remaining delta between our handshake sets is this one slot. The card (via my round-1 reframe, which the owner accepted by naming both state pieces in his §3.3) requires the page to name live-tunnel existence as a *distinct* required state piece alongside the consumed-`jti` set. A distinct required check whose failure is invisible on the wire (folded into 401) erases the very distinction the prose is obligated to draw; 404 keeps "credential bad" (401) and "resource gone" (404) separable, which is exactly what the post-DELETE rejection case *is* — a still-cryptographically-valid token whose tunnel no longer exists. I acknowledge this is the closest call of the six and the client is uniform either way; the closed handshake set I now hold is **`{101, 400, 401, 404, 409, 5xx}`**. If the council's tiebreak is fewest-codes, folding existence into 401 is acceptable **provided** §3.4 still names existence as a distinct check in the ordered list — the substantive requirement (post-DELETE token rejection via the existence check) is what the Skeptic must test, not the code that surfaces it.

**Convergence:** I agree with the owner on: 200 response, endpoint paths on the control-plane origin, `?token=` query param, `wss`-must, camelCase POST/DELETE wire body (host-parsed, not restatable), single-use atomic `jti` consumption strictly before 101, host acting on status alone, the 401/403 never-merge rule, host class-grouping of 400/409/5xx on create, the two required server-state pieces (consumed-`jti`, live-tunnel existence), and live = socket state. Net after round 2: I have moved on #2 and #3 toward the owner; the owner's positions on #1, #4, #5, #6 are each refuted by a specific trace he did not have in view, and my positions on those four hold with new evidence.

**Testable claims (defended, corrected):** (1) opaque token — holds; (2) reconnect key — corrected (DELETE best-effort, absent on crash path), conclusion unchanged (live = socket state); (3) DELETE asymmetry — holds (`test/tunnel.test.ts:322–333` vs `:231–242`); (4) create class-grouping — holds (`src/tunnel.ts:288–291`); (5) §2.6 snake_case — holds (§1.7 makes this document govern over the external prose); (6) clock-skew — holds, refined (first dial consults no local expiry; skew bites only rearm dials). TTL-floor claim #4 retracted and replaced: the floor covers exactly one RTT, so ≥5 s is generous and ≥30 s buys no host-observable safety at the cost of a longer leak window.

**Post-round-2 facilitator delta note:** the two seats have *swapped* on replay-rejection status — owner round 2 argues `403 token_already_used`, principal round 2 argues `409 token_consumed` — and one sub-delta remains on handshake-401 code granularity (single `invalid_token` vs an `invalid_token`/`token_expired` split). All else has converged: snake_case claims, DELETE 404-only with literal discard (no tombstone), MUST-enforced `409 tunnel_already_live` keyed on socket state with a liveness clause and a reap-SHOULD, TTL `[5,300]` clamp default 60, upgrade `404 unknown_tunnel`, both reframes accepted.

### Round 3 — final round (owner job-20.5, principal job-20.6, 2026-09-02)

Both seats closed both deltas; positions stabilized after 3 of 3 rounds.

**owner (job-20.5) — deltas resolved:**
- **Delta (a): CONCEDE to `409 token_consumed`. Closed.** §2.7's table pins 403's "Applies to" as "when scope is the only deficiency," and the conformance rule binds the meaning of the code, not just the surface. The more prominently the page states the handshake mirror — as the owner's own round-2 condition required — the more a reader reads the handshake 403 through §2.7's lens, where it falsely asserts "scope is the only deficiency" of a spent token. The condition and the code choice were mutually corrosive; the principal's are not. 409 ("conflicts with the current state of the target resource") is the exact replay semantics, and makes 409 mean one thing uniformly across the tunnel surface. No client consequence either way; normative hygiene, now settled.
- **Delta (b): single `invalid_token` confirmed; handshake closed set `{101, 400, 401, 404, 409, 5xx}`.** The principal did not defend the `token_expired` split in round 2, so the split has no defender and falls. Codes on this surface are diagnostic-only; §2.7's minimalism uses one code per status. Expired token does not authenticate → correctly 401; spent token authenticates and is state-conflicted → 409; buckets stay clean.

**principal (job-20.6) — deltas resolved:**
- **Delta (a): `409 token_consumed`. The debate is closed for me.** §2.7 gives 403 exactly one meaning — insufficient scope, remedy re-consent + admin grant; a spent token is neither. Reusing 403 forces a disambiguation sentence whose only job is to un-overload a code §3 never had to reuse; 409 already means state conflict in this document (`tunnel_already_live` on POST), and a consumed token is literally that.
- **Delta (b): accept single `invalid_token`** provided §3.4's ordered check list names each failure mode distinctly — signature, expiry, malformed token, binding mismatch — each with its stated failure consequence; that is where the distinction lives, not the code string. URL-level malformation (no extractable token: wrong scheme, missing `?token=`, non-single-segment path) stays `400`. The Skeptic's testable requirement is the ordered list's exhaustiveness, not the number of 401 code strings. Handshake set `{101, 400, 401, 404, 409, 5xx}`.

**Converged terminal design (both seats' confirmations agree):** §3 in six subsections — 3.1 tunnel lifecycle with *live* normatively = host data-plane socket currently connected; 3.2 `POST /tunnels` (fixed path on control-plane origin, Bearer, camelCase body `{sessionId, sessionName, cwd, hostMetadata}`, `200` with `{tunnelId, url, tokenTtl}`, 400 `invalid_request` for malformed body); 3.3 signed one-time URL `wss://<data-plane-host>/<tunnelId>?token=<compact JWS>`, snake_case claims (`tenant_id`, `tunnel_id`, `session_id`, `exp`, `jti` REQUIRED; `iat`/`iss`/`aud` SHOULD), TTL default 60 s bounds `[5, 300]` clamped, server-clock expiry authority; 3.4 single-use consumption (ordered checks: signature, expiry, binding, existence — then atomic `jti` consumption strictly before `101`), handshake closed set `{101, 400, 401, 404, 409, 5xx}` — `400` malformed URL/no extractable token, `401 invalid_token` single code, `404 unknown_tunnel`, `409 token_consumed` replay; 3.5 `DELETE /tunnels/{tunnelId}` Bearer, `204` success, `404`-only idempotent (never-existed / already-deleted / cross-tenant; literal discard, no tombstone, no 410), discards token state + closes live socket + releases relay state; 3.6 error taxonomy: §2.7 body and never-merge 401/403 rule extended (401=`invalid_token` re-enroll, 403=`insufficient_scope` re-consent+admin grant, never merged), server-MUST `409 tunnel_already_live` keyed on socket state with liveness clause (closed/failed-liveness socket = not live) + reap SHOULD, POST partition `{200, 400, 401, 403, 409, 5xx}`, DELETE partition `{204, 401, 403, 404, 5xx}`, host coarse grouping restated (400/409/5xx on create → one generic retryable class; DELETE 5xx → non-blocking teardown-failure class), connection-layer failure no status, malformed 2xx a server conformance violation. Two REQUIRED server-state pieces named: consumed-`jti` set (retained ≥ TTL + leeway, self-GC) and live-tunnel existence. Reframe of card Intent: "no lookup required to interpret the token's claims." PI-SPEC §7.2/§7.5 camelCase-claim prose correction rides the implementing PR as a spec correction (Spec Correction Governance), not a §3 decision.

**Settled by deliberation + traces (for the Skeptic to attack):** claim spelling snake_case (token opaque to host — grep-verified both rounds); rearm = best-effort DELETE-old → optional silent refresh → createTunnel, with the crash path having no DELETE at all (socket-state keying forced); host error mapping asymmetric (create 5xx → generic server-error class; DELETE 5xx → teardown-failed class); first dial consults no local expiry.

### Step 4 — Skeptic attacks and runs tests (skeptic job-20.7 timed out mid-gate, cancelled; re-dispatch job-20.8, 2026-09-02)

**Result: closed-green 9 · closed-red 0 · open-untested 4. No blocking objections.**

Settled closed-green (each with an actually-run test):
- **O1** token opacity / snake_case claims / §2.6 consistency — grep exit 1 (zero claim parsing in src/tunnel.ts, src/transport.ts); §2.6 table confirmed snake_case.
- **O2** client error-mapping asymmetry (create 5xx → server_error; DELETE 5xx → teardown_failed) — `bun test test/tunnel.test.ts` 21 pass / 0 fail.
- **O3** 400/409/429 on create lump into one generic class — route-mapping script: all → `server_error`; all six client-distinguished reasons covered by the taxonomy, zero gaps against the card's acceptance criterion.
- **O4** handshake uniformity (client never reads handshake status; set is server-diagnostic) — src/transport.ts:381–405 read.
- **O5** rearm trace confirmed exactly (best-effort DELETE-old → optional one silent refresh → createTunnel; crash path does no DELETE) — with a record correction: the file is `./index.ts` at repo root, not `src/index.ts` (citations' line numbers match the root file).
- **O6** live = socket state; 409 MUST keyed on socket with liveness clause — consistent; client guard in rcCommand; crash-path zombie handled. Non-blocking residual: a false-negative liveness reap could admit a second live socket (INV-1 fork); host-side guard is the primary defense; reap false-negative window acknowledged but not bounded in prose.
- **O7** first dial consults no local expiry (`url_expired` is rearm-branch-only) — confirmed.
- **O8** TTL [5,300] clamp vs client gate (tokenTtl finite > 0) — always passes.
- **O14** R5/self-containment feasibility — §3 vocabulary introduces no blocklisted term; PI-SPEC correction rides the PR outside §3 prose.

Open-untested (prose-precision gates on the not-yet-written §3 text — none a design contradiction):
- **O10** §3.6 must state the surface disambiguation (control-plane 401 ⇒ re-enroll vs handshake 401 ⇒ re-create), so §2.7's re-enroll remedy is not mis-applied to the data plane.
- **O11** closed POST partition forbids 429 — a rate-limiting server cannot express 429 without being non-conformant, and no alternative is stated. Design choice, needs a page-visible answer.
- **O12** "SHOULD supersede prior unconsumed token" has no mechanism in the two named state pieces — a superseded-but-never-consumed old tunnel stays dialable until exp. Imprecision, not a contradiction.
- **O13** handshake `400` (no extractable token) vs `401` (malformed token) boundary not crisply defined (a `token=` param holding a non-JWS string is both).
