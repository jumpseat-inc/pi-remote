---
id: EV-6
title: "Remote input injection"
state: Done
owner: null
epic: EPIC-1
goal: inject.ts converts inbound AG-UI user-message frames into pi.sendUserMessage calls with the correct deliverAs mode for idle, mid-stream, and queued input, and resolves pending human-input prompts with the sending deviceId recorded.
---

## Intent

Implements §5.4 in `src/inject.ts`. Remote input must be indistinguishable
from typed input in the session log — the extension does not filter or
transform its own injections — which is what makes EV-5's replay correct by
construction. User-visible surface — on the host, an injected remote prompt
appears in the transcript like typed input; on the remote client, an approval
prompt raised by the session (CUSTOM `pi.human_input`) can be resolved from
the device, and that resolution carries the approving `deviceId` for audit.

## Acceptance

- A prompt sent while idle starts a run identical in effect to typing it
  locally; the `input` event shows `source: "extension"` and the JSONL record
  is indistinguishable from a typed message.
- A prompt sent mid-stream arrives as a steer; one sent during an active run
  with queue intent arrives as a followUp (verified against pi's actual
  delivery behavior).
- Resolving a pending approval via `pi.human_input` unblocks the waiting
  session and the resolution is tagged with the sending deviceId; when the
  mode does not support direct resolution, the reply is surfaced as a
  steering message instead of being dropped.

## Deliberation record

### Step 1 — path classification

- Full council (not mechanical): spec-ambiguous (the §5.4 human_input "mode that supports direct resolution" and the steer-vs-followUp intent channel are both under-specified; real design tradeoffs), and cross-seam (consumes transport inbound frames + translate frames, and injects into the real pi SDK).
- Surface-touching: yes — an injected remote prompt appears on the host transcript indistinguishable from typed; a pending approval (CUSTOM pi.human_input) is a user-visible interactable on the remote client. `designer` seated as third generator in steps 2-3.
- Binding context carried in (not open for relitigation): transport's `InboundEnvelope` already carries `deviceId?` end-to-end; translate emits CUSTOM `pi.human_input` with **deterministic** `promptId = fnv1a(promptKind\0prompt)`; envelope stays exactly 4 keys; replay-correct-by-construction demands the injector never transform/filter (never registers an `input` extension handler, never returns `action:"handled"/"transform"`).
- Wiki grounding: `vault/wiki/index.md` has no pages; injection semantics grounded in docs/PI-SPEC.md §5.4/§7.3 and the installed pi SDK, not the wiki.
- pi SDK delivery semantics grounded from installed source (`dist/core/agent-session.js`): `sendUserMessage(content, {deliverAs})` → `prompt(text, {streamingBehavior:deliverAs, source:"extension", expandPromptTemplates:false})`; `prompt` consults `deliverAs` only when `this.isStreaming` (idle → normal turn; streaming+followUp → `_queueFollowUp`; streaming+steer → `_queueSteer`; streaming+no deliverAs → **throws**); `input` event carries `source`+`streamingBehavior`; `sendUserMessage` returns `void` and routes errors to the error bus (fire-and-forget, cannot be caught/retried).

### Step 2 — independent first pass (round 1)

**owner** (job-21.1, verbatim).

## OWNER position — `src/inject.ts` (EV-6)

### Approach

Ground first in what the installed SDK actually does, because the whole module is `[AgUiFrame, run-state] → pi-side call` and every branch maps to one SDK gate. `sendUserMessage` is `sendUserMessage(content, {expandPromptTemplates:false, streamingBehavior: deliverAs, source:"extension"})` → `prompt(text,{…, streamingBehavior:deliverAs, source:"extension"})` (agent-session.js:1161–1188). `prompt` consults `deliverAs` **only if `this.isStreaming`** (line 861): idle → runs a normal turn ignoring `deliverAs`; streaming → `_queueSteer`/`_queueFollowUp` (1046–1070: steer interrupts, followUp waits); streaming **with no `deliverAs` → throws** (861–863). So the run-state source of truth is **`isStreaming`** (getter = `_isAgentRunActive`, 616–618) read **at injection time**, not `isIdle` — it is the literal gate `sendUserMessage` branches on, so mirroring it guarantees we never diverge from the SDK's own decision and never hand it an undefined `streamingBehavior` while streaming (the throw case).

**Module surface** (matches the repo's `create*` DI style like `createTransport`/`createState`):

```ts
type ContentLike = { type: "text"; text: string } | { type: "image"; image: unknown };
export interface InjectDeps {
  sendUserMessage: (c: string | ContentLike[], o?: { deliverAs?: "steer" | "followUp" }) => Promise<void>;
  isStreaming: () => boolean;
  /** Matches a pending prompt pi-remote sponsors; false = mode can't resolve → steer fallback. */
  resolvePendingPrompt: (promptId: string, result: unknown, deviceId?: string) => Promise<boolean>;
}
export type InjectResult =
  | { kind: "injected"; deliverAs?: "steer" | "followUp" }
  | { kind: "resolved"; promptId: string; direct: true; deviceId?: string }
  | { kind: "steered_fallback"; promptId: string; text: string; direct: false; deviceId?: string };
export function createInjector(deps: InjectDeps): {
  handle(env: InboundEnvelope): Promise<InjectResult>;
}
```

`InboundEnvelope` already carries `deviceId?` end-to-end (transport.ts:61–65, handed to `onInbound` at ~line 330), which is what makes the audit tag possible.

**Idle / mid-stream / queue decision** — a pure two-branch fold, because it must be a pure function to unit-test against the SDK's *documented* behavior (the seam): `!isStreaming → {}` (no `deliverAs`); `isStreaming && intent==="followUp" → {deliverAs:"followUp"}`; `isStreaming && otherwise → {deliverAs:"steer"}`. The **never-throw** property (never call without `deliverAs` while streaming) is the load-bearing falsifiable claim. Intent comes from the inbound frame: bare AG-UI user frames (`TEXT_MESSAGE_START` role `user`, or a `CUSTOM` `pi.user_input` wrapper) carry normal input; a queue-intent must be explicitly carried in the frame (a wrapper with `intent`) or it cannot produce `followUp` — I flag this as the one wire-format point that must be pinned with the client before EV-6 is verifiable end-to-end.

**`pi.human_input` resolution end-to-end.** The host emits the prompt as `CUSTOM name:"pi.human_input"` with a **deterministic** `promptId = fnv1a(promptKind \0 prompt)` (translate.ts:474–486) — determinism is what lets a resolution match the same prompt across replay (EV-5). A resolution reaches pi by the inverse path: an inbound `CUSTOM`/user frame carrying `{promptId, result}` is matched against **pi-remote's own pending-prompt registry** (keyed by `promptId`, whose `resolve` it holds — the only prompt an extension can actually "resolve", because the SDK exposes **no** API to answer another component's `ui.confirm`; runner.js tracks `activeUIPrompt` privately and only emits `ui_prompt_start/end` events}. Direct resolution = the promptId matches a sponsor-owned pending prompt **and** the mode can await it → `resolvePendingPrompt` returns true, result delivered, `deviceId` recorded. **"Mode does not support direct resolution"** means one of two concrete states: (a) the prompt was raised by pi/another extension (pi-remote observed it via `ui_prompt_start` and broadcast it, but doesn't hold the resolver), or (b) headless mode where there's no local confirm to await. In both, `resolvePendingPrompt` returns false and the reply becomes a steering message — `sendUserMessage(replyText, {deliverAs: isStreaming ? "steer" : undefined})` — never dropped. This satisfies acceptance verbatim.

**Replay-correct-by-construction constraint**: inject.ts must **not** register an `input` handler and **not** return `{action:"handled"/"transform"}` — pi-remote passes every injection through untouched, so the JSONL record is byte-indistinguishable from a typed message (EV-5 replay is trivially correct). Design asserts this as a negative invariant.

### Tradeoffs accepted

- **`isStreaming` (not `isIdle`) as source of truth**: identical today (`isIdle = !_isAgentRunActive`), but it couples our decision exactly to the SDK's own branch, so a future SDK redefinition of "idle" (compaction, queued continuation) can't silently split our routing from the SDK's. Cost: reads a slightly less "user-meaningful" flag.
- **Pure decision + injected SDK seams** rather than wiring real `pi.sendUserMessage`/`ctx.ui.confirm` into inject.ts: we can't run real pi in `bun test`, so correctness is pinned at the boundary by unit tests + documented fixtures. That's the honest seam — real steer/followUp behavior is trusted to the SDK and grounded by source here, not re-verified in tests.
- **Only sponsor-owned prompts resolve directly**: we give up trying to answer arbitrary session prompts remotely (SDK physically can't), routing them to steer instead. Matches acceptance's "surfaced as steering message instead of dropped." The cost is a real product limitation that should be surfaced to the owner: remote-only approval of *other* extensions' confirms is not achievable without an upstream pi feature.

### Testable claims

(T1) `pickDeliverAs(!streaming, "steer")` → `{}`; `pickDeliverAs(false, "followUp")` → `{}` — idle: `sendUserMessage(text)` with `deliverAs === undefined`. (T2) `pickDeliverAs(true,"followUp")` → `{deliverAs:"followUp"}`; `pickDeliverAs(true,"steer")` and `pickDeliverAs(true,"none")` → `{deliverAs:"steer"}` — **never-throw**: a test asserting every streaming call carries a `deliverAs`. (T3) fixture asserts exact call shape `{deliverAs, expandPromptTemplates:false, source:"extension"}`. (T4) resolution with registered promptId → `resolvePendingPrompt(promptId,result,deviceId)` with envelope `deviceId`, returns `{kind:"resolved",direct:true}`; second resolution of same promptId matches no longer (one-shot registry) → `steered_fallback`. (T5) resolution with unknown promptId → `direct:false`, `sendUserMessage(replyText,{deliverAs:"steer"})` when streaming, no deliverAs when idle — **never dropped**. (T6) deviceId passthrough through `resolvePendingPrompt` and into `InjectResult`; frame with no deviceId still resolves (deviceId undefined). (T7 seam, untestable against real pi) steer-vs-followUp *queueing* is SDK-owned; asserted only via fixtures mirroring `_queueSteer`/`_queueFollowUp`; discriminator test is T2.

**principal** (job-21.2, verbatim).

**Cross-seam reading**

1. **Transport hands inject a frame typed as the *outbound* union, not a validated inbound contract.** `transport.ts`'s `parseInbound` accepts any object whose `frame.type` is a string in `AG_UI_TYPES`, then casts it to `AgUiFrame` and passes it to `onInbound` with `deviceId` on the envelope. It validates nothing about `TEXT_MESSAGE_START.messageId/role` or `CUSTOM.name/value`. So `inject.ts` inherits a trust boundary: it must runtime-validate every inbound frame itself, and it may *not* assume the `AgUiFrame` type describes what actually arrived.
2. **The inbound user-message shape is AG-UI `TEXT_MESSAGE_START`(role "user") → `TEXT_MESSAGE_CONTENT` → `TEXT_MESSAGE_END`. There is no wire field for steer vs followUp.** AG-UI's `TextMessageStartEventSchema` is `{type, messageId, role, name?}`; no event type carries a delivery-mode field. `pi`'s `deliverAs` concept exists only on the SDK side. PI-SPEC §5.4 has a "Client intent" column, but neither `InboundEnvelope` (settled 4-key `{v,seq,ack,frame}`) nor any frame in `AgUiFrame` carries that intent. **The queue-intent case has no wire representation anywhere in the repo today.** Each side of this seam assumes the other side owns that field; neither does.
3. **`pi.sendUserMessage` is the correct and sufficient injection path, but it is fire-and-forget and swallows its own failures.** Grounded: `agent-session.js:1161-1196` → `prompt(text, { expandPromptTemplates: false, streamingBehavior: options?.deliverAs, images, source: "extension" })`; `prompt` emits the `input` event with `source` and `streamingBehavior` (the latter only when `this.isStreaming`), then throws if streaming with no `streamingBehavior`, else `_queueSteer`/`_queueFollowUp`. `types.d.ts` declares `sendUserMessage(...): void`, and the runtime binding catches and routes delivery errors to the extension error bus. **Consequence: `inject.ts` cannot "catch the throw"** — it can only avoid triggering it by always passing a valid `deliverAs` when the run is not idle.
4. **The approval half of the seam is keyed to events the SDK does not emit.** `translate.ts`'s `PiEvent` union contains `{event:"ui.confirm"; promptKind; prompt}` and `{event:"user_input"; ...}`. The real `ExtensionEvent` union has neither; it has `ui_prompt_start`/`ui_prompt_end` with `{kind: UIPromptKind; title?}` and `message_start`(role user). So the emitted `pi.human_input` frame (with `promptId = fnv1a(promptKind + "\0" + prompt)`) is never produced by the live path as written, and the `user_input` echo mapping is dead code. There is also **no public API to programmatically resolve a pending prompt**: `grep` across `dist` finds no `resolvePrompt`/`resolveUIPrompt`/`approvePrompt`; `ExtensionRunner.activeUIPrompt` is private and `withUIPrompt` only *wraps* `ui.*` creators. The only public way to get text into the session is `sendUserMessage`/`sendMessage`. "Resolved via the matching pending UI prompt when the mode supports it" is therefore **not implementable against the installed SDK surface**; only the steering-message fallback is real.

**Reframe**

EV-6 is not "call `sendUserMessage` with the right option" — that part is a thin adapter. The seam itself is the risk, and it is currently unframed in two places. Reframe the card around two explicit inbound wire contracts that must be pinned before the acceptance is testable:

1. **Intent rides the user-message frame, namespaced.** Use the already-valid `name` field on inbound `TEXT_MESSAGE_START` as the sole delivery-intent channel: `name: "steer" | "followUp"` (absent = idle-decided). No 5th envelope key, no new frame family, no CUSTOM-correlation for the common path. The extension does not add intent; the client does, and the client can express it because `name` is part of the AG-UI schema.
2. **Approval responses are a dedicated CUSTOM frame keyed by the emitted `promptId`**, e.g. `{type:"CUSTOM", name:"pi.human_input.response", value:{pi:"ui.confirm.response", data:{promptId, response}}}`. Matching is a map lookup on `promptId`, not "the only pending prompt."

And reframe the approval path honestly: **make the steering fallback the only live code path, and model direct resolution as an injected `resolvePrompt` seam that the current SDK cannot wire.** `inject.ts` should be built around `sendUserMessage(text, {deliverAs:"steer"})` when a resolution can't be applied directly; `resolvePrompt?` exists so the acceptance's "unblocks the waiting session" clause has a home, but we flag it as a fixture seam, not a real pi surface. The mode signal is the *presence of the seam* (`resolvePrompt` absent/throws → fallback), not a read of `ctx.mode`.

Run-state source: `inject.ts` takes `isIdle: () => boolean` injected by EV-8. EV-8 should capture the SDK's own `ctx.isIdle()` from a `session_start` handler. EV-8's `sendUserMessage` wiring must call `pi.sendUserMessage` synchronously (no intervening `await`) so the `isIdle` read and `prompt()`'s internal `isStreaming` check happen in the same microtask.

Testable claims: (P1) inject START(role user,"m1")/CONTENT/CONTENT/END with isIdle true → exactly one `sendUserMessage("Hello")`, no options, no call before END. (P2) same frames isIdle false → `sendUserMessage("Hello",{deliverAs:"steer"})`. (P3) START(role user, "m2", name:"followUp")/CONTENT/END isIdle false → `sendUserMessage(text,{deliverAs:"followUp"})`; with isIdle true the name is ignored → `sendUserMessage(text)`. (P4) decision timing: fake isIdle false-first-then-true must produce `deliverAs:"steer"` for a message finalized on first call — mode read at END/injection time, not cached at START. (P5) no transform/filter: send assembled string byte-for-byte; static guard: inject.ts registers no `pi.on("input")` handler and no `action:"transform"/"handled"`. (P6) approval fallback (live path): register pending {promptId:"p1"}; inbound CUSTOM pi.human_input.response {promptId:"p1",response:"yes"} isIdle false, **no resolvePrompt** → exactly one `sendUserMessage("yes",{deliverAs:"steer"})`, exactly one `recordApproval({promptId:"p1",deviceId:"dev1",response:"yes",at})`, p1 removed. (P7) direct-resolution seam (fixture only — no real pi wiring can satisfy it today): with resolvePrompt present → `resolvePrompt("p1","yes","dev1")`, zero sendUserMessage, recordApproval called. (P8) unmatched promptId not dropped → falls through to `sendUserMessage(response,{deliverAs:"steer"})` (or no options when idle). (P9) inbound validation: TEXT_MESSAGE_CONTENT/END for unopened messageId, or CUSTOM with non-string value.promptId, ignored without calling sendUserMessage and without throwing. (P10) SDK-semantics fixture (flag the seam): fake sendUserMessage mirroring the SDK contract (throws when streaming and no deliverAs; ignores deliverAs when idle) run against P1–P4 must never throw.

**designer** (job-21.3, verbatim — position file `vault/raw/2026-08-31-design-ev6-remote-input-injection.md`, read in full).

The user-visible work is two experiences: on the **host** an injected remote prompt lands in the JSONL exactly like a typed one (replay-correctness invariant); on the **remote client** a `pi.human_input` frame received while the session waits on a host UI prompt can be answered, carrying the `deviceId` of the answering device. Smallest viable shape: a thin I/O-free `inject.ts` exposing one seam per responsibility — `injectUserMessage({content, deliverAs})` → `pi.sendUserMessage(content,{deliverAs})`; `resolveHumanInput({promptId, text, deviceId})` → resolves the pending host prompt directly when the host UI is wrapped and lets us, else falls back to `sendUserMessage(text,{deliverAs:"steer"})` so it is **never dropped**. The fallback is the product choice; dropping is the design failure. Replay-correctness = no transformation: EV-6 must **not** register an `input` extension handler (static-grep gate), and must not return `action:"handled"/"transform"`. `deliverAs` is what pi actually does (verified agent-session.js). `promptId` is the only durable correlation (deterministic in translate.ts) — key on `promptId`, never prompt text. deviceId is opaque and already on `InboundEnvelope.deviceId` — read it from the envelope, propagate as a structured field, never smuggled into `text`. Resolving the host UI prompt is conditional: `runner.js:88 noOpUIContext` is the default; `:269 setUIContext` wraps host UI confirms; the host mode (interactive vs print vs RPC) determines whether a pending confirm promise can be reached. The resolver must detect "no pending prompt I can resolve" and fall back to steer — the fallback is the spec-mandated answer (§5.4 row 4); dropping is a regression. Interaction questions raised (not ruled): (i) how the remote knows a prompt is pending and when the host finished waiting — proposes host emits CUSTOM `pi.human_input.resolved {promptId,deviceId,ts}` from a new `ui_prompt_end` PiEvent (forward dependency on EV-4/EV-8; must not block EV-6); (ii) multiple devices racing to answer — first deviceId wins, others see `pi.human_input.resolved` with `winnerDeviceId`, stale answers surfaced as notice not delivered (a slip); (iii) steer landing while already steering → queued, remote renders `queue_update` feedback (SDK-owned); (iv) reply when direct resolution unsupported → router to `{deliverAs:"steer"}` and remote sees `CUSTOM pi.human_input.fallback_to_steer`. Falsifiable predictions H1–H7: H1 transcript indistinguishability (call args `{content:"x",options:{source:"extension"}}`, no input handler registered); H2 mode derivation (steer vs followUp routed to right SDK queue); H3 human-input resolution carries deviceId structurally not in text; H4 fallback is not a drop (unknown promptId → exactly one steer); H5 remote sees resolution feedback via CUSTOM pi.human_input.resolved (depends on new ui_prompt_end PiEvent — forward dep); H6 stale answers surfaced not delivered; H7 purity (no WebSocket/setTimeout/randomUUID). Preferences: deviceId as structured field not smuggled into text; one function per inbound responsibility; remote renders approval as a modal (one decision at a time); host-side "who answered?" surfaces in JSONL as a `custom_message` entry of kind `human_input_resolution` carrying {promptId,deviceId,ts}. Notes for implementing seat: no input handler (H1 grep); promptId is the only durable correlation; resolution only possible when host UI registered with a context exposing the resolution promise — RPC/print mode → informational frame + steer fallback; forward dependency H5 must not block EV-6 (fallback is the floor, H5 the ideal).

### Step 3 — bounded exchange (round 2, final — positions stabilized, stopped by design ≤3 cap)

**owner** (job-21.4, verbatim highlights). (a) ACCEPTS principal's `name`-field intent channel on inbound TEXT_MESSAGE_START, with two conditions: (1) the extension validates `name` against {steer, followUp} and never passes a junk string to deliverAs; (2) absent must be defined — absent+idle → new turn, **absent+streaming → steer** (the §5.4 mid-stream default), so absent is never a legal way to reach pi undeliverAs'd. AG-UI `TextMessageStartEventSchema` has `name: z.string().optional()` so it is schema-valid; repurposing a field conventionally meaning sender name is acceptable because the relay treats frames as opaque and the client is ours — owned as a pi-remote extension convention. (b) ACCEPTS designer's `pi.human_input.resolved` as forward-dep non-blocking; ships with the steering-fallback floor (§5.4 row 4 mandates it; noOpUIContext is default per runner.js). (c) CONVERGED — the resolvePendingPrompt "sponsor-owned resolves directly" vs principal's "fixture seam, steer only live path" is NOT a real divergence: the runner exposes no public API to settle a wrapped pending prompt today, so no live resolve path exists. "Sponsor-owned" survives as the guard on the FUTURE seam: resolve only promptIds in our emitted registry. (d) Run-state: `isStreaming` and `isIdle` are the SAME boolean inverted (`_isAgentRunActive`); naming choice not a fork. Load-bearing discipline: capture boolean and call sendUserMessage in the same synchronous tick; never let deliverAs be undefined while streaming (else throw). **Converged surface**: `inject.ts` pure; createInjector({sendUserMessage, runState:()=>"streaming"|"idle", resolvePendingPrompt?, emitCustom, hasPrompt}) → {injectUserMessage({content,deliverAs?}):InjectResult, resolveHumanInput({promptId,occurrence,text,deviceId}):InjectResult}; InjectResult = {delivered:true}|{delivered:false;reason:"stale"|"no_pending"|"invalid"} — never throws. **RESIDUAL (owner, wants an open ruling / fixes it as a design point none addressed):** `promptId = fnv1a(kind\0prompt)` is deterministic but not unique per instance — two identical approvals in one session (or a retry/compaction re-raising the same ui.confirm) produce the same promptId; a map keyed SOLELY on promptId cannot distinguish them live, so a late answer can resolve the wrong older instance. Fix: scope live matching by **(promptId, occurrence)**, reserving promptId-only for replay re-apply. Settling test: feed two identical ui.confirm entries → registry has 2 distinct (promptId,occurrence) entries; resolveHumanInput({promptId,occurrence:2}) → occurrence:1 not resolved. Minor: pin name overload as a documented pi-remote extension field with union validation.

**principal** (job-21.5, verbatim highlights). The `name` field survives into `onInbound` at runtime (parseInbound returns the original parsed object cast to AgUiFrame and validates nothing about nested fields), but is invisible to the `AgUiFrame` type — inject.ts must widen its inbound type to `TEXT_MESSAGE_START & { name?: string }` and runtime-validate. AG-UI name is a free string not an enum; `{steer, followUp}` is our documented convention, unknown values treated as absent (collision-safe). (a) Owner's "wrapper with intent" and principal's `name` field are not different contracts — owner left the carrier unnamed; `name` is the concrete encoding. **Settled wire contract:** `TEXT_MESSAGE_START {type, messageId, role:"user", name?: "steer"|"followUp"}`, absent/unknown = idle-decided (or steer when streaming). No 5th envelope key, no CUSTOM wrapper for the common path. DROP the CUSTOM pi.user_input wrapper alternative. (b) Same seam, different confidence — owner's resolvePendingPrompt→true is conditionally live only if pi-remote wraps the host UI and thereby owns the pending prompt's resolver; principal's "fixture seam, steer only live path" is the default reality (no public API to resolve another component's ui.confirm; sendUserMessage returns void, fire-and-forget). Converge: keep resolvePendingPrompt as injected seam, label true-branch "live only when pi-remote is sponsor; fixture otherwise"; false/absent branch is the always-live steer fallback. (c) Settle on **isStreaming** read at injection time in the same microtask as sendUserMessage (no intervening await); retracts the isIdle+synchronous-capture position (solved the same race with two extra assumptions). Never-throw property is the invariant. (d) Accepts pi.human_input.resolved as ideal-but-forward-dep, not blocking; milestones TWO forward deps not one: EV-4 must add a ui_prompt_end PiEvent + CUSTOM mapping (current PiEvent union has ui.confirm and no ui_prompt_end), and EV-8 must wire the event. EV-6's floor is steer fallback; must not import/assume pi.human_input.resolved. First-wins/stale-not-delivered is client-side rendering. **Converged module surface**: InjectDeps {sendUserMessage, isStreaming:()=>boolean, resolvePendingPrompt:(promptId,result,deviceId?)=>boolean|Promise<boolean>}; InjectResult = injected{deliverAs?}|resolved{direct:true,deviceId?}|steered_fallback{direct:false,text,deviceId?}; pickDeliverAs(isStreaming,intent?):{deliverAs?}; createInjector(deps):{handle(env):Promise<InjectResult>}. Wire conventions to sync into PI-SPEC §5.4 and pin with client: user input = TEXT_MESSAGE_START(role user, optional name)→CONTENT→END; approval response = CUSTOM pi.human_input.response, value:{pi:"ui.confirm.response",data:{promptId,response}}, deviceId from InboundEnvelope.deviceId passed structurally never into text. **RESIDUAL (principal, not a test — an open ruling):** whether pi-remote wraps host UI (setUIContext) so resolvePendingPrompt ever gets a live implementation; if no, the resolved branch stays fixture-only and steer fallback is the permanent product behavior — a product-owner judgment for EV-7/EV-8, not a bun test and not blocking EV-6.

**designer** (job-21.6, verbatim highlights). Load-bearing fact: repo's `AgUiFrame.TextMessageStartFrame` has exactly `{type,messageId,role}` — no `name`; AG-UI schema allows `name?` but parseInbound casts to AgUiFrame without validating optional fields. So principal's name-field is a **type widening + frame reader in this repo**, not a drop-in. CONVERGES to principal's `name` channel on the condition that `TextMessageStartFrame` is widened by exactly one optional field (`name?: string`) — one line in src/translate.ts + field-read in inject.ts. Converges to CUSTOM pi.human_input.response keyed by promptId; inject-time sendUserMessage synchronous decision (steering fallback the only LIVE path, direct resolution an injected resolvePrompt fixture seam); first-wins/stale-not-delivered. Does NOT block EV-6 on pi.human_input.resolved — steering fallback is the user-visible floor; resolved event belongs to EV-4's PiEvent surface as a follow-up (FLLWUP-candidate F-pending-1: emit pi.human_input.resolved {promptId,deviceId,ts} from a new ui_prompt_end PiEvent in EV-4/EV-8). <<<<**Gulf closed:** remote user's "intent=follow-up" is a single AG-UI-valid field on a frame they already send (no wrapper/second affordance); reply path "modal shows pending → host UI wrapped → resolves direct; not wrapped → reply lands as steer + remote sees pi.human_input.fallback_to_steer notice". **Converged interaction surface:** inject.ts exposes injectUserMessage({content,deliverAs}) and resolveHumanInput({promptId,text,deviceId}); deliverAs derived pure fold (!isStreaming→no deliverAs; isStreaming&&name:"followUp"→"followUp"; isStreaming&&(name:"steer"|absent)→"steer"); queue-intent rides optional `name` on TEXT_MESSAGE_START (one-line widening); prompt resolutions arrive as CUSTOM pi.human_input.response {value.data:{promptId,response}} matched by promptId; direct resolution = only via injected fixture resolvePrompt(promptId,result,deviceId) (real wiring blocked by SDK — all agree); race = first-wins via one-shot promptId→resolver registry, stale surfaced as pi.human_input.stale {winnerDeviceId}, never delivered; remote renders pending approvals as a MODAL. **RESIDUAL (designer — product-owner question, not design disagreement):** how loudly should the "I see a pending approval but the host is RPC/print-mode so my reply arrives as a steer" notice surface? Silent = confused user; loud = notice asks to confirm in a context where they weren't asked. Designer leans loud-on-first-failure-of-session-then-silent — but this is a taste/product judgment for product-owner. Also reject-wrapper: F4 wrapper-intent CUSTOM frame is IGNORED (validates no second channel); F5 TextMessageStartFrame widening preserves translate emission byte-for-byte.

### Round outcome (facilitator record)

Exchange stopped after 2 rounds; positions stabilized (≤3 cap not reached by design). **CONVERGED across all three:** (1) queue-intent wire contract = optional `name` field ("steer"|"followUp", absent/unknown treated as absent) on inbound `TEXT_MESSAGE_START`, requiring a one-line widening of `TextMessageStartFrame { name?: string }`; absent+idle→new turn, absent+streaming→steer (never reach pi undeliverAs'd — never-throw property). (2) run-state source = SDK `isStreaming` (the `_isAgentRunActive` boolean), read at injection time in the same synchronous tick as `sendUserMessage`; isStreaming/isIdle are the same boolean inverted (naming only). (3) approval responses = CUSTOM `pi.human_input.response` {value:{pi:"ui.confirm.response",data:{promptId,response}}}, matched by promptId; deviceId from InboundEnvelope.deviceId, structured, never into text; registry is one-shot. (4) steering fallback is the ONLY live code path; direct resolution is an injected `resolvePendingPrompt`/`resolvePrompt` fixture seam — conditionally live only when pi-remote is the host-UI sponsor (future; EV-7/EV-8), and "sponsor-owned" = present in our emitted registry keyed by promptId. (5) inject.ts is pure — no socket/timer/random; MUST NOT register an `input` extension handler (replay-correct invitation, never transform/handled). (6) first-wins / stale-not-delivered; remote renders pending approvals as a modal; fallback reply surfaced as `pi.human_input.fallback_to_steer`. (7) `pi.human_input.resolved` (host-side completion event) is ideal-but-forward-dep (EV-4 ui_prompt_end PiEvent + EV-8 wiring) — NON-BLOCKING for EV-6, filed as follow-up candidate. THREE residuals routed forward: **(R1, design, testable/preventive):** live matching must scope by (promptId, occurrence) because deterministic promptId is not unique per instance — owner's fixing test (2 identical ui.confirm → 2 distinct (promptId,occurrence); resolving occurrence 2 doesn't resolve occurrence 1). **(R2, open judgment → product-owner):** how loudly the fallback-to-steer notice surfaces when the host mode cannot resolve directly (designer's lean: loud-on-first-failure-then-quiet per session; product copy/taste). **(R3, open judgment → product-owner / forward to EV-7·8):** whether pi-remote ever wires a live resolvePrompt via host-UI wrapping (setUIContext) — if not, steer fallback is the permanent product behavior; not blocking EV-6.

### Step 4 — Skeptic attacks and runs tests (job-21.7)

Grounded PI-SPEC §5.4/§7.3, transport.ts parseInbound/InboundEnvelope, translate.ts TextMessageStartFrame/CustomFrame, installed pi SDK agent-session.js/runner.js, example send-user-message.ts. Baseline: tsc clean, 62 tests pass.

All seven falsifiable claims **closed-green, no open objections**:
- (a) never-throw property: `pickDeliverAs(isStreaming, intent)` truth table 8/8 — streaming+undefined→steer, streaming+followUp→followUp; never undefined deliverAs while streaming.
- (b) (promptId, occurrence) scoping necessary+sufficient: fnv1a("confirm\0Delete file?")="1b6309ec"; two identical prompts → occurrence1=1, occurrence2=2; resolve(pid,2) fires only o2, resolve(pid,1) only o1, re-resolve fails; promptId-only resolves latest — distinct entries, correct isolation.
- (c) `name` widening backward-compatible: existing TEXT_MESSAGE_START without name assigns cleanly (name=undefined); client frame with name survives parseInbound cast.
- (d) NO public resolvePrompt/resolveUIPrompt/resolvePendingPrompt API in SDK — zero matches across dist; activeUIPrompt private (runner.js:151); withUIPrompt wraps ui.* calls with no resolve handle; only public injection path is sendUserMessage (void, fire-and-forget). Confirms steering fallback is the only live path.
- (e) pi.human_input.response / fallback_to_steer / stale frames consistent with CustomFrame {type,name,value:{pi,data}}.
- (f) deviceId from envelope (o.deviceId, transport.ts:254), never from frame; resolution record carries deviceId as field not in text. (resume uses f.deviceId but never reaches onInbound.)
- (g) replay-correctness: zero `on("input")`/emitInput/action:"handled"/"transform" in src/ today; negative invariant enforceable by static grep post-inject.ts.

Additional non-blocking findings: (1) `user_input` PiEvent in translate.ts is **dead code** — real SDK emits message_start/update/end, zero occurrences of "user_input" in agent-session.js; live path never triggers the translate mapping (design correctly bypasses it; document or remove — FLLWUP-candidate). (2) `name` invisible to TS (parseInbound casts to AgUiFrame without name); inject.ts must type-assert + runtime-validate against {steer, followUp} — already the acknowledged widening commitment.

**Verdict: no open objections.** Design sound against the installed SDK, transport.ts, translate.ts. Confirmations: no resolvePrompt API → steer fallback the only live path; user_input PiEvent dead code. Gate integrity: tsc clean, bun test 62/62; the never-throw defect mode (streaming+undefined deliverAs → SDK throw at prompt 861-863) is exercised by the `/tmp/ev6-probes.ts` truth table and confirmed green.

### Step 5 — Consolidator synthesis

**Consolidator synthesis** (job-21.8, verbatim buckets). *Note: the consolidator flagged that the `council/cards/EV-6.md` it read was a stale/different checkout ("Add the gates GitHub Actions workflow"). The on-disk file is verified correct under the single-writer discipline — this EV-6 record (145 lines, steps 1-4 above) is intact and matches the synthesis input. The synthesis itself is authoritative.* 
SETTLED (all seats + Skeptic converged, no dispute): queue-intent `name` field on inbound TEXT_MESSAGE_START (accepted/absent→idle-decided, missing+streaming→steer, followUp+streaming→followUp, never-throw); run-state = SDK `isStreaming` same synchronous tick as sendUserMessage; approval responses = CUSTOM pi.human_input.response {value:{pi:"ui.confirm.response",data:{promptId,response}}} matched by (promptId, occurrence); deviceId from InboundEnvelope structured never into text; registry one-shot; steering fallback the ONLY live resolution path, direct resolution an injected fixture seam conditionally live only when pi-remote sponsors host UI (EV-7/EV-8); inject.ts pure no `input` handler never transform/handled; first-wins/stale-not-delivered; fallback surfaced as pi.human_input.fallback_to_steer; pi.human_input.resolved forward-dep NON-BLOCKING. All 7 Skeptic claims closed-green, no open objections. Non-blocking step-4 findings (follow-ups): user_input PiEvent dead code (document or remove); name runtime-validation in inject.ts.
OPEN JUDGMENT (→ product-owner, escalating to steward): **R2** fallback-to-steer notice loudness when host mode can't resolve directly (designer lean: loud-on-first-failure-then-quiet per session; counter: consistent low-noise) — product copy/surface taste, test-unsat, non-blocking for acceptance but is EV-6's user-visible fallback surface. **R3** whether pi-remote ever wires a live resolvePrompt via host-UI wrapping (setUIContext) — if yes the seam goes live; if no steer fallback is the PERMANENT product behavior and R2's loudness is the durable contract; forward dep with EV-7/EV-8, non-blocking.
OPEN OBJECTIONS: none.
Handoff: yes to product-owner for R2/R3; no open objections block EV-6.

### Step 6 — routing

Open-judgment items R2 and R3 both classified by the consolidator as routing to product-owner (escalating to steward). No Phase 1 ruling on this epic covers either (orchestrator: all judgment delegated to council, none on record). Neither is a settled test result, and I do not decide them. Per the escalation contract the facilitator does not dispatch ruling seats; returning ESCALATION to the orchestrator for the rulings, then resuming this card on them. R3 is explicitly forward-linked to EV-7/EV-8 (host-UI wrapping lives in EV-7/EV-8 surface) but its answer determines how EV-6 writes the design (permanent-fallback vs interim-fallback language), so it is ruled jointly with R2 here. R1 (the (promptId, occurrence) preventive, closed-green by Skeptic) is settled design, not a ruling.

### Step 6 — rulings applied (binding, verbatim from orchestrator)

**R2 — Fallback-to-steer notice loudness (product-owner): Position A.** A single, stated-sentence notice on the first fallback-to-steer occurrence in a session; silent thereafter. "Loud" is defined by EV-2 Item 3's discipline: a sentence, not a glyph, that names the cause class (host mode cannot resolve directly — RPC/print/headless, or the prompt belongs to another extension) and states honestly that the reply was surfaced as a steering message instead of being delivered directly. Subsequent occurrences emit no notice. Grounds: EV-2 Item 3 (stated-refusal sentence over glyph acks); EV-3 (honest metadata, never terminal); EV-5 A (no fabrication / no re-stating what the system already knows). Reversibility: one boolean ("have I announced yet this session") plus a copy string.

**R3 — Host-UI sponsorship (steward): Side B.** The steering fallback is the permanent product behavior. pi-remote does not sponsor or wrap the host UI. The fixture seam (resolvePendingPrompt) stays a test-only seam; EV-6 ships with the fallback as documented in PI-SPEC §5.4 row 4. Grounds: (1) §7.4 security model — sponsorship would add a fourth trust-summary row ("can resolve any pending UI prompt in the session"), expanding blast radius beyond "create tunnels for sessions on this host"; (2) architecture boundary — pi-remote's portfolio identity is "extension that translates and tunnels," it observes, it does not mediate; (3) spec integrity — §4's ui.confirm row defers native interrupt framing "if/when the client implements it" and §5.4 row 4 documents the steering fallback as the spec-mandated path, not a stopgap; plus the Skeptic's finding that a live path would depend on SDK internals (zero public resolve API; activeUIContext private) — fragility no portfolio commitment accepts. Cheapest-to-reverse: Side B; if pi exposes a public cross-component prompt-resolution API later, a dedicated post-epic card can add wrapping — the fixture seam already exists and §4's "if/when" language documents the deferred path. What changes: EV-6's steering fallback confirmed permanent; R2's loud-once notice is the durable user-facing contract; EV-7 scope unchanged (OAuth2 enrollment only); EV-8 scope unchanged (command surface and lifecycle wiring only); no new card.

**General rule for EV-7 / EV-8 (product-owner, binding until/unless steward reopens R3):** any EV-7/EV-8 design contemplating host-UI wrapping must name in its goal or risk section: (1) the §7.4 blast-radius expansion; (2) the disconnect-mid-resolve contract; (3) the resolver-ownership handoff; (4) the impact on EV-6's notice. Under steward's Side B, none of EV-7/EV-8 contemplate it.

**Effect on EV-6 (applied):** the steering fallback (direct resolution via injectable `resolvePendingPrompt` fixture seam) is the PERMANENT product behavior — the design spec writes it as the spec-mandated path per PI-SPEC §5.4 row 4, not an interim stopgap. R2's loud-once notice is EV-6's durable user-facing contract and is implemented in the fallback path. R1 ((promptId, occurrence) scoping) remains settled design from step 4. Step 6 is CLOSED; proceeding to step 7.

### Step 7–9 — design spec, owner implementation, Skeptic verification

- **Step 7 (full-council path):** written up the settled design (not derived) as
  `docs/superpowers/specs/2026-08-31-EV-6-design.md` — refused to reopen anything
  steps 2–6 closed, including R1/R2/R3. Self-reviewed (no placeholders/TODOs, scope
  confined to inject.ts + one-line translate widening + §5.4 sync + plan; no index.ts).
  Committed; card → `In Progress` (validate.py clean).
- **Step 8 (owner, job-25.1):** planned at
  `docs/superpowers/plans/2026-08-31-EV-6-implementation.md`, implemented TDD in
  worktree `.worktrees/ev-6-inject` on branch `ev-6-inject`, cleared gates, pushed,
  opened **PR #6** (head SHA `b77f5763b3d54fada5983538eb6c5ff82a89bb10`, state OPEN —
  observed via `gh pr view 6`). Reported: tsc exit 0; `bun test` 85/85 pass (62 existing
  + 23 new); negative invariant clean; §5.4-only sync; `src/index.ts` untouched;
  `user_input` dead code NOT removed (FLLWUP-candidate). Card → `In Review` (validate clean).
- **Step 9 (Skeptic, job-25.2), at head SHA `b77f576`:** re-ran all 4 gates with gate
  integrity confirmed (mutation checks threw/red then restored). tsc exit 0; `bun test`
  85/85, 0 fail, 500 expect() calls; negative invariant zero matches; scope confined (only
  inject.ts new, translate.ts additive, inject.test.ts, §5.4 sync, plan; index.ts untouched;
  user_input NOT removed). All 10 load-bearing property probes GREEN: never-throw truth table
  + end-to-end SDK-mirroring fixture; R1 (promptId,occurrence) registry isolation (resolving
  occurrence 2 does not resolve occurrence 1); R2 loud-once sentence notice once per session,
  stale never triggers it; R3 permanent fallback with resolvePendingPrompt test-only seam
  (resolved branch reachable only via fixture); stale-not-delivered (`pi.human_input.stale`
  surfaced, no sendUserMessage); unknown promptId → fallback, never dropped; deviceId from
  envelope structural never in text; malformed/non-user ignored without throw. **Result: no
  open objections.** Proceeding to step 10.

### Step 10–12 — judge, deterministic merge, reconciled, Done

- **Step 10 (judge, job-25.3), PR #6 @ head SHA `b77f5763b3d54fada5983538eb6c5ff82a89bb10` (branch, not main):** verdict **PASS**. Read the branch implementation and ran the suite in the worktree; confirmed the goal is met: AG-UI user-message → sendUserMessage assembly at END with correct deliverAs (idle deduced, streaming+steer, streaming+followUp, absent/unknown→steer); (promptId, occurrence) resolution with deviceId from the envelope passed structurally and into the result. 85/85 tests pass.
- **Step 11 — deterministic merge check (5 criteria, all met):** (1) owner gates green in full (tsc 0; 85/85; negative invariant; §5.4-only sync); (2) `gates` workflow **SUCCESS** on PR head SHA `b77f576` via `gh pr checks 6 --json name,state,workflow` (keyed on workflow field); (3) no blocking Skeptic objection (no open objections); (4) judge PASS; (5) no Needs Human and no outstanding ruling (R2/R3 applied). Merged PR #6 pinned via `gh pr merge 6 --merge --match-head-commit b77f5763b3d54fada5983538eb6c5ff82a89bb10` → **merged**, merge commit `3ea9e810c33e5945f50f1553756d9628ad6fdbfd`. Head SHA unchanged at merge (no drift).
- **CI on merged SHA:** `gates` check on `3ea9e81` conclusion=success (push event run 33431417971), confirmed via commit check-runs and `gh run list`.
- **Step 12 — sync & reconcile:** fetched origin; local `main` (which carried the design-spec/council commit `3c15219` via the PR base, plus two later council-doc commits) rebased cleanly onto `origin/main` (exit 0, no conflicts); `main` now at `21ac078` (council docs) on top of merge `3ea9e81`. Board + card set `Done`; validate.py clean. Council commits pushed to origin.
