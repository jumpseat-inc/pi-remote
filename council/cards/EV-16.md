---
id: EV-16
title: "Make `--headless` discoverable from the `/rc:login` command surface"
state: Deliberating
owner: null
epic: EPIC-7
goal: The `/rc:login` command is registered with a description containing `--headless` and the condition it is for (a remote machine with no usable browser); an attended login whose `openUrl` fails routes to the `login.failure.browserOpenFailed` line, whose English row names `/rc:login --headless` as the remedy; `rc` and `rc:off` descriptions do not contain `--headless`.
---

## Intent

A user on a remote machine sees a `/rc:login` palette row that says only `pi-remote`, then
the attended flow assumes a browser can reach `http://127.0.0.1:<port>/callback` on the remote
host — which it cannot — so the user waits out the timeout and never learns `--headless`
exists. The fix belongs at the surface where the user chooses the command (the registered
description, carrying the condition, not just the token) and at the browser-open failure line,
which must be reachable for that case and name the remedy.

## Acceptance

1. The test harness widening needed to capture `registerCommand`'s `opts.description` is
   declared in the card and the PR (today `test/index.test.ts`'s `command` dep discards opts
   and `test/pi-sdk-load.test.ts`'s strict Proxy does `void opts`); the registered options for
   `rc:login` carry a description whose English text contains `--headless`, asserted at the
   real boundary via the load smoke.
2. Negative pin: the registered descriptions for `rc` and `rc:off` do not contain the
   substring `--headless`.
3. With a failing `openUrl` dep injected, attended login emits
   `login.failure.browserOpenFailed` and does not proceed to the
   `login.attended.waiting`/loopback-`fallback` path as if a browser had opened; the row's
   English text contains `/rc:login --headless`; the byte-diff is named in the PR.
4. If argument completion is added, declaring `getArgumentCompletions` is an explicit R-TYPE-1
   re-diff, not a silent widening.
5. Copy for this card's row and EV-17's wait/cancel copy is settled in one shared design pass
   (card-level constraint).
## Deliberation record (full-council path, Deliberate mode, surface-touching — designer seated)

Run 2026-09-25, main HEAD 5736131 → 5dcb26e. Three rounds (cap 3), all verbatim texts and
results in `council/cards/EV-16-deliberation/`:
`owner-r1.md`, `principal-r1.md`, `designer-r1.md` (independent first passes, isolated
dispatches); `owner-r2.md`, `principal-r2.md`, `designer-r2.md` (exchange round 2 —
verbatim key parts as circulated to the seats); `round3.md` (final round + facilitator
convergence note); `skeptic-r1.md` (step-4 attack — O1–O7 all closed-green, no open
objections); `consolidator-synthesis.md` (S1–S10: SETTLED, no open judgment, no open
objections, nothing `Needs Human`, nothing routed to `product-owner`).

Settled outcome (full spec: `docs/superpowers/specs/2026-09-25-EV-16-design.md`):

- Descriptions: `rc` → `Start the remote tunnel — if not enrolled, run /rc:login first.` ·
  `rc:off` → `Stop the remote tunnel.` · `rc:login` → `Enroll this host — use --headless on
  a remote machine with no usable browser.`
- `openUrl` contract: optional; absent ⇒ proceed (with `opening` print made conditional —
  today's unconditional "Opening your browser…" with no opener wired is false copy);
  present-and-false/rejects ⇒ Phase-1-fixed `login.failure.browserOpenFailed` row +
  `{ kind: "failure", reason: "browserOpenFailed" }`. Attempt BEFORE the fallback print.
- Harness widening: `RemoteControllerDeps.command` gains required
  `opts: { description: string }`; both `test/index.test.ts` stand-ins and the
  `test/pi-sdk-load.test.ts` strict Proxy capture it (real-boundary assertion).
- `browserOpenFailed` stays out of the id overlay by design; descriptions noted in the
  COVERAGE BOUNDARY. PI-SPEC §7.2/§8 "opens the default browser" prose-synced in the PR
  (Spec Correction Governance). PR claims discipline: dormancy named, opener wiring named
  as follow-up.
- Shared design pass item (b) — EV-17: affordance settled to
  `ctx.ui.confirm(title, message, { signal })` via a `waitForCancel` dep raced against the
  callback wait (Skeptic O1: the member exists on the installed SDK — the designer's
  nonexistence hold was refuted; O2: a mid-wait command cannot dispatch — the designer's
  registered-command affordance collapsed on timing). Wait line unchanged
  (`Waiting for browser…`); dialog copy `Waiting for browser…` / `Cancel sign-in?`; cancel
  line unchanged (`Sign-in cancelled — no credentials were saved.`, printed once by the
  facade). Recorded on EV-17's card; EV-17 promotion unblocked.
- Scope exclusions: no opener wiring, no `getArgumentCompletions` (explicit R-TYPE-1
  re-diff owed by any future card that adds it).

Phase-1 rulings applied: `error and empty-state text` (fixed row, verbatim) · `surface copy`
(delegation consumed by this design pass) · `state and field naming` (EV-15, untouched).
