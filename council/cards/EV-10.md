---
id: EV-10
title: "Enrollment and identity spec — discovery, both grant flows, tokens and claims"
state: Done
owner: null
epic: EPIC-2
goal: docs/SERVER-SIDE-SPEC.md specifies the full enrollment and identity surface — the discovery document with required endpoints, the attended authorization-code-plus-PKCE flow, the headless device flow, refresh and revocation semantics, and the token claim and scope contract — as a self-contained section restating every shape and error case the client drives.
---

## Intent

The enrollment contract exists as shipped client behavior; this card writes the
server's side of it **on the page, in full** — nothing may be left to "as the
client expects". Covered: the discovery document (`authorization_endpoint`,
`token_endpoint`, `device_authorization_endpoint` required; `revocation_endpoint`
optional), attended authorization-code + PKCE with loopback redirect, headless
device flow with `interval`/`slow_down`/`authorization_pending`/`expired_token`/
`access_denied` semantics, refresh as `grant_type=refresh_token` at
token_endpoint (no separate /refresh), the `pi-remote:host` scope, and the token
claim contract (tenant-scoped `sub`). The error semantics must be stated with
their client-consumed meanings: 401 = invalid/expired/revoked credential (the
client's retry-with-re-enroll case), 403 = valid-but-insufficient-scope (the
client's distinct contact-admin case) — the two are never merged.

**Self-containment requirement:** every request/response shape, status code,
and error body appears complete on the page; the council verifies the section
against the client's enrollment fixtures (the in-repo mock IdP is reference
behavior), but the page cites none of that.

## Acceptance

- Every endpoint the client calls is specified with complete request/response
  shapes, status codes, and error bodies.
- Both grant flows are specified step by step, including the device-flow error
  codes the client's driver handles by name.
- Token claims and scope semantics are stated fully on the page.
- Council-side check (card record, not the page): the section passes the
  client's enrollment fixtures with nothing the client sends unspecified and
  nothing the page specifies the client contradicts.

## Verification record (council-side)

**Cycle 1 — Skeptic at PR #21, SHA 5b4236e (job-18.3, 2026-09-02): BLOCKS.**
Gates: tsc exit 0 (gate-integrity probed), bun test 207 pass / 1 skip / 0 fail (gate-integrity probed) — both closed-green. Probes: coverage (O5) closed-green; blocklist R5 (O6) closed-green; §1 consistency (O7) closed-green; fixture conformance 3(a) closed-green. Findings:
- **F1 (closed-red, blocking):** §2.4 specifies the refresh request as `application/x-www-form-urlencoded`; the shipped client's refresh sends `application/json` with a JSON body, and the in-repo mock IdP JSON-parses refresh bodies (throwing on form-encoded input). A §2.4-conformant server reads no `grant_type` from the client's body and must answer 400 — the shipped client is not interoperable with a conformant server. Silent in the PR body.
- **F2 (secondary):** §2.4's refresh table and the PR body carry `client_id`; the shipped client sends only `grant_type` + `refresh_token`. Silent divergence.
- **O4(b) partial (closed-red, documentation gap):** the `interval`/`refresh_token` REQUIRED tightenings are not called out in the PR body (the poll-error 400 divergence (a) is — verified accurate).
Skeptic's fix menu: align §2.4 to the client's JSON refresh body and disclose, or keep form-urlencoded and add an explicit divergence call-out plus a host-side alignment card (the poll-error (a) precedent). Card returned to In Progress; owner dispatched with the findings verbatim.

**Cycle 2 — Skeptic at PR #21, SHA a2dd2fc (job-18.5, 2026-09-02): PASS, no open objections.**
Owner fix cycle 1 (job-18.4): Option A — §2.4 refresh request aligned to the shipped client's actual JSON body (exactly two REQUIRED fields, `client_id` dropped with MUST-NOT-require rule), RFC 6749 §2.3.1 divergence disclosed in the PR body; all five divergences now called out, none silent. Skeptic closed F1, F2, O4(b) all closed-green; regression sweep (blocklist, §1 untouched, rotation rule intact) closed-green; gates at a2dd2fc closed-green with failure-injection demonstrated both ways. Two non-blocking editorial residuals recorded (call-out 5 parenthetical terseness; call-out 2 citing §2.3.1 rather than §6+App B — substance verified accurate). Verify→fix cycles used: 2 of 3.

**Step 13 — follow-up filing (2026-09-02).** The filing question was routed to the orchestrator, which ruled directly (EPIC-1 precedent: step-13 follow-up filing is the orchestrator's confirm-or-drop bookkeeping, not a ruling-seat decision). Ruling, applied verbatim:
1. PR-body call-out 5 parenthetical ("(§2.2, §2.3, §2.4)" vs the response-field OPTIONAL nuance): **CLOSED AS DOCUMENTED.** The call-out lives on the merged PR body, not in any repo file — a fix card would have no artifact to touch. The merged page was verified unambiguous; this card record's note of the nuance IS the documentation.
2. PR-body call-out 2 RFC citation anchor (§2.3.1 vs §6 + Appendix B): **CLOSED AS DOCUMENTED.** The Skeptic verified the substance accurate as-is; same no-artifact reasoning.
3. Owner's "we should also" RFC conformance alignment (the merged §2.4 specifies refresh as a JSON body, deliberately diverging from RFC 6749 §2.3.1 form-encoding): **FILED AS BACKLOG — FLLWUP-18**, "Align the refresh request encoding with RFC 6749 form-encoding or document the divergence permanently". Rationale: no functional gap exists today (the Skeptic verified the shipped client's refresh body is exactly the REQUIRED shape on the page), but it is a genuine, implementable future direction — the same preservation-of-intent standard applied to FLLWUP-17. State Backlog; epic EPIC-2. Scope: either migrate the refresh request to RFC 6749 §2.3.1 form-encoding (client and spec in the same PR, per the governance precedent) or promote the JSON-body divergence from PR-body disclosure to a stated, permanent exception in docs/SERVER-SIDE-SPEC.md — the deliberation picks.

**Merged (2026-09-02).** PR #21 merged with `--match-head-commit a2dd2fc9fc970d27449e3637b820f89cba0922a6` (the SHA all five deterministic-merge criteria were read against); merge commit 7170212. CI on the merged SHA: `gates` success, `gates-windows` success (check-runs API). Judge PASS (job-18.6). Verify→fix cycles used: 2 of 3. Local main was rebased onto origin/main to replay this runner's own unpushed council bookkeeping commits (touching only council/); the merge commit was verified an ancestor and docs/ verified identical to the merged tree — no forced resolution.
