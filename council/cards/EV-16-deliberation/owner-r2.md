Owner round-2 (verbatim, key parts): Conceded ordering to principal — attempt before fallback; on failure the fallback URL is a DEAD remedy (finally closes the loopback server and discards verifier/state, so printing it violates Copy Honesty). Held against undefined-as-failure; third reading: make `login.attended.opening` conditional on deps.openUrl being present (today :535 prints "Opening your browser…" unconditionally with no opener wired — that line is false today). Absent openUrl → skip `opening`, print `fallback` + `waiting`, proceed. Present+false/reject → failure row, return {kind:"failure", reason:"browserOpenFailed"}. Opener wiring = follow-up card; failure row production-dormant, PR must say so.
Affordance: withdrew onTerminalInput; holds `ui.confirm({signal})` — cites installed types.d.ts:73 `confirm(title, message, opts?): Promise<boolean>` and ExtensionUIDialogOptions.signal (:39, "AbortSignal to programmatically dismiss the dialog") + examples/extensions/timed-confirm.ts proving the pattern. Wiring: LoginDeps.waitForCancel?: (signal) => Promise<boolean> implemented as requireCtx().ui.confirm("Waiting for the browser…", "Cancel sign-in?", { signal }), re-armed on false; driver races it against cbPromise; confirm-cancel settles cbPromise {type:"cancelled"}; callback-won aborts signal; cancel() aborts same signal. Copy: waiting stays "Waiting for browser…"; login.cancelled unchanged, printed once by facade.
Descriptions (i): rc → `Start the remote tunnel — if not enrolled, run /rc:login first.` (principal's — converged); rc:off → `Stop the remote tunnel.` (principal's — converged); rc:login → `Enroll this host with the control plane — on a remote machine with no usable browser, use --headless.` with fallback shorter form if truncation bites.
