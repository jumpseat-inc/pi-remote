Designer round-2 (verbatim, key parts): Withdrew FLLWUP-107 citations (don't exist; substantive focus-steal concern re-argued without them). Conceded openUrl semantics (absent ⇒ proceed) and PI-SPEC correction to product-owner.
HOLDS: both `ctx.ui.confirm({signal})` and `onTerminalInput` are claims about surfaces that do not exist — but the designer checked only the VENDORED mirror: src/pi-sdk-on.ts:25-37 PiExtensionContext.ui has exactly setStatus and input; "ui.confirm" in DepsOnEvent (:94-99) is a synthetic EVENT name, not a method; `examples/extensions/timed-confirm.ts` → 0 hits IN THIS REPO. (The owner/principal cite the INSTALLED SDK types.d.ts — a different surface the designer did not check.) So the designer collapses the choice to: (1) registered /rc:login:cancel command, or (2) ctx.ui.input() raced against the wait (real vendored primitive). Holds (1); labels the principal's blocked-editor claim an unverified prediction (P3) and requests an out-of-band smoke: if the host blocks a second command's invocation mid-wait, the registered-command affordance collapses and the fallback is ctx.ui.input racing the wait.
Wiring requirements named: cancel() must also settle cbPromise with {type:"cancelled"} (else EV-17 acceptance-2 timing fails); clearTimeout so no duplicate timeout settle; cmdHolder cell for the in-flight facade.
Descriptions (i): rc → `Open a tunnel to a live remote session`; rc:off → `Close the tunnel`; rc:login → `Enroll this host with the control plane — use --headless on a remote machine without a browser` (action clause first so truncation preserves the verb; "without a browser" tighter than "no usable browser").
Wait line (holds round-1): `Waiting for browser… run /rc:login:cancel to abort.` Cancel line unchanged (:201). No-op refusal line: `No /rc:login in progress — nothing to cancel.` Cancel command description: `Cancel an in-progress /rc:login — no-op if none is running`.
