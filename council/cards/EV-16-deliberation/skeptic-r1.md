Skeptic step-4 report (verbatim result summary):
O1 Surface existence — CLOSED-GREEN: installed types.d.ts:72-73 `confirm(title, message, opts?): Promise<boolean>`, signal?: AbortSignal at :39; timed-confirm.ts EXISTS in installed examples and uses {signal}; scratch compile probe EXIT=0. Designer's ui.confirm-nonexistence claim refuted (true only of the vendored minimal mirror src/pi-sdk-on.ts, which omits confirm by design, FLLWUP-11).
O2 Mid-wait command dispatch — CLOSED-GREEN (for principal): interactive-mode.js:868-870 sequential await getUserInput → session.prompt; commands typed during a non-streaming wait queue to pendingUserInputs (:2632-2634) and dispatch only after the current handler resolves. Designer's /rc:login:cancel affordance is NOT performable during the wait — collapses on timing.
O3 Ordering/false copy/dead row — CLOSED-GREEN: opening prints unconditionally (:535, no guard), fallback :536 before attempt :539, waiting :541; no reason:"browserOpenFailed" return exists; runtime probe demonstrated the false copy by execution. Acceptance-3 reorder is an implementation obligation.
O4 Production dormancy — CLOSED-GREEN: openUrl only at index.ts:116/:680; createRemoteController deps literal has no openUrl; PI-SPEC.md:289 and :411 claim attended "opens the default browser" — the false spec text the PR must annotate/correct exists exactly as claimed.
O5 Copy-row mechanics — CLOSED-GREEN: browserOpenFailed absent from id overlay (test/copy.test.ts:84-85 pins 21-key derivation); login.cancelled byte-exact at src/login.ts:201.
O6 Mechanical pins on converged strings — CLOSED-GREEN: rc:login contains --headless x1 and "remote machine with no usable browser" x1 (77 chars); rc/rc:off contain zero; binding row byte-exact with Phase-1 ruling, contains /rc:login --headless.
O7 Additional — all CLOSED-GREEN: (1) no existing test asserts the opening/waiting/fallback strings; all attended fixtures inject openUrl→true; change additive. (2) Widening enforcement bites at call sites (TS2554 probe EXIT=1); stand-in lambdas stay legal TS — correction to owner's framing. (3) EV-17 red-at-base proven by runtime probe: cancel at ~50ms against 300ms wait → outcome cancelled but elapsed 305ms; cb.type==="cancelled" branch unreachable today; waitForCancel design would make it reachable. (4) Informational pre-existing drift: COVERAGE BOUNDARY comment says "22 settled keys", test pins 21 (FLLWUP-42 already tracks this).
Verdict: no open objections. bun test 314 pass / 1 skip / 0 fail; repo typecheck clean at HEAD 5dcb26e.
