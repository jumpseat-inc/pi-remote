Principal round-2 (verbatim, key parts): Conceded opener-wiring scope — EV-16 ships dep-level branch + descriptions; PR may claim dep-level branch implemented and fixture-reachable + description is the production user-facing fix; PR may NOT claim browserOpenFailed reachable for a real user or that production attended login attempts a browser; must name dormancy + follow-up and correct/annotate docs/PI-SPEC.md:287-289 ("opens the default browser" is false today). Holds absence⇒proceed (routing absence to failure would tell local users with working browsers to run --headless — a regression).
NEW load-bearing finding: a registered command cannot be performed during the wait — interactive-mode.js:867-869 `await getUserInput(); await session.prompt(userInput)` sequential; session.prompt awaits command.handler (agent-session.js:1347); commands typed at the editor queue to pendingUserInputs and only shift after the current prompt resolves. So `/rc:login:cancel` typed mid-wait dispatches only after the wait ends — designer's affordance fails EV-17 Acceptance 1 on timing. Labeled falsifiable (his claim 1).
Affordance: holds `ctx.ui.confirm(title, message, {signal})` via LoginDeps.waitForCancel?: (signal) => Promise<boolean>; driver races vs cbPromise; on confirm set ctl.cancelled, wake wait, return {kind:"cancelled"}; remove or make-reachable the dead cb.type==="cancelled" branch. Wait line unchanged "Waiting for browser…"; dialog title "Waiting for browser…", message "Cancel sign-in?"; cancel line unchanged, printed once by facade.
Descriptions (i): rc → `Start the remote tunnel — if not enrolled, run /rc:login first.`; rc:off → `Stop the remote tunnel.`; rc:login → `Enroll this host — use --headless on a remote machine with no usable browser.` Threading: widen RemoteControllerDeps.command to (name, handler, opts: { description: string }) required; forward to pi.registerCommand(name, {...opts, handler}); COVERAGE BOUNDARY note in src/copy.ts.
