Round-3 (final) positions, verbatim key parts:

OWNER: Final rc:login string — minimal merge: `Enroll this host with the control plane — use --headless on a remote machine with no usable browser.` Rationale: adopted principal's remedy-early order (truncation safety); kept "with the control plane"; card-exact condition phrase. TRADEOFF: ~101 chars vs principal's ~78 — "If the principal holds that brevity wins, I yield to their round-2 string as-is — the substantive convergence (remedy-early order, exact card phrase) is identical; only the clause is at stake, and I won't burn the final round on it." rc/rc:off converged strings stand.

PRINCIPAL: Final rc:login: `Enroll this host — use --headless on a remote machine with no usable browser.` Rationale: action-first and token-early (designer's truncation argument), card-exact condition phrase, no "with the control plane" spending pre-truncation characters. Notes designer's own candidate contradicted its stated tiebreaker ("without a browser" vs card's "no usable browser"). rc/rc:off converged strings stand.

DESIGNER: Output truncated mid-stream (hit a turn/token limit mid-reasoning about truncation measurement); delivered NO final string and no conditional affordance ranking. Round-2 position stands as its last complete statement: registered /rc:login:cancel command preferred; ui.confirm claimed nonexistent (but designer only checked the vendored mirror src/pi-sdk-on.ts, NOT the installed SDK types the owner/principal cite); P3 smoke requested on command dispatchability mid-wait; wait line `Waiting for browser… run /rc:login:cancel to abort.`; no-op refusal line proposed.

FACILITATOR NOTE: Owner's yield + principal's hold converge on the principal's string. The designer's two standing design criteria (action clause first; remedy token early) and the card-exact condition phrase are all satisfied by the converged string; the designer filed no contrary final position. The converged strings stand as the shared design pass outcome for (a), subject to the Skeptic's mechanical checks (contains `--headless`, contains the condition, negative pin on rc/rc:off).
