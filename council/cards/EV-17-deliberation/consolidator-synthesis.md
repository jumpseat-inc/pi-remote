# Consolidator — step 5 synthesis (job-21.8)

## Settled

**Closed by the binding settled block (product-owner ratified, not re-litigable):**
- **Affordance and wiring contract** — `ctx.ui.confirm(title, message, { signal })` behind an optional `LoginDeps.waitForCancel?: (signal) => Promise<boolean>`; re-armed on `false` until the signal aborts; confirm-cancel settles `{ type: "cancelled" }`; `cancel()` must wake the wait and clear the timer.
- **Copy, byte-exact and unchanged** — wait line `Waiting for browser…` (= dialog title), dialog message `Cancel sign-in?`, cancel line `login.cancelled` = `Sign-in cancelled — no credentials were saved.` printed exactly once by the facade, never the driver; no new refusal line. Pin the two new strings as exported verbatim constants in `src/login.ts` (Stable Keys precedent); no `src/copy.ts` keyed rows, one coverage-boundary sentence only.
- **`/rc:login:cancel` command rejected** (settled-block evidence: host input loop parked in `session.prompt` mid-wait — Skeptic O2 of the shared pass). No new command; the confirm dialog is the only user-facing surface.

**Closed by Skeptic tests that ran:**
- **O-1 closed-green** — the owner-r1 wiring (`requireCtx().ui.confirm(...)` inline at the `index.ts:676` call site) genuinely does not compile (`TS2552` at the probe). The corrected position stands: a new `RemoteControllerDeps` member (e.g. `uiConfirm`) built in the `export default` closure from `requireCtx().ui.confirm` and threaded to the call site. Member *name* is implementation-local, the threading is forced.
- **O-2 closed-green** — installed SDK abort semantics verified in both modes: TUI `resolve(undefined)` → `=== "Yes"` → `false`; RPC `resolve(defaultValue)` → `false`; rejection only via host `.reject()`. Abort is an ordinary `false`, never a rejection. Bonus: TUI polarity is affirming (`["Yes","No"]`, `=== "Yes"`), so the driver's `true → cancelled` mapping holds at code level for the installed host. The re-arm loop must check `signal.aborted` after the await, never branch on the resolved value to detect abort; rejection ≠ abort ≠ cancel.
- **O-3 closed-green (wake claim) / closed-red (citation + reason sub-claim)** — the green half: at base, `cmd.cancel()` mid-wait does not wake the wait (probe: cancel at 120 ms resolved at 501 ms). The red half: `vault/wiki/login.ts.md:55` is fabricated (the page is 25 lines), and the base outcome is `{ kind: "cancelled" }`, not `reason: "redirectTimeout"` — the flag check at `:572` precedes the timeout branch. The red-at-base record is **timing-only** — assert `elapsed ≥ redirectTimeoutMs`, nothing about `reason`; the print-once assertion is green at base too and does not discriminate; the `wiki:55` citation must be struck from the record.
- **O-4 closed-green** — driver invariants confirmed against source: exactly two settle sites (`:493`, `:566`), the `cb.type === "cancelled"` branch is dead, zero abort plumbing exists, facade `cancel()` is flag-only, base suite green (320/1 skip/0 fail). All abort/settle plumbing is genuinely new code; seats' ±3-line citation drift is cosmetic.
- **Closed by all-seats convergence (round 2 — agreement, not a test closure):** single guarded `finish(result)` (settled-guard + clearTimeout + abort + settle) routed from all four terminal paths (server callback, timer, confirm-true, `cancel()`); driver-side re-arm loop with condition `!signal.aborted && !settled`; rejection re-arms with an explicit test; confirm-true settles `{ type: "cancelled" }` **without** setting `ctl.cancelled` so the dead `:575` branch becomes reachable via two honest causes; external `cancel()` sets the flag then wakes; no production caller of `cancel()` exists (verified by grep), so Acceptance 5 is satisfied by the dialog alone and `cancel()`'s wake is documented as a test/external seam; Acceptance 1's print-once assertion drives `createLoginCommand().run("attended")`, never the raw driver.

## Open judgment

1. **Non-interactive host gating** (principal r2; round-2 summary item 3): whether `waitForCancel` is wired unconditionally or gated to hosts where `confirm` can genuinely wait on a user. One side: wire unconditionally — simple; an immediately-`false` re-arm loop is bounded only by the timeout. Other side: designer r1 open item 5 — optional-absent is acceptable by construction (no TUI user in RPC mode), and an unbounded re-arm on a non-interactive host is a live spin hazard for the full `redirectTimeoutMs`. Needs `product-owner`; escalate to `steward` if the robustness/simplicity tradeoff is contested.
2. **PI-SPEC §7.2 prose-sync scope** (owner r1, explicitly flagged and never answered): whether the attended-flow bullet's one-sentence cancel-affordance update rides the implementing PR or is out-of-card scope. Owner claims in-scope; no facilitator ruling exists. Needs `product-owner`.

Nothing else is open on values: loop placement, member naming, and `finish()` factoring were explicitly converged or declared implementation-local taste.

## Open objections

1. **O-1b closed-red — the settled wiring cannot compile against this repo's type graph.** The probe added the corrected r2 wiring and got `TS2339: Property 'confirm' does not exist on type '{ setStatus; input }'` at `index.ts:953`. Root cause verified by inspection: `package.json` has no `@earendil-works/pi-coding-agent` dependency; `PiExtensionContext` comes from the vendored `src/pi-sdk-on.ts`, whose `ui` is exactly `{ setStatus, input }` (the only `"ui.confirm"` there is a fixture-only event name). The vendored file was absent from every seat's changed-file list; O1's "exit 0" compile probe measured the installed SDK's types — a universe this repo never compiles against. **Fix shape not settled by the deliberation.** The evidence forces *this much*: the type the repo compiles against must gain `confirm` matching the installed SDK's real signature — `confirm(title, message, opts?: { signal?: AbortSignal; timeout?: number }): Promise<boolean>` — per the FLLWUP-11/R-TYPE-1 vendor-mirror discipline, or the wiring cannot land green. What the evidence does **not** force is the route: extending the vendored `src/pi-sdk-on.ts` (repo precedent, but a changed file no seat proposed) vs. adding the real SDK as a dependency vs. some cast-based escape. The first is evidence-aligned; which one is ratified is a judgment the deliberation never took up. Step-7's changed-file list must name the remediation explicitly; a spec written from the current record lands red.
2. **O-3 closed-red (residual) — record repair owed.** The red-at-base record's citation (`wiki:55`) is fabricated and its `reason` expectation is wrong. The fix shape *is* settled by O-3 itself (timing-only red, corrected base outcome `{ kind: "cancelled" }`); what has not happened is the repair. The spec's red-at-base section must be written from O-3's probe results, cite the probe (not the wiki), and the wiki correction should be noted for the record — no code consequence.
3. **O-5 open-untested — dead-branch reachability is not observably pinned.** Through the planned test surface, gate-cancel and flag-cancel are observationally identical; no settling test can run until the mechanism exists. The spec must add the settling test Skeptic O-5 named: drive `runAttendedLogin(deps, null, ctl)` with a **test-owned** `ctl`, gate resolving `true`; assert `outcome.kind === "cancelled"` **and** `ctl.cancelled === false` — an implementation that sets the flag on the confirm path fails, proving `:575` (not `:572`) executed.

Non-blocking carry-forwards (not Skeptic objections): designer P-A/P-D/E/F/P-G/P-H CDP smokes remain unrun and route to step 13; P-G's *label* question is host-controlled taste now that O-2 closed polarity at code level.

## Verdict

The card **needs a ruling before step 7**. The mechanism, copy, invariants, and test map are converged and well-evidenced — but O-1b is a closed-red defect sitting under the settled wiring itself, and its remediation (extend the vendored `src/pi-sdk-on.ts` to mirror the installed SDK's `confirm` signature, or take a real SDK dependency) was never deliberated or ratified by any seat, and the changed-file list is wrong until it is. Route to `product-owner` in one pass: (1) ratify the O-1b remediation route, (2) the non-interactive-host gating call, and (3) the PI-SPEC §7.2 prose-sync scope question. With those three rulings in hand, the spec can be written immediately — O-5's test and O-3's corrected red-at-base record slot straight into it.
