---
id: FLLWUP-5
title: "Emit pi.human_input.resolved host-side completion event"
state: In Review
owner: null
epic: EPIC-1
goal: A remote device that resolves an approval prompt receives a CUSTOM pi.human_input.resolved completion frame (promptId, deviceId, ts) confirming its resolution was applied, emitted via an added ui_prompt_end surface in translate.ts and wiring in the lifecycle layer.
---

## Intent

Filed from EV-6's step 13. EV-6 shipped the steering-fallback resolution path
(per product-owner R2 and steward R3 rulings — the fallback is the permanent
product behavior) but deferred the host-side completion signal as a
forward-dependency: the remote user currently gets no explicit confirmation
that their resolution was applied, only indirect evidence from session output.
Root cause: the SDK emits `ui_prompt_start`/`ui_prompt_end` but translate.ts's
PiEvent union only carries `ui.confirm`, so `ui_prompt_end` never reaches the
AG-UI stream. User-visible surface — the remote client's approval UI, which
after this card can flip the prompt to "resolved" state on real feedback
instead of guessing.

## Acceptance

- translate.ts's PiEvent union carries `ui_prompt_end` and maps it through the
  established CUSTOM conventions; the pure fold shape and purity guards stay
  green.
- The lifecycle wiring (or its owner at run time) emits CUSTOM
  `pi.human_input.resolved` with `{promptId, deviceId, ts}` when a resolution
  (direct or steering-fallback) is applied to a prompt EV-6 tracked.
- The deviceId in the frame is the resolving device from the envelope, never
  into free text (EV-6's invariant).
- Fixtures cover the direct-resolution and steering-fallback paths; bunx tsc
  --noEmit exit 0; bun test exit 0 with the full suite green.
- Any §4-adjacent spec sentence this needs rides the PR as a
  facilitator-authored evidence-cited amendment per the standing precedent.

## Deliberation record

### Step 1 — path classification

- Full council (not mechanical): cross-seam (translate.ts pure mapper + inject.ts
  resolution surface + index.ts lifecycle wiring) and spec-ambiguous/design-judgment —
  the relation between an added `ui_prompt_end` PiEvent mapping in the pure fold and
  the lifecycle-layer emission of `pi.human_input.resolved {promptId, deviceId, ts}`
  is not pinned (a pure fold cannot mint `ts` or know the envelope `deviceId`; whether
  `ui_prompt_end` maps to the resolved frame directly, to a separate surface, or both,
  and whether the resolved frame fires for untracked steering-fallbacks, each admits
  a different design).
- Surface-touching: yes — the remote client's approval UI flips to a "resolved" state
  on real feedback (EV-6 H5 forward-dep). `designer` seated as third generator in steps 2–3.
- Binding context carried in (not open for relitigation): CUSTOM shape
  `{type:"CUSTOM", name:"pi.<category>", value:{pi, data}}` with `name` the sole
  dispatch key (§4); deviceId from the inbound envelope never into free text (EV-6
  invariant); the pure fold shape and purity guards (no clock/entropy in translate.ts)
  stay green — the emitting site of the resolution frame is the lifecycle layer
  (index.ts); resolution tracking to hook is EV-6's (promptId, occurrence) registry in
  inject.ts, with `InjectResult` kinds `resolved` (direct, fixture-seam only per R3
  Side B) and `steered_fallback` (the permanent live path).
- Grounding: `vault/wiki/index.md` has no module pages (stub catalog) — semantics
  grounded in docs/PI-SPEC.md §4/§5.4/§7.3 and the installed pi SDK
  (`pi-coding-agent/dist/core/extensions/types.d.ts`): `ui_prompt_end` is a real SDK
  event `{type:"ui_prompt_end"; reason:"ui_prompt"; kind:UIPromptKind; title?}`,
  registered via `pi.on("ui_prompt_end")`.

### Step 2 — independent first pass (round 1; owner job-34.1, principal job-34.2, designer job-34.3 — dispatched in parallel, each with only the card + binding contract)

**owner (job-34.1, verbatim).** Two independent frames at two layers. (1) Pure surface in translate.ts: add `ui_prompt_end` to PiEvent; map to `CUSTOM name:"pi.human_input", value.pi:"ui_prompt_end", data:{kind,title?}` — only what a pure fold can carry (no ts/deviceId/promptId; cannot correlate to a prompt because ui_prompt_end carries only kind+title, so promptId=fnv1a(kind\0prompt) is unreconstructible). (2) Lifecycle completion in index.ts: change onInbound to capture InjectResult; on `resolved` or `steered_fallback`-with-`tracked:true` emit `CUSTOM name:"pi.human_input.resolved" data:{promptId, deviceId, ts}` — deviceId from result (recorded from envelope, never free text), ts from injected `deps.now`. Requires adding `tracked: boolean` to the steered_fallback InjectResult so index.ts can tell tracked from untracked. Resolved fires ONLY for EV-6-tracked prompts (resolved ∪ tracked steered_fallback); NEVER for untracked steered_fallback (a prompt this host never raised — a phantom ack) or stale (already surfaced via pi.human_input.stale). Two frames neither feed each other; split is forced by purity (fold cannot mint ts/deviceId/promptId). §4-adjacent amendments: §4 row note pinning ui_prompt_end→pi.human_input value.pi:"ui_prompt_end" data:{kind,title?} as passive mirror; §5.4 paragraph pinning the resolved frame (fires only for tracked; deviceId from envelope never into text; ts host clock). Testable claims: fixtures for direct (fixture seam), steered-fallback tracked, untracked (no resolved), stale (no resolved), pure fold mapping + G-11 guard. Tradeoffs: widening InjectResult steered_fallback with tracked (published toEqual checks must update); resolved emitted on commitment (steer sendUserMessage is fire-and-forget) not full delivery; emitting ui_prompt_end mirror is arguably noise but required by acceptance. Residuals to route: whether ui_prompt_start should also be added (card scopes ui_prompt_end only — preserves asymmetry); whether client should dispatch on value.pi:"ui_prompt_end"; whether resolved should await sendUserMessage; whether occurrence belongs in the resolved frame (acceptance mandates only {promptId,deviceId,ts}).

**principal (job-34.2, verbatim).** The frame the card asks for is a lifecycle emission, not a pure mapping — ui_prompt_end is a *different* frame entirely. Forced by three purity facts: (1) promptId lives in the inject.js (promptId→occurrence) registry closure invisible to the fold, and ui_prompt_end carries no promptId (types.d.ts {type,reason:"ui_prompt",kind,title?}); (2) deviceId only on inbound envelope transport.ts, never seen by the fold; (3) ts is a clock read banned by purity. So pi.human_input.resolved {promptId,deviceId,ts} cannot be produced by translate.ts. translate.ts owns the informational ui_prompt_end mapping → `CUSTOM name:"pi.human_input.closed"` value:{pi:"ui_prompt_end", data:{kind,title,schemaVersion:1}}; index.ts owns the resolution emission. The ui_prompt_end→registry correlation is *impossible* (schema gap: kind≠promptKind, title≠prompt; plus purity), so ui_prompt_end/closed is an independent "host stopped waiting" surface — correct, because ui_prompt_end fires for reasons unrelated to a remote answer (host operated own TUI, dismissed, timeout) that must not forge a resolved. Trust boundary: index.ts:341 `void injector.handle(env)` throws away the InjectResult — the seam. New wiring awaits and dispatches by kind: resolved→emit; steered_fallback tracked→emit; steered_fallback untracked→no resolved (R2 fallback_to_steer already fired); stale→no resolved (pi.human_input.stale already emitted); ignored/injected→nothing. Seam gap: steered_fallback carries reason:"mode" for BOTH unknown-promptId and tracked-but-not-directly-resolvable — index.ts can't distinguish without a `tracked:boolean` flag on steered_fallback (false at unknown-entry fallback, true at live-entry fallback). Without the flag you must choose over-emission (false confirmation) or no emission (dropping the permanent path — the card's point). Recommend the flag. Q4: fire resolved only for EV-6-tracked. Q5: separate, never merged — resolved sourced from InjectResult, not ui_prompt_end (in the steer-fallback path the host UI promise may never settle, so ui_prompt_end/closed may not fire at all — strongest argument resolved must come from InjectResult). Q6: the resolved frame belongs at §5.4/§7.3, plus a one-line §4 note that ui_prompt_end maps to informational pi.human_input.closed. Blind spots: (a) inside mapper, ui_prompt_end kind/title never match the promptId it helped mint, and reason:"mode" conflates two truth-distinct outcomes (fallback() is shared); (b) inside index.ts, the discarded result is the only carrier of tracked/untracked, so temptation is to emit resolved on every non-ignored result and fabricate confirmations; also warns the existing `forward(ev as PiEvent)` cast pattern (index.ts:565-569) is a LATENT DROP — SDK events are keyed `type:` while translate.ts switches on `input.event`; message_start/message_update/tool_result/user_input cast raw SDK shapes and would silently fall through the exhaustive default at runtime (tests never catch it because harness feeds {event:…} payloads). The ui_prompt_end wiring must construct the PiEvent MANUALLY (mirror agent_start, index.ts:558), or it will be dead-on-arrival like ui.confirm. Reframe: two decoupled contracts. In/out of scope on the raise: keep ui_prompt_start out of FLLWUP-5 but record it is a HARD RUNTIME PRECONDITION — while the raise is dead, registerPrompt is never called live, so every live response is unknown→untracked→no resolved, and the client never received a promptId to answer. FLLWUP-5 ships a completion surface that is fixture-reachable but not runtime-observable end-to-end until the raise card lands. Whether acceptance (b) means fixture-green or runtime-observable is open judgment. Module changes: translate.ts PiEvent+=`{event:"ui_prompt_end", reason, kind:UIPromptKind, title?}` + case→CUSTOM pi.human_input.closed; inject.ts steered_fallback+=tracked; index.ts onInbound captures result + emits resolved + registers deps.on("ui_prompt_end", …) manual construction; test harness expose resolvePendingPrompt+now. Testable claims: pure mapping; field-normalization regression net (SDK shape type:→construct event:); direct path; steered-fallback tracked; untracked (never dropped, no resolved); stale; fan-out audit (deviceId winning envelope); gates. Residuals to rule: (1) runtime-observability vs fixture-green for acceptance (b) [open judgment — the resolved frame is correct and testable now but cannot fire live until the raise is wired]; (2) the type:→event: cast pattern is a probable live-path drop affecting existing message translation — separate scope, same normalization need; claim 2 is the falsifiable probe; (3) closed disambiguation when two same-kind prompts concurrent (SDK blocking-UI model is singular, kind likely suffices).

**designer (job-34.3, verbatim).** The remote approval UI should treat `pi.human_input.resolved {promptId, deviceId, ts, kind:"won"|"stale"|"fallback"}` as a single, terminal frame that closes the prompt row on real evidence, keying identity on (promptId, occurrence). Exactly-once per (promptId, occurrence), emitted only from the lifecycle layer. Folded into the same name namespace as the existing two surfaces (pi.human_input.stale, pi.human_input.fallback_to_steer); kind is the single thing the client keys state-transition on. No ui_prompt_end PiEvent needed in translate.ts for the resolution — the resolution is an EV-6 outcome, not an SDK event; ui_prompt_end carries no promptId to bind to. For untracked prompts the resolved frame does not fire (phantom confirmation). The fallback-to-steer path emits BOTH the loud-once fallback_to_steer (explanatory sentence) and `pi.human_input.resolved {kind:"fallback", deviceId, ts}` (state-machine input) — different things for different clients. Gulf closed: remote user now perceives whether their own device won, another device won first (which one), or the host converted the tap to steer. Principle/evidence: knowledge-in-the-world; signifier+conceptual model — four-state lifecycle (raised→won/stale/fallback/unanswered); feedback immediate; purity constraint (fold can't mint deviceId/ts — lifecycle layer only); forcing function (the honest answer to a second tap is don't tap again). Falsifiable predictions H1..H7: H1 exactly one resolved frame per (promptId,occurrence) with winning envelope deviceId regardless of direct/fallback; H2 stale is a separate terminal (client sees both resolved{kind:"won"} and stale{winnerDeviceId} in order, distinct frames); H3 fallback carries resolving deviceId + stable identifier, after fallback_to_steer, kind:"fallback"; H4 no resolved for untracked promptId; H5 resolved keyed by (promptId,occurrence) not just promptId (two occurrences each get own terminal); H6 exactly-once even under retry (keyed on entry.settled, second arrival → stale not second resolved); H7 the resolved frame is the only frame that closes the row; client listens only to lifecycle pi.human_input.resolved — no pure-fold output needed for the flip (if a future SDK stopped emitting ui_prompt_end the UI would still close correctly). §4-adjacent amendment: add one paragraph after the stale sentence in §5.4 (line 192): **Resolution confirmation (FLLWUP-5)** — when a pi.human_input prompt is resolved by a remote device, the extension emits a single CUSTOM pi.human_input.resolved {promptId, occurrence, kind:"won"|"fallback", deviceId, ts}; deviceId the envelope's resolving device; kind:"won" direct (fixture-only), kind:"fallback" permanent steering-fallback (R3 Side A); stale answers receive pi.human_input.stale {promptId, winnerDeviceId} and no resolved; untracked promptIds receive no resolved. No §4 row edits. Preferences: kind discriminator on the resolved frame (one namespace, one dispatcher, three terminal frames; mirror the injector's two-valued result); ts = lifecycle-layer clock (the injected `now`). **Residuals to route:** (1) should resolved carry kind:"won" ever in production — per R3 Side B resolvePendingPrompt returns false in production so the live path is always kind:"fallback"; "won" only honest in fixtures; keeping it costs one discriminator per client switch, forward-compatible — product ruling, argues keep but not authoritative; (2) should pi.human_input.resolved be REPLAYED — replay (JSONL→translate) doesn't carry these events (they originate in the lifecycle layer and have no JSONL entry); on resync a reconnecting client re-sees the raise but not the resolved frame, so a reconnecting client sees the prompt as "open" in replay even though resolved. Acceptable iff client treats replayed history as a snapshot then continues on live frames; a design defect iff client expects replay self-sufficient. No test in this card covers it (replayActiveBranch doesn't run the lifecycle emit path). Route to product-owner. (3) should client see kind:"fallback" REPLACE the fallback_to_steer notice — argues no (notice=sentence user reads; resolved=state the row transitions to), settle at client level, one-line spec confirmation the notice is not deprecated.

### Step 3 — bounded exchange (round 2)

**owner (job-34.7, verbatim, abridged).** Concedes the wire-shape argument to the designer on `kind` (judgment) AND on `occurrence` (a key, not a defect). Grounding: inject.ts:35-41 InjectResult carries promptId/deviceId/direct/text/reason — no occurrence; handleApprovalResponse reads d.occurrence purely as a lookup key and drops it; resolvePendingPrompt in production is () => false (index.ts default), so kind:"resolved" unreachable live; emitCustom has no registry-key knowledge. **kind withdrawn** — one production value (always fallback), derivable by client from resolved{deviceId=me} vs stale{winnerDeviceId=other}, addable later with no break. **occurrence withdrawn from the wire frame only** — not a defect in InjectResult; occurrence is a control-flow key, and threading it speculatively is scope the owner discipline forbids. **Settles the divergence mechanically:** add a strict deep-equal test asserting pi.human_input.resolved.value.data === exactly {promptId, deviceId, ts} (any occurrence/kind key fails), plus a test pinning InjectResult has no occurrence property. Settled: two decoupled contracts; ui_prompt_start out of scope; resolved wire shape {promptId,deviceId,ts}; InjectResult keeps occurrence internal (non-defect). Routes open: whether future concurrency needs occurrence on wire; kind/direct when/if live resolve API; runtime-observability vs fixture-green.

**principal (job-34.8, verbatim, abridged).** The raise and close speak two different identity contracts: the raise publishes (promptId, occurrence) (index.ts:383-388 registerPrompt + stamps f.value.data={...data, occurrence}); the close can't echo it (inject.ts:110 validates/looks up occurrence, inject.ts:38-39 drop it from InjectResult, index.ts:324 discards result). So frame-shape is which identity key the close may express. Reframe: land on the middle ground with teeth — **pi.human_input.resolved = {promptId, occurrence, deviceId, ts}**. Drop kind (dead weight — only live terminal is steered_fallback; deviceId already answers the only production question). Keep occurrence on the wire because the raise already puts it there; the close must echo the identity key the raise established, or the client has a raise keyed (promptId,occurrence) and a close keyed promptId with the singular-model assumption baked into the close alone. Testable claims: (1) occurrence fidelity — register same promptId twice, answer occurrence:2, assert InjectResult.occurrence===2 and emitted resolved.value.data.occurrence===2, then occurrence:1→1 (green proves occurrence can survive e2e; does not alone prove wire placement — that rests on raise-side contract); (2) kind reachability — assert production dep is resolvePendingPrompt:()=>false and kind:"resolved" fixture-only; whether to ship a fixture-only discriminator is design judgment → ruled: do not ship; (3) exactly-once/stale suppression stays green. Settled: (a),(b),(c) as before. Routed open: runtime-observability vs fixture-green; replay self-sufficiency. kind closed by this ruling.

**designer (job-34.9, verbatim, abridged).** CONVERGES to {promptId, occurrence, kind:"won"|"fallback", deviceId, ts} — WITHDRAWS its round-2 minimal position. Why its two strongest grounds did not survive: (1) the "client can derive steered-vs-won" claim works from frame correlation, but the loud-once fallback_to_steer notice is a transient UI artifact, NOT a contract field a second consumer can subscribe to, so on prompt 2+ the client has only the resolution frame; (2) its "SDK blocking-UI-singular" claim is falsified by the EV-6 registry's existence — if (promptId,occurrence) is the server's identity key, promptId alone is insufficient on the wire. Gulf closed: consumer no longer walks prior frames to know if it won or was steered, or which re-occurrence this resolution binds. Principle: knowledge-in-the-world beats knowledge-in-the-head; floor-not-ceiling — the amendment's three-field minimum is a floor, not a ceiling; using tolerance to justify withdrawing fields the server already knows inverts the principle. Falsifiable predictions: without kind a first SDK consumer mis-classifies prompt 2+ steered-vs-won in some case; without occurrence two resolved frames for the same promptId are indistinguishable; loud-once notice is a transient UI artifact. Settled: resolved shape {promptId, occurrence, kind:"won"|"fallback", deviceId, ts}; contract (a)/(b); raise out of scope; acceptance fixture-green. Routed open — testable (owner): whether InjectResult carries occurrence to the wire today (verifiable code claim; producer-side fix local if true). Routed open — judgment (product-owner): whether the (promptId,occurrence) registry is load-bearing in production or defensive against replay/retries; shapes future-proofing but does not alter the contract — occurrence belongs on the frame either way.

### Round outcome (facilitator record)

Exchange hit the ≤3-round cap without full convergence on the resolved-frame WIRE SHAPE. The two decoupled contracts (a) ui_prompt_end → CUSTOM pi.human_input.closed {pi:"ui_prompt_end", data:{kind,title,schemaVersion:1}}, manual construction, distinct name; (b) index.ts emits pi.human_input.resolved on resolved / steered_fallback-with-tracked:true, never untracked or stale; raise (ui_prompt_start) OUT OF SCOPE as hard runtime precondition; acceptance (b) = fixture-green) are SETTLED across all three. The genuine remaining divergence is the resolved-frame field set: owner={promptId,deviceId,ts}; principal={promptId,occurrence,deviceId,ts}; designer={promptId,occurrence,kind,deviceId,ts}. This is an OPEN-JUDGMENT wire-contract/client-visible item — no test settles which fields belong on the wire (occurrence fidelity and kind reachability are testable facts, but the client-visible field decision is judgment). Carried forward to the consolidator → step 6 routing.

Also settled across all three: the latent `forward(ev as PiEvent)` type:/event: cast drop (index.ts:565-569) is a probable live-path bug affecting existing message translation — OUT OF SCOPE for FLLWUP-5, filed as a separate follow-up; the ui_prompt_end wiring must construct the PiEvent manually.

Additional open-judgment-items agreed by all/seats (to be carried by the consolidator): (O1) runtime-observability vs fixture-green for acceptance (b) (raise precondition); (O2) replay-self-sufficiency of the resolved frame (lifecycle-emitted, no JSONL entry, reconnecting client re-sees raise but not resolved); (O3) the resolved-frame wire field set (narrowest item above); (O4) whether future non-singular concurrency / a live resolve API will ever need occurrence/kind on the wire (forward-compat posture).

### Step 4 — Skeptic attacks and runs tests (job-34.10)

Grounded PI-SPEC §4/§5.4/§7.3, translate.ts, inject.ts, index.ts, transport.ts, SDK types.d.ts, test harnesses. Baseline gates closed-green: tsc exit 0; bun test 146 pass / 0 fail.

Probes (all run, real output):
1. **Occurrence dropped from InjectResult** — closed-green. handleApprovalResponse reads d.occurrence as a lookup key only; returned InjectResult variants carry no occurrence (fact behind the O3 field-set dispute).
2. **Raise publishes occurrence** — closed-green. index.ts:391-398 stamps f.value.data={...data, occurrence}; EV-8 occurrence-stamp test proves occurrence 1 and 2 on wire frames.
3. **resolvePendingPrompt is () => false in production** — closed-green. index.ts:608 + inject.ts TEST-ONLY SEAM (R3 Side B); kind "resolved" only fixture-reachable.
4. **`ev as PiEvent` cast drop** — closed-green AND EXTENDED. Worse than silent: SDK `{type:"ui_prompt_end", kind:"confirm"}` MISROUTES to translateJsonl because JS `"kind" in input` is true, colliding with JsonlEntry's discriminator → produces a WRONG `pi.session.info_change` frame. SDK `{type:"ui.confirm"}` → translateLive default → 0 frames (silent drop).
5. **Name dispatch collision** — closed-green. §4 name = sole dispatch key; pi.human_input.closed/.resolved distinct names, no collision.
6. **Purity guard** — closed-green. G-11 static guard; CUSTOM mapping adds no clock/entropy.
7. **Baseline gates** — closed-green. tsc 0; bun test 146 green.
8. **Raise premise** — closed-green, CRITICAL. Installed SDK has NO "ui.confirm" event anywhere; ExtensionAPI.on is an exhaustive TYPED interface with no string-generic overload; pi.on("ui.confirm", …) would be a type error at real SDK levels. No deps.on("ui_prompt_start") exists. **The entire raise path is dead in production — registerPrompt is never called live.**

Objections (verdict: blocks — 3 items):
- **O1 (open-untested):** acceptance (b) is dead-on-arrival in production as scoped. Settling test: the raise precondition was ruled out-of-scope; with no deps.on("ui_prompt_start") + manual construction + registerPrompt wiring, the resolved frame fires in fixtures but can NEVER fire live (registerPrompt never called → every live answer untracked → no resolved). The acceptance's user-visible claim implies runtime-observable; the deliberation ruled the raise out-of-scope. The acceptance criterion must explicitly scope "fixture-green only" or include the raise wiring.
- **O2 (closed-red):** the `ev as PiEvent` cast is a SYSTEMIC bug affecting ALL event subscriptions (seven of ten), not just ui_prompt_end (probe 4). SDK events keyed type: vs PiEvent event:; structurally incompatible; ALL forward(ev as PiEvent) subscriptions dead-on-arrival in production (only agent_start/agent_settled work, which construct inline). Filed out-of-scope by the council, but it is a precondition for ANY live path including contract (b). Blocking until either (a) all casts replaced with manual construction, or (b) the card acknowledges acceptance (b) is fixture-only and updates the acceptance text.
- **O3 (closed-red, planned requirement not a design defect):** steered_fallback InjectResult must gain a `tracked: boolean` field before contract (b) can distinguish tracked from untracked fallbacks (probes 1+3). Blocking implementation dependency.
- **O4 (closed-red):** index.ts local ExtensionAPI.on is `(event: string, …)` while the real SDK's on is an exhaustive typed interface with no generic overload; the pi.on(event,…) bridge passes a generic string to a typed overloaded on — a compile-time error at real type level. The local type hides this.

Skeptic verdict on the design: the two settled contracts (a)/(b) are implementable, but contract (b)'s runtime reachability, the systemic cast bug (O2), and the AcceptanceAPI type gap (O4) are blocking scope/acceptance decisions that require the card to either expand scope (manual construction everywhere + raise wiring) or explicitly scope acceptance (b) to fixture-green.

### Step 5 — Consolidator synthesis (job-34.11, verbatim buckets)

**Agreed design (settled, not reopened):** (1) Contract (a) — translate.ts adds `ui_prompt_end` PiEvent → CUSTOM `pi.human_input.closed`, value {pi:"ui_prompt_end", data:{kind,title,schemaVersion:1}}; name distinct from pi.human_input.resolved; PiEvent constructed MANUALLY (mirror agent_start), never `ev as PiEvent`; fold carries only kind/title (no ts/deviceId/promptId); purity green. (2) Contract (b) — index.ts onInbound captures InjectResult, emits CUSTOM `pi.human_input.resolved` for InjectResult `resolved` and `steered_fallback`-with-`tracked:true`; NEVER untracked fallback or `stale`; deviceId from result (envelope, never free text); ts from injected `deps.now`. (3) `steered_fallback` gains `tracked: boolean` (Skeptic O3 closed-red confirms absent today) — in-scope prerequisite for contract (b). (4) The raise (`ui_prompt_start`/registerPrompt) is OUT OF SCOPE, a hard runtime precondition (all three seats settled). (5) The latent `ev as PiEvent` type:→event: cast pattern is OUT OF SCOPE, filed as separate follow-up (all three seats settled round 3). (6) §4/§5.4-adjacent spec amendments ride the PR.

**Settled disputes (Skeptic probes all green except noted):** occurrence dropped from InjectResult (green); raise publishes occurrence (green); resolvePendingPrompt () => false (green); ev as PiEvent cast worse than silent drop — ui_prompt_end-shaped payload MISROUTES to translateJsonl via kind collision → wrong pi.session.info_change frame (green+extended); name dispatch no collision (green); purity holds (green); baseline tsc 0/bun 146 (green); raise dead in production — SDK has NO ui.confirm, on() is typed exhaustive, no deps.on("ui_prompt_start") (green, CRITICAL); tracked:boolean absent from steered_fallback (O3 closed-red, planned in-scope work).

**OPEN JUDGMENT (→ product-owner, escalating to steward; no test settles, both sides carried equally):**
- **J-FIELDSET** resolved-frame wire field set: owner {promptId,deviceId,ts}; principal {promptId,occurrence,deviceId,ts}; designer {promptId,occurrence,kind,deviceId,ts}. Testable facts (probe1/2) don't decide which fields the client sees. Wire-contract ruling.
- **J-ACCEPT** acceptance (b) fixture-green vs runtime-observable.
- **J-REPLAY** replay-self-sufficiency of the resolved frame (lifecycle-emitted, no JSONL entry, reconnecting client re-sees raise but not resolved).
- **J-FUTURE** forward-compat posture on occurrence/kind (future concurrency / live resolve API).

**OPEN OBJECTIONS (Skeptic claims whose settling test hasn't passed / need a ruling to close):**
- **S-O1 (open-untested)** acceptance (b) dead-on-arrival in production as scoped — needs a product-owner scope ruling: acceptance text scoped to fixture-green, OR include the raise wiring.
- **S-O2 (closed-red)** systemic `ev as PiEvent` cast affecting seven of ten live subscriptions; filed out-of-scope in round-3 when described as "probable live-path drop," but Skeptic later showed it blocks ANY live path incl. contract (b). Scope ruling needed to confirm or overturn the follow-up call.
- **S-O3 (closed-red)** steered_fallback lacks tracked:boolean — agreed in-scope implementation, not a scope-ruling item.
- **S-O4 (closed-red)** index.ts local ExtensionAPI.on is string-generic vs real SDK's typed exhaustive on; pi.on(event,…) bridge is a compile-time error at real type level, hidden by the local type. NO scope ruling exists — in-card fix vs follow-up is product-owner judgment.

**Handoff: NO.** Card cannot move to implementation/ruling until product-owner (escalating to steward) resolves J-FIELDSET, J-ACCEPT, J-REPLAY, J-FUTURE, S-O1, S-O2, S-O4. In-scope and non-blocking: S-O3 (tracked:boolean implementation work must land regardless).

### Step 6 — routing

All open-judgment items (J-FIELDSET, J-ACCEPT, J-REPLAY, J-FUTURE) and open-objection scope rulings (S-O1, S-O2, S-O4) route to product-owner authority. No Phase 1 ruling on EPIC-1 covers any of these (orchestrator: none on record). The consolidation surfaced S-O2 specifically needs the round-3 out-of-scope call re-issued now that the Skeptic showed the cast bug is systemic. Per the escalation contract the facilitator does not dispatch ruling seats. The card remains in Deliberating — NOT Done, NOT retired, NOT returned to Ready — pending the rulings. Returning ESCALATION to the orchestrator for the rulings, then resuming this card on them.

### Step 6 — rulings (product-owner, binding; resumed)

ESCALATION returned; product-owner ruled on all seven items, no steward deferral. Full document: `vault/raw/2026-08-31-po-fllwup5-ruling.md`. Binding and appended verbatim. Two follow-up cards ordered: **FLLWUP-raise** and **FLLWUP-typed-bridge**, pre-confirmed by the orchestrator to be filed as **FLLWUP-8** and **FLLWUP-9** respectively (FLLWUP-8 was never used — an earlier draft under that number was folded into FLLWUP-4 and never filed; verified against council/cards/ before filing).

1. **J-FIELDSET** — `pi.human_input.resolved` carries `{promptId, occurrence, deviceId, ts}` (principal's design); `kind` withdrawn across all three seats. Grounds: `inject.ts` keys its registry on (promptId, occurrence); the raise stamps occurrence on the wire; kind adds a discriminator one production frame never takes (kind:"won" is fixture-only per R3 Side B); deviceId answers the only production question. Reversibility: symmetric on kind (additive later); one-way on occurrence (dropping later breaks compound-key consumers).
2. **J-ACCEPT** — Fixture-green only. Acceptance (b) rewritten (facilitator-authored amendment per the EV-1 Q3 governance precedent) to say contract (b) emits the frame correctly and is testable today; the runtime path is gated on FLLWUP-raise. No spec change to PI-SPEC §4 — the row documents the contract, not runtime reachability.
3. **J-REPLAY** — Replay is NOT required to be self-sufficient for the resolved frame. Reconnect replays the raise from JSONL; the resolved frame is lifecycle-emitted with no JSONL entry kind, so it surfaces only in the live stream after resync. Acceptable: the remote client treats replay as a snapshot and continues on live frames (EV-5's design); a client that never processes post-replay live frames is already broken for every other lifecycle frame.
4. **J-FUTURE** — No contract change beyond item 1. Ship `{promptId, occurrence, deviceId, ts}` as the minimum; everything else is additive later under the name dispatch key.
5. **S-O1** — FLLWUP-5 stays **fixture-scoped**. The raise wiring is its own follow-up card (FLLWUP-raise → filed as FLLWUP-8, Backlog). Card split (binding): FLLWUP-5 scope = contracts (a) and (b); `tracked:boolean` on InjectResult.steered_fallback (S-O3, in-scope); manual construction across all seven `forward(ev as PiEvent)` call sites (S-O2 fold-in, item 6); acceptance rewritten fixture-green per item 2. FLLWUP-8 scope = wires deps.on("ui_prompt_start") with manual PiEvent construction; maps the SDK {kind, title?} payload to a fold-shaped event; wires registerPrompt to the canonical payload shape; re-opens acceptance (b) to runtime-observable when it lands.
6. **S-O2** — The round-3 "out of scope as follow-up" call is **OVERRULED**. The cast fix folds into FLLWUP-5 as manual construction across all seven affected subscriptions, mirroring agent_start (index.ts:556, already manual). Grounds: probe 4 showed the cast is worse than a silent drop (a ui_prompt_end-shaped SDK payload misroutes to translateJsonl via a kind collision with JsonlEntry's discriminator, producing a wrong pi.session.info_change frame) and affects seven of ten live subscriptions. The work is mechanical (design space zero; PiEvent types already specified at src/translate.ts:127-143) and the acceptance is unchanged. The seven subscriptions (binding list): message_start, message_update, message_end, tool_result, ui.confirm, user_input, ui_prompt_end (new in contract (a) — manual by design).
7. **S-O4** — Does NOT fold into FLLWUP-5. Separate follow-up card (FLLWUP-typed-bridge → filed as FLLWUP-9, Backlog): replaces the local ExtensionAPI stand-in (index.ts:43-65, permissive on(event: string)) with the real SDK's typed exhaustive on(). Binding constraint for FLLWUP-5: the local type continues to hide the cast; FLLWUP-5's acceptance must pin the manual-construction fix as defense in depth — it is both the S-O2 fix and the S-O4 mitigation until FLLWUP-9 ships.

The rulings resolve the four open-judgment items and three scope rulings. Step 7 proceeds on the settled design folded with the rulings. FLLWUP-8 and FLLWUP-9 filed at step 13 per the pre-confirmation.

### Step 7 — spec + handoff

Spec written to `docs/superpowers/specs/2026-08-31-FLLWUP-5-design.md` (sole handoff; folds the settled design with the seven rulings; scope split per ruling Item 5; wire shape {promptId, occurrence, deviceId, ts}; acceptance  rewritten fixture-green per Item 2). Committed (154ee89). Card → In Progress on board (4ef5567).

### Step 8 — owner implementation (job-36.1, 45-min window)

Owner implemented the spec in worktree `.worktrees/flluwp-5-resolved` (branch `flluwp-5-resolved`), six TDD commits, pushed, PR **#10** opened against main. Head SHA **96e2866ba894e43a712515dab5d5908ac472a7db**. Local gates recorded: tsc exit 0; bun test **155 pass / 0 fail** (146 baseline + 9 new fixtures). One design-necessity deviation documented by the owner: `translate()`'s JsonlEntry/live discriminator changed from `"kind" in input` to `"entryId" in input` because the new `ui_prompt_end` PiEvent legitimately carries `kind` (the probe-4 kind-collision class); behavior-identical for all pre-existing inputs (155-test suite confirms). Card → In Review on the board from the observed artifact (PR #10 open, `gates` workflow SUCCESS at head SHA) (60ee4a4).

### Step 9 — Skeptic verification (job-36.2, 30-min window)

Skeptic at branch head 96e2866 re-ran the full gate set and its own probes. All seven objections **closed-green**, no open objections: (1) contract (a) ui_prompt_end → pi.human_input.closed pure mapping, runtime probe confirms not pi.session.info_change; (2) contract (b) lifecycle emission, 4 tests incl. strict {promptId, occurrence, deviceId, ts} wire shape, untracked→no resolved, stale→no resolved; (3) S-O3 tracked:true/false + occurrence round-trip, stale has no occurrence; (4) S-O2 `grep 'as PiEvent' index.ts` → no matches, all seven subscriptions manual, forward() ui.confirm special-case preserved; (5) 155 pass / 0 fail, 870 expect() calls; (6) acceptance fixture-green, PI-SPEC §5.4 amendment ends "fixture-green today, runtime-observable once the raise path lands (FLLWUP-8)"; (7) discriminator probe closed-green — `"entryId" in input` sound (every JsonlEntry carries entryId, no PiEvent does), pre-existing JSONL dispatch identical. Gate-integrity self-checks run (tsc and test gates demonstrably fail when broken).

### Step 10 — judge (job-36.3, 15-min window)

Judge evaluated the PR branch at the Skeptic-verified SHA 96e2866 (not main), per the standing step-10 rule. Re-ran the decisive gates itself: tsc exit 0; bun test 155 pass / 0 fail; source audit of `emitResolved()` (exactly {promptId, occurrence, deviceId, ts}, emitted only for resolved / steered_fallback-tracked:true, never untracked or stale, deviceId envelope-derived, ts from deps.now); all seven subscriptions manually constructed, no `as PiEvent` cast. **Verdict: PASS.**
