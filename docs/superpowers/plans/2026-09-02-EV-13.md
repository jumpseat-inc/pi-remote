# EV-13 — §5 Device registry, grants, push reservation, and the server-side trust model — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Write section 5 of `docs/SERVER-SIDE-SPEC.md` per the settled design at
`docs/superpowers/specs/2026-09-02-EV-13-design.md`, in the document's own voice,
plus the step-8 card record. Documentation only — no runtime code changes.

**Architecture:** §5 is appended after §4.9 as new subsections §5.0–§5.14
(0 = opening; 1–13 = the design's §2–§14; 14 = error vocabulary and closed
partitions), written incrementally — roughly one subsection per tool call, per
the binding critical-mitigation against long single generations. §1.6's map row
gets an only-if-needed accuracy touch.

**Tech Stack:** Markdown only. Gates: `bunx tsc --noEmit`, `bun test`.

**Spec:** `docs/superpowers/specs/2026-09-02-EV-13-design.md` (binding; folds
rulings Q1/Q2/Q3, round-1–5 deliberation on `council/cards/EV-13.md`, and the
skeptic's writer obligations OBJ-1/5/7/8).

## Global Constraints

- Documentation only: `docs/SERVER-SIDE-SPEC.md` §5 + only-if-needed §1.6 map
  row touch + `council/cards/EV-13.md` step-8 record. No other section's
  normative text. No runtime code.
- Never reopen anything the design settles (rulings Q1/Q2/Q3 are binding).
- Incremental writes: bounded-length write/edit calls, ~one subsection each.
- Voice: match §1–§4 exactly — field tables with `| Field | Requirement |
  Meaning |`, closed-partition tables `| Surface | Partition |`, guidance as
  `> **Guidance (non-normative).` blocks, cross-references as `(§n.n)`,
  normative keywords capitalized.
- Writer obligations: §5.0 states closures precisely (one literal forward
  reference §4.1; one undefined concept §4.2 with no pointer); §5 carries its
  own closed-partition tables; `tunnel_not_granted` vs `tunnel_not_found`
  each defined exactly once; device-upgrade binding check uses **first
  segment** wording (OBJ-7); `GET /devices` excludes `deviceCredential`
  (OBJ-8); scope union exactly `{pi-remote:host, pi-remote:admin}` (Q1);
  upgrade's three REQUIRED lookup states named in order (Q2);
  `POST /devices` admin-minted (Q3); `403 tunnel_not_granted` stated as
  reserved — reachable by no MUST in v1.

---

### Task 1: Write §5.0–§5.1 (opening; device object and identity)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md` (append after §4.9)

- [ ] Append `## 5. Device registry, grants, push reservation, and the server-side trust model` with §5.0 opening (per design §1–§2: the four closures kept exact — §4.1 literal pointer, §4.2 undefined concept, §2.5/§2.7/§3.6 scope-grant debt, distinct-surface statement) and §5.1 (device object table, `deviceId` stability/no-recycle normative paragraph, device-credential normative shape).
- [ ] Commit: `docs(spec): begin §5 — device registry opening and device identity`

### Task 2: Write §5.2–§5.4 (registration; list; tenant membership)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md`

- [ ] §5.2 `POST /devices` (admin-minted per Q3; request/response tables; `200` not `201`; `deviceCredential` shown exactly once; unknown fields ignored; maxima guidance-grade).
- [ ] §5.3 `GET /devices` (response array; MUST NOT include `deviceCredential` — OBJ-8; closed partition `{200, 401, 403, 5xx}`).
- [ ] §5.4 tenant membership is the base grant (INV-3/INV-4 concrete; move = revoke + re-register).
- [ ] Commit: `docs(spec): §5.2–§5.4 — registration, listing, tenant membership`

### Task 3: Write §5.5–§5.6 (mint surface; device upgrade)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md`

- [ ] §5.5 `POST /device/connections` (two-surface model rationale; ordered checks 1–5; cross-tenant = `404 unknown_tunnel` never `403`; `403 tunnel_not_granted` stated as reserved; response `{url, tokenTtl}`; URL shape `wss://<host>/<tunnelId>/devices?token=<token>`; token claims table with `device_id`; TTL bounds per §3.3; identifier-not-capability guard).
- [ ] §5.6 device upgrade (ordered checks 1–6 per ruling Q2; first-segment binding wording — OBJ-7; three REQUIRED lookup states named in order with §3.4 two-state contrast; folded 401 branch; closed partition `{101, 400, 401, 404, 409, 5xx}`; no `403`; success binding = only source of stamped `deviceId`; reconnect contrast).
- [ ] Commit: `docs(spec): §5.5–§5.6 — device mint surface and upgrade`

### Task 4: Write §5.7–§5.9 (grants algorithm; revocation; pushProvider)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md`

- [ ] §5.7 grants and enforcement at fan-out (floor MUST; two-point enforcement; four-line algorithm; uplink half — no merge after revocation; ceiling MAY).
- [ ] §5.8 revocation `DELETE /devices/{deviceId}` (idempotent `204`; `404 unknown_device` never `403`; ordered effects a–d; retained record with the §3.5 contrast; no-recycle corollary; without host cooperation).
- [ ] §5.9 `pushProvider` reservation (closed union, default, `400` at set time; two observation points; no delivery behavior; closed-vocabulary growth note).
- [ ] Commit: `docs(spec): §5.7–§5.9 — grant enforcement, revocation, push reservation`

### Task 5: Write §5.10–§5.14 (admin surface; tenancy table; trust; guidance; vocabulary)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md`

- [ ] §5.10 admin surface (`pi-remote:admin` on enrollment point; `GET /devices`, `DELETE /devices/{deviceId}`, `POST /subjects/{sub}/scopes` with union exactly `{pi-remote:host, pi-remote:admin}` per Q1; grants tenant-scoped; partitions).
- [ ] §5.11 three credential contract points table (self-contained, per design §13).
- [ ] §5.12 trust summary from the server's side (self-contained; honest compromise statement; blast radius total across hosted tenants, zero beyond; host/device/admin rows).
- [ ] §5.13 guidance block (one shape each: storage sketch, admin ops, key ceremony, out-of-band discovery as named non-decision).
- [ ] §5.14 error vocabulary (`unknown_device`, reserved `tunnel_not_granted`; `tunnel_not_found` naming note; inherited codes) + §5's closed-partition table + `429` rule.
- [ ] Commit: `docs(spec): §5.10–§5.14 — admin surface, tenancy, trust, guidance, vocabulary`

### Task 6: §1.6 map-row accuracy touch (only if needed)

**Files:**
- Modify: `docs/SERVER-SIDE-SPEC.md` (§1.6 table, §5 row)

- [ ] Compare the current row ("Device registry, grants, and the server-side trust model | Client-device registration; tenant-scoped grants; …") against what §5 now actually covers (it adds the device data-plane connection surface and the pushProvider reservation). Touch only if inaccurate; keep the reader-path cell's function.
- [ ] Commit (with Task 7 or standalone): `docs(spec): sync §1.6 map row with §5`

### Task 7: Gates, card record, PR

**Files:**
- Modify: `council/cards/EV-13.md` (append `## Step-8 — owner implementation`)

- [ ] Gate 1: `bunx tsc --noEmit` — expect exit 0.
- [ ] Gate 2: `bun test` — expect 207 passing, 1 skip, 0 fail.
- [ ] Append the step-8 record (date; what was written — subsection list; gate results verbatim).
- [ ] Commit: `chore(council): EV-13 step-8 record`
- [ ] Push branch `feat/ev-13-device-registry-spec`; open PR with `gh` titled `feat(spec): device registry, grants, push reservation, and trust model`. Record PR number and headRefOid. Do not poll CI.
