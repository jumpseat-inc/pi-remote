# EV-1 Design Spec — Sync PI-SPEC with the /rc:login OAuth2 enrollment design

Card: `EV-1` (EPIC-1) — "Sync PI-SPEC with the /rc:login OAuth2 enrollment design and colon command naming"

Status: settled by Council deliberation (3 rounds), product-owner rulings Q1/Q2/Q3 applied and binding. This file is the **complete** design. If anything here is ambiguous, the design is wrong — so it is not.

## 0. What the card is

Docs-only. The single deliverable is a rewrite of parts of `docs/PI-SPEC.md`
to pin the OAuth2-based host-enrollment design and the colon command namespace
that downstream cards (EV-2, EV-7, EV-8) already assume. No source code
changes. No server-side build (the server stays out of this repo; §2's
dumb-relay mandate and §10's contract-surface list are unchanged).

**Files touched:** `docs/PI-SPEC.md` only — sections §7.2 (rewrite), §7.4
(prose sync), §7.5 (row 1 of the table only), §8 (rewrite), §9.1 (one clause
sync).

**Files that must NOT be touched:** `README.md` (its line 93 has a stale
`PI_REMOTE_HOST_KEY` reference — that is a filed follow-up card, not this
card), `src/` (does not exist and must not be created), every other doc.

## 1. The settled design (binding)

### 1.1 Grants

- **Attended (browser available):** Authorization Code + PKCE (RFC 7636),
  native-app pattern per RFC 8252. Public client `pi-remote`, **no client
  secret**. Loopback redirect `http://127.0.0.1:<ephemeral>/callback` bound
  to 127.0.0.1 only; `code_challenge_method=S256`; `scope=pi-remote:host`.
  The loopback listener exists only for the duration of the `/rc:login`
  command — never a background resource (§3 module rule, §7.1 outbound-only).
- **Unattended (headless):** RFC 8628 device authorization grant.
  `/rc:login --headless` POSTs the `device_authorization_endpoint`, prints
  `user_code` + `verification_uri_complete` (the single short value the user
  relays to another device — EV-7 pins this), then polls the `token_endpoint`
  with `grant_type=urn:ietf:params:oauth:grant-type:device_code`, honoring the
  full RFC 8628 semantics: `interval` between polls, `slow_down`,
  `authorization_pending` (keep polling), `expired_token`, `access_denied`.
  A manual-paste/headless-driver variant was explicitly rejected (bespoke,
  not automatable, no RFC behind it).
- Both flows exchange at the `token_endpoint`; the extension stores what the
  server issues.

### 1.2 Endpoint discovery (contract, like §5.3)

- RFC 8414 discovery: `GET /.well-known/oauth-authorization-server` against
  the configured server URL. **No endpoint paths are hardcoded in the spec.**
- The discovery metadata **must contain** — all three required contract
  fields: `authorization_endpoint`, `token_endpoint`,
  `device_authorization_endpoint`.
- `revocation_endpoint` (RFC 7009) is optional.
- Refresh is `grant_type=refresh_token` at the `token_endpoint` (RFC 6749 §6).
  **There is no separate `POST /refresh` endpoint** — that was designer's
  rejected round-1 invention (Skeptic O-4, closed-green on the settled
  design).
- `POST /tunnels` (existing §7.2 surface, payload unchanged: session id,
  session name, cwd, host metadata) is authenticated with
  `Authorization: Bearer <access_token>` — a short-TTL access token, never a
  static key.

### 1.3 Credential storage

Extension settings, **user-only readability** (EV-7 pins this):
`piRemote.serverUrl`, `piRemote.accessToken` (short-TTL),
`piRemote.refreshToken` (long-lived, revocable at the control plane),
`piRemote.tokenExpiry`, `piRemote.tenantId` (cached from the token).

- A failed flow writes nothing half-written; re-running `/rc:login` replaces
  the stored credential cleanly.
- Credentials are **never carried in environment variables** — not even as a
  documented override.

### 1.4 Env override (ruling Q1 — binding)

**`PI_REMOTE_HOST_KEY` is retired entirely.** It must not appear anywhere in
`docs/PI-SPEC.md` — not as a primary path, not as an override, not as a
mention. The **only** env override is `PI_REMOTE_SERVER_URL` (server URL). The
settings-based OAuth2 enrollment is the documented path; the env override is
a documented override only.

### 1.5 `/rc` behavior (unenrolled / expired)

- No credential exists → `/rc` **refuses to dial**, footer shows
  `not enrolled`, output names the next step (run `/rc:login`). No inline
  enrollment prompt — OAuth enrollment is never attempted from `/rc`;
  it is `/rc:login`'s job.
- Credential exists but access token expired → **one silent refresh**
  (`grant_type=refresh_token` at `token_endpoint`); if there is no refresh
  token, or the refresh fails, don't dial and name the `/rc:login` remedy.
- Idempotent: if already connected, notify and no-op (EV-8 depends on this).

### 1.6 Command surface

`/rc`, `/rc:login`, `/rc:off`. **`/rc-off` must not appear anywhere in the
doc.** `/rc:login --headless` selects the device flow; plain `/rc:login` is
attended.

### 1.7 Footer state machine (ruling Q2 — binding)

Exactly **seven states, in lifecycle order**: `off` → `not enrolled` →
`authorizing` → `dialing` → `resyncing` → `live` → `error`. The §8 status
surface must enumerate all seven. Distinctions that matter:

- `not enrolled` ≠ `off` — it tells the user the next step is `/rc:login`.
- `resyncing` is a **seventh, healthy state** — replay in progress (§5),
  distinct from `dialing` (no fresh connection) and from `live` (no new
  frames, only replay). It is NOT merged into `error` and NOT dropped
  (EV-8's recorded acceptance commits to showing resyncing during replay).
- `error` is terminal with the reason.

### 1.8 Trust model (ruling Q3 — binding)

§7.4/§9.1/§7.5-row-1 updates are **in-mandate prose sync**, no ratification
needed. The **blast radius is preserved and unchanged**: a leaked
credential lets an attacker create tunnels for sessions they can already see
locally (they are on the host), grants no session access by itself, and is
contained to its own tenant; the refresh token is additionally revocable at
the control plane. Tenancy now derives from token claims: §7.5 row 1 changes
from "key belongs to a tenant" to "token `sub` claim is tenant-scoped".

## 2. Target text — replacement blocks in docs/PI-SPEC.md

The owner's job is to replace exactly the blocks below, in place, and touch
nothing else. Each block is the complete replacement for the named section
(verbatim — do not reword, the greps in §3 check these).

### 2.1 §7.2 Host enrollment (full section replacement)

Replace the entire current §7.2 (heading through the final "the connection
itself is the capability." bullet) with:

```
### 7.2 Host enrollment

An alternative design — *host-minted, server-signed* tokens — forces a
signing key distribution problem before anything works. **Chosen instead: the
server mints and signs; the host never holds a signing key.** The host's
identity with the control plane is an **OAuth2 enrollment credential**
obtained via `/rc:login` (§8) and persisted in extension settings — never a
static key, never an environment variable.

- **Endpoint discovery.** All control-plane endpoints are derived at runtime
  from RFC 8414 discovery (`GET /.well-known/oauth-authorization-server`
  against the configured server URL); no endpoint paths are hardcoded. The
  discovery metadata must include `authorization_endpoint`,
  `token_endpoint`, and `device_authorization_endpoint` — all three are
  required contract fields. `revocation_endpoint` (RFC 7009) is optional.
- **Attended enrollment (browser available) — Authorization Code + PKCE
  (RFC 7636, native-app pattern per RFC 8252).** `/rc:login` opens the
  default browser at `{authorization_endpoint}` with `client_id=pi-remote`,
  `response_type=code`, `code_challenge_method=S256`, a generated
  `code_challenge`, `redirect_uri=http://127.0.0.1:<ephemeral>/callback`,
  `scope=pi-remote:host`, and `state`. The extension is a **public client** —
  no client secret. The loopback redirect is bound to 127.0.0.1 only, and
  the listener lives for the duration of the `/rc:login` command only
  (§7.1 — the extension never listens otherwise).
- **Unattended enrollment (headless host) — Device Authorization Grant
  (RFC 8628).** `/rc:login --headless` POSTs `{device_authorization_endpoint}`
  to obtain a device code, prints `user_code` and `verification_uri_complete`
  — the single short value the user relays to another device — then polls
  `{token_endpoint}` with
  `grant_type=urn:ietf:params:oauth:grant-type:device_code`, honoring the
  RFC 8628 semantics: `interval` between polls, `slow_down`,
  `authorization_pending` (keep polling), and the terminal conditions
  `expired_token` and `access_denied` (failure names the `/rc:login` remedy).
- **Token issuance and refresh.** Both flows exchange at `{token_endpoint}`
  (`grant_type=authorization_code` with `code_verifier`, or the device-code
  grant). When the response includes a refresh token, the extension stores it
  and refreshes silently at tunnel time with
  `grant_type=refresh_token` at `{token_endpoint}` — there is no separate
  refresh endpoint.
- **Credential storage.** The enrollment credential is persisted in
  **extension settings** with user-only readability: `piRemote.serverUrl`,
  `piRemote.accessToken` (short-TTL), `piRemote.refreshToken` (long-lived,
  revocable at the control plane), `piRemote.tokenExpiry`, and
  `piRemote.tenantId` (cached from the token). A failed flow writes nothing
  half-written; re-running `/rc:login` replaces the stored credential
  cleanly. The token authorizes *creating tunnels*, nothing else.
- **Environment override (documented override only).** The control-plane
  server URL may be overridden with the `PI_REMOTE_SERVER_URL` environment
  variable. **Credentials are never carried in environment variables.**
  Settings-based OAuth2 enrollment above is the documented path; the env
  override exists for the server URL only.
- `/rc` flow:
  1. Extension `POST /tunnels` to the control plane with
     `Authorization: Bearer <access_token>`, payload: session id, session
     name, cwd, host metadata.
  2. Server responds with `{ tunnelId, url, tokenTtl }` — a **signed,
     expiring `wss://` URL with a one-time token**
     (`wss://server/tunnelId?token=…`). The token is self-describing: it
     embeds its claims (`tenantId`, `tunnelId`, `sessionId`, `exp`), so the
     server needs no lookup state to authenticate a dial.
  3. Extension dials the URL within the token TTL (default 60 s). The token
     is **single-use**: consumed on successful WS upgrade, then bound to that
     socket. A replayed URL is rejected.
- The host stores no tunnel secrets after connection: the token is discarded,
  and the connection itself is the capability.
```

### 2.2 §7.4 Trust summary (table + blast radius)

Replace the §7.4 table rows and the blast-radius paragraph:

```
| Component | Holds | Can do |
|---|---|---|
| Host (extension) | OAuth2 access/refresh token (from `/rc:login`) | create tunnels (Bearer-authenticated), dial out, translate, inject |
| Server | authorization-server (enrollment) registry, device registry, signing key | issue/revoke enrollment credentials, mint/revoke tunnel tokens, enforce device grants, relay frames |
| Client device | device credential | connect to granted tunnels, ack, resume, send input |
```

And:

```
Compromise blast radius: a leaked access or refresh token lets an attacker
**create tunnels for sessions they can already see locally** (i.e., they are
on the host) — it does not grant session access by itself, and its damage is
contained to its own tenant; the refresh token is additionally revocable at
the control plane. A leaked tunnel token is bounded by its TTL and single-use
property, and only reaches its own tunnel.
```

(The §7.4 heading and any prose between table and blast-radius paragraph
stay as they are.)

### 2.3 §7.5 Multi-tenancy — row 1 of the table

Replace only the first table row:

```
| Enrollment credential (§7.2) | token `sub` claim identifies the tenant-scoped account | opaque credential, presented as Bearer at `POST /tunnels` |
```

The §7.5 intro sentence and the other two rows are unchanged.

### 2.4 §8 Lifecycle & command surface (full section replacement)

Replace the entire current §8 through the status-surface paragraph:

```
## 8. Lifecycle & command surface

| Command | Behavior |
|---|---|
| `/rc` | If no enrollment credential exists, **refuse to dial** and output a line naming the next step (`run /rc:login`); footer state `not enrolled`. If enrolled but the access token is expired, perform **one silent refresh**; if there is no refresh token or the refresh fails, output the same `/rc:login` remedy and do not dial. Otherwise `POST /tunnels` (§7.2), dial the signed URL, and start translating live events. Idempotent: if already connected, notify and no-op. OAuth enrollment is never attempted from `/rc` — that is `/rc:login`'s job. |
| `/rc:login` | Enroll the host with the control plane's OAuth2 authorization server: the **attended** flow (default) opens the default browser (Authorization Code + PKCE, §7.2); the `--headless` flag runs the RFC 8628 device flow and prints `user_code` + `verification_uri_complete`. Persists the credential in extension settings; on failure, prints what to do next. |
| `/rc:off` | Close the WS, notify the control plane (`DELETE /tunnels/:id`), discard token state. Idempotent. |
| `session_shutdown` handler | Tear down the tunnel for **every** shutdown reason (`quit`, `reload`, `new`, `resume`, `fork`) — exiting without `/rc:off` must not leave a live tunnel. Idempotent with `/rc:off`. |

Status surface: footer status via `ctx.ui.setStatus("pi-remote", …)` showing
exactly one of seven states, in lifecycle order: `off` → `not enrolled` →
`authorizing` → `dialing` → `resyncing` → `live` → `error`.

- `off` — no tunnel, no credential action pending.
- `not enrolled` — no credential; distinct from `off` because it names the
  next step (`/rc:login`).
- `authorizing` — OAuth enrollment in progress (browser or device flow).
- `dialing` — dialing the signed `wss://` URL.
- `resyncing` — replay in progress (§5); a healthy phase distinct from
  `dialing` (the connection is already up) and from `live` (no new frames,
  only replay).
- `live` — connected; frames flowing.
- `error` — terminal failure state with the reason shown.

The host user can always see that the session is remotely reachable.
```

### 2.5 §9.1 — one clause

In resolved-design-question 1, replace "the host holds only the long-lived
host enrollment key used to request tunnels" with "the host holds only the
OAuth2 access/refresh credential obtained from `/rc:login`". Nothing else in
§9 changes.

## 3. Post-change gates (must all pass; the Skeptic re-runs these at step 9)

Run from the repo root against the modified `docs/PI-SPEC.md`:

| # | Command | Expected |
|---|---|---|
| G-1 | `grep -n 'rc-off' docs/PI-SPEC.md` | exit 1 (zero matches); §8 lists `/rc`, `/rc:login`, `/rc:off` |
| G-2 | `grep -n 'PI_REMOTE_HOST_KEY' docs/PI-SPEC.md` | exit 1 (zero matches — ruling Q1) |
| G-3 | `grep -n 'Bearer' docs/PI-SPEC.md` | ≥1 hit inside §7.2 (`Authorization: Bearer <access_token>` for `POST /tunnels`) and `grep -n 'host key' docs/PI-SPEC.md` has zero matches inside §7.2's tunnel-create description |
| G-4 | `grep -inE 'authorization_code\|PKCE\|code_challenge\|device_authorization_endpoint\|device_code\|grant_type=refresh_token\|RFC 8414\|token_endpoint' docs/PI-SPEC.md` | ≥3 distinct terms in §7.2 (deliberation text has all of them) |
| G-5 | `grep -nE 'off\|not enrolled\|authorizing\|dialing\|resyncing\|live\|error' docs/PI-SPEC.md` | all seven states appear in §8, enumerated in lifecycle order (ruling Q2) |
| G-6 | `grep -n '/rc:login' docs/PI-SPEC.md` | ≥1 hit (and G-1 already ensures no `/rc-off`) |
| G-7 | §7.2 `POST /tunnels` step contains `Bearer` and not `host key` | covered by G-3 |
| G-8 | `grep -nE 'inline\|prompt.*once\|enrollment key if missing' docs/PI-SPEC.md` | exit 1 (zero matches — no inline/prompt-once language in `/rc` behavior) |
| G-9 | `grep -inE 'post.*/refresh' docs/PI-SPEC.md` | exit 1 (refresh is `grant_type=refresh_token` at `token_endpoint`, no separate endpoint — O-4) |
| G-10 | `grep -n 'device_authorization_endpoint' docs/PI-SPEC.md` | ≥1 hit, stated as a required contract field in §7.2 discovery (O-5) |
| G-11 | `grep -n 'host enrollment key' docs/PI-SPEC.md` | exit 1 (zero matches — §7.4/§9.1 synced) |
| G-12 | §7.5 row 1 | contains "`sub` claim" (tenancy from token claims — ruling Q3) |
| G-13 | `git diff` of `docs/PI-SPEC.md` | hunks confined to §7.2, §7.4, §7.5 (row 1), §8, §9.1 — nothing in §4–§6, §5.3, §7.1, §7.3, or §10 |

Acceptance mapping: G-1+G-6 → "§8 command table lists `/rc`, `/rc:login`,
`/rc:off` with no `/rc-off` anywhere"; G-2+G-8 → "settings-based OAuth2
enrollment is the documented path, env vars at most a documented override";
G-4+G-10+G-9 → "both attended and unattended login modes specified with
control-plane endpoints listed as contract (like §5.3)"; G-13 →
"no other section's wire contract altered".

Repo-wide gates (CI `gates` workflow, and the owner runs them locally in its
worktree): `bunx tsc --noEmit` and `bun test`. This repo is docs-only so far —
both stay green; a docs change cannot regress them. Note: whenever the owner
sees a gate that does not exist in this repo (the packaged owner seat names
an import smoke and `/healthz` boot from another codebase), those do NOT
apply here — this card has no `src/`, no server, no database; the only gates
are the greps above plus typecheck/tests.

## 4. Explicit non-goals (from the deliberation record)

- No `POST /refresh` endpoint, no hardcoded `/authorize`/`/token`/
  `/device_authorization` paths, no boot-time credential-exchange endpoint
  (designer's r2 idea — rejected, §2 dumb relay).
- No `PI_REMOTE_HOST_KEY` anywhere (Q1) — including README (separate
  follow-up).
- No change to EV-8's card file; EV-1 only defines the authoritative §8 state
  set so EV-8 can sync later.
- No `src/` code, no package.json changes, no new npm dependencies.
- §4–§6, §5.3, §7.1, §7.3, §10: untouched, word for word.